# 当前计划任务

更新时间：2026-06-30

本文是后续继续整理计划任务的入口。上一轮 M460/600 采集主线任务已审查归档：`archive/completed-plans/当前计划任务_20260630_M460主线收口.md`。

## 执行入口规则

1. 默认执行范围只限“当前待办”里的 `TASK-*-FUNC-*` 功能任务；未明确点名时，不执行“交给其他 AI 的验证任务”和“可分派低复杂度开发任务”。
2. 归档计划只作历史追踪，不作为新会话自动执行来源；需要恢复任务时，先把结论合并回本文“当前待办”。
3. 开始前必须查看 `git status --short`，不得整理、回滚或提交非本轮目标文件，尤其不要处理 `.codegraph/`、数据库文件和其他 AI 正在修改的测试文件。
4. 文本文件按 UTF-8 无 BOM + LF 写入；Windows sandbox 下 `apply_patch` 不可用时，使用 .NET `UTF8Encoding(false)` 精确写入，写后执行 `git diff --check` 并检查文件头。
5. 本文“当前待办”已有下一轮可执行功能主线；用户要求“继续”且未点名任务时，按 P0 到 P3 顺序执行 `TASK-*-FUNC-*`，不要从归档文件中自行挑任务执行。

## 当前待办

### P0：测试计划独立采集命令适配（已完成）

1. `TASK-M460-OPTPLAN-FUNC-001`：已完成。`BatteryOptCollectorCommandAdapter` 已存在于 `manage/opt/service`，负责把 `DevBatteryOpt` 的 `_2` 连接条电阻测试、`_6` 单节内阻测试尝试映射到 `BatteryCollectorCommandService` 独立采集命令；开关关闭或不支持时保留旧链路兜底。来源任务：`energy_refactor_current_plan_20260622.md` 的 `TASK-AI-DIRECT-013`。
   - 实际位置：`03code/energy/src/main/java/com/shanhe/project/manage/opt/service/BatteryOptCollectorCommandAdapter.java`。
   - 验收：`mvn "-DskipTests" compile`、`git diff --check`，并检查新增文件无乱码。
2. `TASK-M460-OPTPLAN-FUNC-002`：已完成。`/batteryOpt/doCmdOptBatteryTest` 继续走 `ControlBattery.executeBatteryOpt` 统一入口，`ControlBattery.toSendBatteryCmdToOat` 在上报/告警/测试条件校验后优先调用 `BatteryOptCollectorCommandAdapter.tryExecute(opt)`；返回空时保持旧 980 链路兜底。来源任务：`energy_refactor_current_plan_20260622.md` 的 `TASK-AI-DIRECT-014`。
   - 依赖：必须先完成 `TASK-M460-OPTPLAN-FUNC-001`。
   - 实际边界：不在 controller 直接调用适配器，避免绕过 `ControlBattery` 前置校验；不改 `/batteryOpt/edit` 和旧 980 命令生成。
   - 验收：`mvn "-DskipTests" compile`、`git diff --check`，并检查目标文件无乱码。
3. `TASK-M460-OPTPLAN-FUNC-003`：已完成。`BatterySyncHandler.syncBatteryOpt` 的立即执行分支复用 `ControlBattery.executeBatteryOpt` 页面执行链路，不再保留内部重复的 `_2/_6` 采集命令映射逻辑；适配服务返回空时由 `ControlBattery` 继续旧链路兜底。来源任务：`energy_refactor_current_plan_20260622.md` 的 `TASK-AI-DIRECT-015`。
   - 依赖：必须先完成 `TASK-M460-OPTPLAN-FUNC-001`。
   - 修改范围：只改 `BatterySyncHandler`，不改计划/配置同步分支和 `MethodEnum._43` 返回语义。
   - 验收：`mvn "-DskipTests" compile`、`git diff --check`，并确认 `tryCollectorCommand|executeCollectorCommand|resolveBatteryCount` 不再作为重复逻辑保留。

### P1：旧 M460 指令链路串联复核

1. `TASK-M460-TRACE-FUNC-001`：已完成初步复核。旧链路必须按“energy 外层 `TcpCidEnum._54` + `BatteryCidEnum` -> M460 `Server_Command_Execute`/`RS485_Command_Execute` -> 是否进入 `Protocol_Package_Set_Command` 或 `Verify_Capacity_Package_Set_Command`”串联判断，不能只看 energy 的测试类型枚举。
2. `TASK-M460-TRACE-FUNC-002`：整理 `_1` 整组内阻测试迁移方案。已确认 energy 立即执行 `_1` 发送 `0x05/SERVER_SET_SYSTEM_STATE`，参数 `0x79/E_SYS_MANUAL_INTERNAL_RESISTANCE_TEST`；M460 经 `Server_Set_System_State_Function` 调用 `Protocol_Package_Set_Command(..., E_4G_CHANNEL)`，再由 `protocol_package.c` 的 `Sys_Set_Manual_Internal_Resistance_Test_Process` 逐节调用 `Internal_Resistance_Test_Package_Process` 和 `Send_Command_To_Submodule`。后续不能简单把 `_1` 等同 `_6`，需要补整组地址推进、浮充继电器状态、5 分钟间隔、结果记录和 `dev_opt_log/battery_device_state` 口径。
3. `TASK-M460-TRACE-FUNC-003`：整理 `_3/_5/stop` 核容/备电模块迁移方案。已确认 energy `_3`、`_5`、停止均发送 `0x30/E_Sys_Set_Verify_Capacity_Operation_Mode`，模式分别为 `2` 核容、`1` 备电、`4` 停止；M460 进入 `Server_Set_Verify_Capacity_Operation_Mode` / `RS485_Set_Verify_Capacity_Operation_Mode` 后调用 `Verify_Capacity_Package_Set_Command`，下游是核容/空开/容量模块 Modbus 路径，不是 600 单体采集模块命令。后续需单独建“核容/备电模块适配”任务，不并入 600 采集命令队列。
4. `TASK-M460-TRACE-FUNC-004`：整理 `_4/_7` 边界。已确认 `_4` 计划配置发送 `0x31/E_Sys_Set_Float_Charging_Parameters`，M460 会写 `Lcd_Floating_Charge_Management`/Flash，且在 `Verify_Capacity_Function` 开启时可能转发到核容模块；当前 energy 没有 `_7` 充电测试命令生成，`_7` 主要由充电状态派生为日志/状态类型，固件侧只在 `0x30` 工作模式中存在充电状态候选。后续不得凭 `_7` 枚举直接新增命令。
5. `TASK-M460-TRACE-FUNC-005`：整理 `31..35` 计划配置命令。已确认 `_4/_1/_2/_3/_5` 计划配置分别对应 `0x31/0x32/0x33/0x34/0x35`。其中 `0x32..0x35` 主要写 `Lcd_Operational_Tasks_Parameter` 到 SPI Flash 并响应，后续由 M460 operational tasks 周期调度；`0x31/_4` 浮充配置写 `Lcd_Floating_Charge_Management` 到同一配置结构/Flash，但不是后续定时测试项，配置入口本身在 `Verify_Capacity_Function` 开启时会转发到核容模块。后续 `/batteryOpt/edit` 是否继续只保存计划，需要结合 energy 已有 `BatteryOptScheduleJob`、核容功能编译开关和现场平台兼容确认。

6. `TASK-M460-TRACE-FUNC-006`：已完成 `0x31..0x35` 计划配置后续执行复核。结论：`0x31/_4` 与 `0x32..0x35` 都保存到 `Lcd_Operational_Tasks_Parameter`，但执行模型不同；`0x31/_4` 是浮充参数配置，配置入口即按条件转发核容模块，不进入后续 120s 定时测试；`0x32..0x35` 本身只保存并响应，不会在配置入口立即执行测试，真正执行发生在 M460 `capacity.c` 的 `vSys_Verify_Capacity_Task`，每轮调用 `Battery_Array_Verify_Capacity_Operational_Tasks_Automitic_Test_Process`，约 `300*400ms = 120s` 进入 `Operational_Tasks_Automitic_Test_Process` 扫描 1..4 组电池并按保存参数触发。
   - `0x31/_4` 浮充配置：server/rs485 入口 `Server_Set_Float_Charging_Parameters` / `RS485_Set_Float_Charging_Parameters` 调用 `Set_Float_Charging_Parameters`，把 `Float_Charging`、截止电压、恢复电压写入 `Lcd_Floating_Charge_Management` 并保存 Flash；`Verify_Capacity_Function` 开启时，`RS485_Set_Float_Charging_Parameters` 会调用 `Verify_Capacity_Package_Set_Command(Command_Package, type)`。LCD 本地入口 `Lcd_Read_Floating_Charge_Management_Data` 还会保存 `Float_Charging_Duration`，并通过 `Send_Floating_Charge_Management_Data_To_Verify_Capacity(Array, p)` 构造 `E_Sys_Set_Float_Charging_Parameters` 发送到核容模块。该链路属于浮充/核容模块配置转发，不调用 `Protocol_Package_Set_Command`，也不下发 600/submodule 采集命令。
   - `0x32/_1` 定时整组内阻：`Operational_Tasks_Internal_Resistance_Automitic_Test` 满足到点、启用、间隔天数、当前 `-1A..5A`、均衡未开启后，调用 `tft.c` 的 `Lcd_Battery_Array_Internal_Resistance_Test_Process(Array)`；该函数构造 `SERVER_SET_SYSTEM_STATE + E_SYS_MANUAL_INTERNAL_RESISTANCE_TEST` 并调用 `Protocol_Package_Set_Command(..., E_LCD_CHANNEL)`。后续进入 `protocol_package.c` 的整组内阻状态机，逐节走 `Internal_Resistance_Test_Package_Process` / `Send_Command_To_Submodule`，属于 600/submodule 采集链路；与 4G 立即 `_1` 的下游测试状态机一致，但入口 channel 和触发条件不同。
   - `0x33/_2` 定时连接条电阻：`Operational_Tasks_Connect_Resistance_Automitic_Test` 满足到点、启用且 `fabs(Current) >= 5A` 后，调用 `Lcd_Connect_Resistance_Test_Process(Array)`；该函数构造 `SYS_CONNECT_RESISTANCE_TEST` 并调用 `Protocol_Package_Set_Command(..., E_LCD_CHANNEL)`，后续进入 `Sys_Connect_Resistance_Test_Process`，属于 600/submodule 采集链路；与新 `_2` 独立采集命令目标一致，但旧 M460 会额外依赖电流阈值、告警屏蔽位和 Flash 计划推进。
   - `0x34/_3` 定时核容：`Operational_Tasks_Battery_Capacity_Automitic_Test` 满足到点、启用、间隔天数后，调用 `Lcd_Battery_Operational_Tasks_Test_Process(E_Verify_Capacity_Staus, Array)`；该函数构造 `0x30/E_Sys_Set_Verify_Capacity_Operation_Mode` 并调用 `Verify_Capacity_Package_Set_Command(..., E_LCD_CHANNEL)`，下游是核容/空开/容量模块 Modbus 路径，不是 600 采集链路；与 4G 立即 `_3` 的下游模块一致，但参数来自 `Lcd_Battery_Capacity_Test` 保存值。
   - `0x35/_5` 定时备电时长：`Operational_Tasks_Power_Support_Time_Automitic_Test` 满足到点、启用、间隔天数、空开设备在线且未远程断开、当前状态非备电后，先推进 Flash 时间，再调用 `Lcd_Battery_Operational_Tasks_Test_Process(E_Backup_Power_Status, Array)`；同样进入 `Verify_Capacity_Package_Set_Command(..., E_LCD_CHANNEL)`，下游是核容/空开/容量模块 Modbus 路径，不是 600 采集链路。
   - 对 energy 整合影响：`BatteryOptScheduleJob` 当前由平台按 `DevBatteryOpt.testTime/isEnabled` 调 `ControlBattery.executeBatteryOpt(...SCHEDULED)`，它不是旧 M460 `0x32..0x35` 的设备内置 120s 轮询，也不覆盖 `0x31/_4` 的浮充参数即时配置转发。若继续保留 `/batteryOpt/edit` 下发旧配置，需要承认设备和平台可能存在两套计划触发源；若迁移到平台调度，应逐步停止依赖 `0x31..0x35` 旧配置下发，并按测试类型分别接入 600 采集链路、浮充/核容模块配置适配器或核容模块执行适配器。

### P2：以 `ControlBattery` 为主线迁移 M460 测试链路

1. `TASK-M460-CONTROLBATTERY-FUNC-001`：已完成。把后续 M460 测试迁移入口统一收敛到 `ControlBattery.executeBatteryOpt` / `toSendStopBatteryCmdToOat`，manual、scheduled、sync 只能复用该入口，不在 controller、`BatterySyncHandler` 或 `CmdBatteryControlService` 旁路新增执行逻辑。当前 `_2/_6` 已通过 `BatteryOptCollectorCommandAdapter` 优先接入 600 队列，旧 980 命令只作为开关关闭或无法处理时兜底。
   - 实际边界：本次已整理 `ControlBattery` 内部执行分派、`executeType` 透传、新链路尝试点、stop 分派和旧 M460 fallback 注释；未改旧配置保存接口 `/batteryOpt/edit`，未迁移 `_1/_4/_3/_5` adapter。
   - 验收：manual、scheduled、sync 三入口均只调用 `ControlBattery.executeBatteryOpt`；`mvn -DskipTests compile`；`git diff --check`。
2. `TASK-M460-CONTROLBATTERY-FUNC-002`：进行中。迁移 `_1` 整组内阻测试。旧链路下游是 `Protocol_Package_Set_Command` -> 600/submodule 整组内阻状态机，不等同 `_6` 单节内阻；当前已在 `ControlBattery` 主线下接入 `_1` 适配入口和 `BatteryCollectorCommandService.groupInternalResistanceTest` 稳定方法，但因 600 显式控制层尚无等价整组状态机，当前会安全回退旧 M460 链路。
   - 已完成：`BatteryOptCollectorCommandAdapter` 已识别 `_1` 并调用 `groupInternalResistanceTest`；该方法明确拒绝把 `_1` 降级为 `_6` 循环，能力未就绪时返回回退信息，由 `ControlBattery.generateCommand(_1)` 继续旧 M460 fallback。
   - 待补：实现等价旧 M460 整组状态机的 600 显式控制流程，包括逐节地址推进、5 分钟间隔/运行状态、结果闭环和 `dev_opt_log/battery_device_state` 口径；不得把 `_1` 简化成循环调用 `_6`，不得绕过 `ControlBattery`。
3. `TASK-M460-CONTROLBATTERY-FUNC-003`：已完成边界收敛。迁移 `_4` 浮充配置/浮充测试边界。旧 `0x31/_4` 是 `Lcd_Floating_Charge_Management` 保存并按 `Verify_Capacity_Function` 转发核容模块，不是 600 采集命令，也不是 120s operational tasks 定时测试项。
   - 已完成：`ControlBattery` 执行主线已对 `_4` 返回明确边界提示，避免把 `_4` 当未知测试类型或误接入 600 采集队列；`0x31` 仍保留在旧配置保存链路。
   - 后续：如业务要求迁移 `/batteryOpt/edit` 的 `0x31` 下发，再单独实现浮充/核容模块配置 adapter；不得把 `_4` 接入 `BatteryCollectorCommandService` 600 采集队列，不得凭 `_7` 枚举推导新增充电测试命令。
4. `TASK-M460-CONTROLBATTERY-FUNC-004`：进行中。迁移 `_3/_5/stop` 核容/备电执行链路。旧链路统一是 `0x30/E_Sys_Set_Verify_Capacity_Operation_Mode`，下游 `Verify_Capacity_Package_Set_Command` -> 核容/空开/容量模块 Modbus，不是 600 采集链路。
   - 已完成：新增 `BatteryOptCapacityModuleCommandAdapter` 作为 `_3/_5/stop` 的固定插入点，`ControlBattery` 会先尝试核容/备电模块 adapter；当前尚无等价新模块命令通道时返回 null，继续旧 `genCmd30` / `CommServer.returnCmd` fallback。
   - 待补：实现等价 `0x30` 的核容/空开/容量模块新命令通道和回执闭环；当前未并入 `BatteryOptCollectorCommandAdapter` 的 600 命令队列，未改变 stop 下发后立即关闭 `_3/_5` running log 的兼容行为。
5. `TASK-M460-CONTROLBATTERY-FUNC-005`：已完成本轮硬化。`BatteryOptScheduleJob` 继续复用 `ControlBattery.executeBatteryOpt(...SCHEDULED)`，不新增 timer；本轮只收口计划触发边界，不混入各测试类型 adapter 的等价实现。
   - 已完成：`_1` 整组内阻计划补入 `BatteryModeStatusService.MODE_INTERNAL_RESISTANCE` 互斥判断，与 `_6` 单体内阻共用运行态口径，避免平台计划在内阻测试运行中重复触发。
   - 已完成：`_4/0x31` 浮充属于参数配置/转发边界，不作为平台定时执行命令；到点时记录 `SKIPPED` 并按既有 `updateNextSchedule` 推进下一次或关闭一次性计划，避免每分钟反复失败。
   - 保留：失败结果仍不推进 `testTime`，成功执行后按既有逻辑推进间隔或禁用一次性计划；`/batteryOpt/edit` 旧 `0x31..0x35` 配置下发行并未在本任务中改动。

### P3：Modbus 和告警方案拆分

1. `TASK-M460-MODBUS-FUNC-001`：已完成本轮只读梳理。当前 Modbus RTU 读入口是 `ModbusRtuServer.processReadHoldingRegisters` -> `BatteryModuleModbusReadMappingService.readHoldingRegisters`，支持 0x03 读保持寄存器；默认仍按文档参考寄存器号映射，不在本轮新增寄存器。
   - 当前读范围：单体电压 `410004..410248`、单体内阻 `410252..410496`、单体温度 `410500..410744`、单体鼓包电压 `410748..410992`；设备状态 `411483..411488`；组测量/容量状态 `411729..411752`、`411762..411766`。
   - 数据来源：优先 `BatteryModuleRealtimeSnapshotService.getCachedSnapshot(packNum)`；缺失时回落 `BatteryModuleRealtimeMapper.selectCells/selectGroup`。设备状态寄存器来自 `BatteryDeviceStateService` / `battery_device_state`，包括通道打开、通道异常、模块活跃、模块超时、246 新鲜度、工作模式。
   - 草案差异/风险：读映射类注释仍保留 SH 980 表述，但代码已混入 M460 `Battery_State_Register` 编码和 600 实时快照数据源；容量/SOC/SOH/备电时长等地址依赖轮询外计算缓存，缺失时抛未就绪，不应伪造 0。
   - 高频读风险：实时单体/组数据已有内存快照，主要风险在 `411483..411488` 连续状态寄存器读取时逐地址调用 `BatteryDeviceStateService` 产生多次 SQL；后续可做最小开发任务，把 pack/channel 状态摘要一次加载进 `ModbusReadSnapshot`。
   - 无来源策略：首包实时数据完全未就绪时抛 `IllegalStateException`，由 RTU 层转为 Modbus 异常 `0x04`；缺单体值返回 `0`；普通组实时字段缺组数据返回 `0`；SOC/SOH/容量/备电等状态字段缺组或缺值时抛未就绪，不伪造为 `0`。
   - 告警边界：旧 JSON/TCP `87/8D` 只作为兼容解析链路；M460/600 新链路应从 `BatteryModuleAlarmAdaptService`、`BatteryDeviceStateService` 和实时快照派生，不得直接复用旧 `BatteryAlarmHandler` / `BatteryAlarmBitMapping` 的 87/8D bit 位图作为 Modbus 新寄存器来源。
   - 暂缓读范围：`SH_Modbus寄存器映射草案.md` 中的 `410994..411237`、`411238..411482`、`412580`、`413561..413563` 等告警/故障内容寄存器尚未实现；参数区 `400xxx` 与 `400421` 报警屏蔽也需先映射标准参数/告警配置模型。
   - 旧告警缺口：旧 `87` 位图中的 `zfcdygd/zfcdygg/ztlmxgj/dtdckl/dtfcdydt/dtfcdyg/dtnzbj/dtdcwdbj/dtdybj`、旧 `8D` 位图中的 `zwlgz/ztdgj/zwdcgq1gz/zwdcgq2gz/dtljtgj/dtwdcgqgz` 等缺少已确认的新实时字段或设备状态来源；`dtgb/dtlygj/dttxzt/txzt` 虽已有部分新来源，但语义不是完整复刻旧位图。
   - 可交给其他 AI 开发：`TASK-M460-MODBUS-FUNC-001-A` 已完成，`411483..411488` 状态寄存器改为请求级 `selectByPackNum` 快照，连续读取从最多 6 次状态 SQL 收敛为 1 次；`TASK-M460-MODBUS-FUNC-001-B` 修正文档/注释中的 SH 980/M460 边界命名；`TASK-M460-MODBUS-FUNC-001-C` 复核 `BatteryModuleModbusReadMappingServiceTest` 现有覆盖并补缺口，重点看暂缓地址拒绝、RTU 异常码映射和状态寄存器热路径。
   - 禁止：未确认 bit 语义前不得新增告警位图寄存器；不得新增参数寄存器；不得把旧 980 `87/8D` 当成 M460 Modbus 读寄存器的直接数据源。
2. `TASK-M460-MODBUS-FUNC-002`：已完成本轮只读梳理。当前写侧入口是 `ModbusRtuServer.processWriteSingleRegister` -> `ModbusWriteMappingService.writeSingleRegister`，仅支持功能码 `0x06` 写单寄存器；`0x10` 写多个寄存器直接返回非法功能。
   - 当前白名单：只允许写参考寄存器 `404915`，语义为单体均衡控制；写入值高字节为 `balanceValue`，低字节为 `modelNum`，单体编号限制 `1..245`。
   - 内部调用：`ModbusWriteMappingService` 通过 `BatteryCollectorCommandService.resolveChannelName(packNum)` 找采集通道，再调用 `singleBatteryBalance(channelName, packNum, modelNum, balanceValue, null)`；成功语义是“命令已入队”，不表示硬件已完成。
   - 拒绝边界：`packNum` 为空/非法、写入值超出 `0..0xFFFF`、单体编号越界、未找到采集通道均拒绝；`404901/404902/404921/404922/410004` 等非白名单地址已有测试确认拒绝。
   - 暂缓项：地址设置、清调试数据、内阻系数、校准、连接条/内阻/核容/备电测试触发、报警屏蔽 `400421` 等均未开放；如后续开放，必须先确认寄存器所有权、权限、互斥、回执语义，并统一调用内部命令/告警服务，禁止拼旧 980 命令。
   - 验收依据：`ModbusWriteMappingServiceTest` 已覆盖均衡映射、内部服务拒绝返回 false、非白名单地址拒绝、非法值/pack/model 拒绝和无通道拒绝。
   - 禁止：不得在本任务开放新的可写地址；不得绕过 `ModbusWriteMappingService` 新增 Modbus 写路径。
3. `TASK-M460-ALARM-FUNC-001`：已完成本轮只读梳理。新告警入口是 `AlarmContextProcessor` 从 600 实时 `group/cells` 和通信状态构建 `BatteryModuleAlarmContext`，再调用 `IAlarmLogService.alarmBatteryValue`；旧 `BatteryAlarmHandler` / `BatteryAlarmBitMapping` 只保留 JSON/TCP `87/8D` 兼容解析边界。
   - 新链路已生成 itemCode：组级 `zdygc/zdygf/zcgdlgj/zwdg/zwdd/zsocdgj/zsohdgj/txzt`；单体 `dtdygc/dtdygf/dtnzgd/dtnzgx/dtdcwdg/dtdcwdd/dtgb/dtlygj`；通信适配 `dttxzt`，以及模块超时、inactive、246 stale 映射到 `txzt`。
   - 旧链路未确认来源：旧 `87` 中 `zfcdygd/zfcdygg/ztlmxgj/dtdckl/dtfcdyd/dtfcdyg/dtnzbj/dtdcwdbj/dtdybj`，旧 `8D` 中 `zwlgz/ztdgj/zwdcgq1gz/zwdcgq2gz/dtljtgj/dtwdcgqgz` 缺少已确认的新实时字段或设备状态来源。
   - 恢复边界：`AlarmContextProcessor` 每轮会对未上报单体调用 `alarmFix(packNum, true, activeCellNums, ALL_CELL_ALARM_CODES)`；通信状态恢复来自 `BatteryDeviceState` 的 `recovered/normal` 语义，不等同旧 `8D` 位图恢复。
   - 屏蔽/解除边界：现有屏蔽入口仍是页面/同步链路 `AlarmLogServiceImpl.shiedAlarmLog` 与 `AlarmHandler.shieldAlarm`；Modbus 写侧尚未开放 `400421` 报警屏蔽，不能绕过现有告警服务直接写旧命令。
   - 可交给其他 AI 开发：`TASK-M460-ALARM-FUNC-001-A` 为新链路已支持 itemCode 补齐覆盖矩阵和单元测试缺口；`TASK-M460-ALARM-FUNC-001-B` 输出旧 87/8D 未确认来源 itemCode 的现场确认表；`TASK-M460-ALARM-FUNC-001-C` 复核告警恢复与屏蔽/解除的页面验收口径。
   - 禁止：不得在未确认 bit 语义、屏蔽口径和恢复边界前新增告警规则；不得把旧 `87/8D` payload 作为新 Modbus 或 600 告警直接来源。

### P9：计划整理收口

1. `TASK-PLAN-FUNC-001`：已完成。已从 `蓄电池采集整合计划.md`、`energy_refactor_current_plan_20260622.md`、当前代码边界和候选池中合并下一轮有效任务。
2. `TASK-PLAN-FUNC-002`：已完成。现场串口、SQLite 压测、未冻结 Modbus/告警寄存器和旧链路最终删除已拆入“现场确认清单”和“后续候选池”，不作为默认自动执行任务。
3. `TASK-PLAN-FUNC-003`：已完成。`01document/README.md` 已补充上一轮 M460/600 主线归档说明，新会话继续时先读取本文整理新的当前待办，不从归档计划自动恢复任务。

## 已归档计划

| 归档文件 | 说明 |
|---|---|
| `archive/completed-plans/当前计划任务_20260630_M460主线收口.md` | M460/600 采集主线执行计划收口归档；剩余现场依赖、暂缓清理和长期约束不再作为当前自动执行任务。 |

## 后续候选池

以下仅作为整理素材，不作为默认执行任务：

1. M460 `_2/_6` 旧 980 fallback 删除：待默认切到 600 后再评估。
2. `_1/_3/_5` 停止旧链路迁移：迁移前需确认核容、备电日志和容量预测依赖。
3. `/batteryOpt/edit` 是否改为“只保存计划、不立即下发旧配置命令”：需要业务确认，未确认前不改行为。
4. `_1` 整组内阻测试、`_3` 核容、`_4` 浮充、`_5` 备电时长、`_7` 充电测试：缺少已核验的新采集命令或长周期后处理闭环，暂缓迁移。
5. 6 通道并发、SQLite 写入压力、状态 upsert 频率：进入现场观测，不能靠删除数据或关闭功能掩盖。
6. 600 控制命令现场样例：等待真实串口样例后回填请求帧、响应帧、耗时和日志结果。
7. 写库耗时指标：当前无稳定采集源，后续如新增持久化计时点再透传。
8. Modbus TCP：除非出现明确部署需求，否则不纳入当前任务。
9. 运行期数据库删表：需确认升级策略、现场历史数据保留和 Flyway 记录处理方式。

## 现场确认清单

以下事项依赖真实设备、现场平台或人工业务确认，不作为默认开发任务：

1. 6 路串口参数、轮询间隔、超时、重试、6 通道并发和 SQLite 锁等待。
2. `03/83`、`08/88`、`0F`、`11/91`、`18/A8`、`12/92`、`76/F6` 的真实请求/响应、耗时、失败恢复、页面返回和日志结果。
3. Modbus RTU 站号、端口、异常码、写权限、上位系统对温度/电流偏置和寄存器地址的最终确认。
4. 告警确认、屏蔽、恢复、解除的页面验收口径，以及是否需要暴露到 Modbus 写寄存器。
5. SOC/SOH、容量预测、核容、备电时长算法是否完整迁移，以及旧 `dev_opt_log`、`pre_battery_group` 的保留边界。

## 执行约束

1. 默认只处理“当前待办”功能主线；其他 AI 分派池只读，不主动执行、不改完成状态。
2. 功能开发优先；测试验证任务原则上交给其他 AI，当前主线只做必要的最小自检。
3. 提交前必须执行 `git diff --check`，审查 staged diff 后再 `git commit`。