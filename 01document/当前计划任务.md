# 当前计划任务

更新时间：2026-06-29

本文是继续开发时的任务入口。已完成或过期计划不再散落在根目录，历史内容见 `archive/completed-plans/`。

## 当前工作重点

1. 蓄电池采集主流程继续收敛到 600 模块端采集链路。
2. M460 能力整合优先闭合测试计划、停止、执行和结果可观测路径。
3. `_2` 连接条电阻测试、`_6` 单节内阻测试优先走 600 命令队列。
4. `_1`、`_3`、`_5` 旧兼容链路暂保留，后续按风险逐步迁移。
5. 继续减少旧 `cmdBatteryControlService` 和 `CommServer.returnCmd` 依赖，但不做大范围重写。

## 近期已完成

| 提交 | 结果 |
|---|---|
| `b40540c` | 600 队列测试停止时取消未下发命令并标记测试停止 |
| `fd29106` | `_2/_6` 采集命令执行失败时不再回退旧链路 |
| `1f9982c` | 增加仓库编码/换行约束，统一 UTF-8/LF 规则 |
| `0299163` | 计划任务跳过 600 模块正在运行的同类测试 |
| `861a613` | 600 命令日志完成时同步写旧 `result`，避免运行日志阻塞 |
| `3c56272` | `_2/_6` 找不到采集通道时阻止旧链路兜底 |
| `3ab67c3` | 测试执行前置校验复用同一份上报数据，避免 `_2` 无数据空指针 |
| `8d4d611` | 补 `_2/_6` 停止适配和 `stopRunningTest` 同模式取消队列/拒绝分支最小覆盖 |
| `afc6a52` | 补齐 `stopRunningTest` 非法参数和运行组不匹配拒绝分支覆盖 |

## 当前待办

### P0：测试计划功能闭环

1. `TASK-M460-OPT-FUNC-001`：把测试计划保存明确改成“保存计划参数”，不再把 `_2/_6` 的计划配置下发到旧 980/M460 配置链路。
2. `TASK-M460-OPT-FUNC-002`：梳理 `/batteryOpt/edit`、同步配置入口和计划任务扫描之间的数据契约，确认 `DevBatteryOpt` 保存字段能支撑 600 命令执行。
3. `TASK-M460-OPT-FUNC-003`：到点调度只作为触发器，统一调用 `ControlBattery.executeBatteryOpt(opt, SCHEDULED)`，不在 job 内拼命令、不写第二套状态机。
4. `TASK-M460-OPT-FUNC-004`：计划成功后的下一次执行时间、一次性禁用、失败保留当前计划时间等规则要在业务代码里固化，不依赖外部约定。
5. `TASK-M460-OPT-FUNC-005`：计划执行前的互斥判断统一读取 `dev_opt_log` 运行中日志和 `BatteryModeStatusService`，避免计划重复入队。
6. `TASK-M460-OPT-FUNC-006`：计划执行结果要能在页面/日志中看见：入队成功、前置拒绝、队列失败、运行中跳过、执行异常分别有清晰状态。
7. `TASK-M460-OPT-FUNC-007`：计划入口和手动入口保持同一套前置校验和同一套 600 命令适配，不允许分叉实现。

### P0：_2/_6 执行路径收敛

1. `TASK-M460-EXEC-FUNC-001`：页面立即执行 `_2` 连接条电阻测试必须进入 `BatteryOptCollectorCommandAdapter.tryExecute`，成功后只入 600 命令队列。
2. `TASK-M460-EXEC-FUNC-002`：页面立即执行 `_6` 单节内阻测试必须进入 `BatteryOptCollectorCommandAdapter.tryExecute`，按 `modelNum` 下发 600 单体内阻命令。
3. `TASK-M460-EXEC-FUNC-003`：同步入口执行 `_2/_6` 必须复用同一适配器，不能绕到旧 `cmdBatteryControlService`。
4. `TASK-M460-EXEC-FUNC-004`：模块命令开关开启时，`_2/_6` 的无通道、入队失败、命令异常都应直接返回错误，不回退旧 `CommServer.returnCmd`。
5. `TASK-M460-EXEC-FUNC-005`：模块命令开关关闭时保留旧兼容链路，便于现场切换和回退；开关语义要在配置和文档中保持一致。
6. `TASK-M460-EXEC-FUNC-006`：`_2` 前置电流条件继续保留，但前置校验只读一次当前上报数据，避免实时/旧缓存口径不一致。
7. `TASK-M460-EXEC-FUNC-007`：`_6` 单体编号校验以电池组实际单体数为准，异常或缺省时最多按 245 上限处理，不能发送非法地址。
8. `TASK-M460-EXEC-FUNC-008`：逐步清理 `_2/_6` 在 `generateCommand` 中的旧执行可能性，最终让 `_2/_6` 不再产生旧 980 指令。

### P0：停止路径功能闭环

1. `TASK-M460-STOP-FUNC-001`：`_2/_6` 停止只调用 `BatteryOptCollectorCommandAdapter.tryStop`，不进入旧 `genCmd30` 停止链路。
2. `TASK-M460-STOP-FUNC-002`：停止只在当前组正在执行同类型测试时生效；无运行测试、运行组不匹配、运行类型不一致均返回业务错误。
3. `TASK-M460-STOP-FUNC-003`：停止时取消队列中未下发的同组同模式命令，不取消其他组或其他模式命令。
4. `TASK-M460-STOP-FUNC-004`：已完成。停止成功后同步关闭运行模式和 `_2/_6` 对应运行日志，避免下一次计划/手动执行被旧状态阻塞；设备当前态继续沿用 M460 状态同步链路刷新。
5. `TASK-M460-STOP-FUNC-005`：停止返回文案区分“停止成功并取消 N 条队列命令”和“未停止，原因是无运行/类型不一致”。
6. `TASK-M460-STOP-FUNC-006`：`_1/_3/_5` 停止旧链路暂保留；后续迁移前先确认核容/备电日志和容量预测依赖。

### P0：日志和运行态一致性

1. `TASK-M460-LOG-FUNC-001`：`dev_opt_log` 继续作为测试/命令事实表，不能新增另一套测试历史表。
2. `TASK-M460-LOG-FUNC-002`：600 命令日志完成时必须同步写旧 `result`，保持旧页面和 `getRunningOptLog` 查询兼容。
3. `TASK-M460-LOG-FUNC-003`：队列取消、入队失败、响应超时、响应码不匹配、payload 失败标志分别写入可追踪状态和错误原因。
4. `TASK-M460-LOG-FUNC-004`：`type=99` 继续用于非传统测试/维护命令，`_2/_6` 传统测试要明确是否使用原测试类型或命令扩展字段。
5. `TASK-M460-LOG-FUNC-005`：`BatteryModeStatusService` 的当前模式、`battery_device_state` 当前态和 `dev_opt_log` 运行中状态要形成单一事实链，不互相打架。
6. `TASK-M460-LOG-FUNC-006`：页面测试记录、计划任务跳过判断、停止判断都应使用同一套运行态口径。

### P1：连接条电阻测试完整闭环

1. `TASK-M460-CONNRES-FUNC-001`：`0F` 启动成功后进入连接条测试运行态，页面和状态接口可观察到当前组正在连接条测试。
2. `TASK-M460-CONNRES-FUNC-002`：`11/91` 按地址推进读取测试电压，地址范围来自电池组单体数，最大不超过 245。
3. `TASK-M460-CONNRES-FUNC-003`：连接条电阻计算使用已确认公式、电流来源和单位；缺电流或无效电流时明确跳过、失败或标记不可计算。
4. `TASK-M460-CONNRES-FUNC-004`：连接条结果写入标准实时表和兼容缓存，页面、统计、JSON/TCP、Modbus 后续从统一字段读取。
5. `TASK-M460-CONNRES-FUNC-005`：中途失败、单体超时、队列取消、服务停止、串口重连后要结束运行态并记录失败原因。
6. `TASK-M460-CONNRES-FUNC-006`：重复启动时给出明确拒绝；同组不同测试、不同组同类测试的互斥规则按 M460 模式口径固化。
7. `TASK-M460-CONNRES-FUNC-007`：连接条测试结束后触发必要统计刷新，不在采集轮询线程做重型统计。

### P1：单节内阻测试闭环

1. `TASK-M460-SINGLEIR-FUNC-001`：`_6` 只下发单个单体的 600 `02/82`，不触发整组旧 `_1` 内阻测试流程。
2. `TASK-M460-SINGLEIR-FUNC-002`：明确 `_6` 响应结果写入标准实时单体内阻字段、命令日志和页面可见状态。
3. `TASK-M460-SINGLEIR-FUNC-003`：`_6` 当前模式与旧 `_1` 内阻测试模式区分清楚，停止 `_6` 不能误关闭 `_1` 旧运行日志。
4. `TASK-M460-SINGLEIR-FUNC-004`：单体无响应、地址非法、通道不可用、队列取消均写入明确错误状态。
5. `TASK-M460-SINGLEIR-FUNC-005`：确认单节内阻测试结束后是否触发内阻统计；没有完整组状态时不触发旧整组统计。

### P1：外部读取和 M460 输出能力

1. `TASK-M460-READ-FUNC-001`：页面当前态、测试记录、连接条结果、单节内阻结果逐步从标准实时表、`battery_device_state`、`dev_opt_log` 读取。
2. `TASK-M460-READ-FUNC-002`：JSON/TCP 输出字段继续切到标准实时快照，默认保留回退开关，未确认字段不在输出层临时计算。
3. `TASK-M460-READ-FUNC-003`：Modbus 高频读优先走 `BatteryModuleRealtimeSnapshotService`，状态/告警寄存器来源统一到标准当前态和告警模型。
4. `TASK-M460-READ-FUNC-004`：未就绪、stale、无来源、不支持的字段输出口径要统一，避免页面、JSON/TCP、Modbus 三套不同默认值。
5. `TASK-M460-READ-FUNC-005`：`/collector/battery/status`、`/metrics`、`/currentState` 作为现场诊断入口，补齐 `_2/_6` 运行态所需信息。
6. `TASK-M460-READ-FUNC-006`：外部读取不能直接依赖临时缓存；临时缓存必须有快照服务或当前态服务封装。

### P1：告警和设备状态能力

1. `TASK-M460-ALARM-FUNC-001`：抽取标准告警规则入口，承载阈值告警、通信告警、设备状态告警和可计算的 87/8D 语义。
2. `TASK-M460-ALARM-FUNC-002`：通信告警覆盖 `CHANNEL_ERROR`、`MODULE_TIMEOUT`、`MODULE_ACTIVE=inactive`、`GROUP_246_FRESHNESS=stale`，并有恢复/过期口径。
3. `TASK-M460-ALARM-FUNC-003`：单体阈值告警按 `packNum + batNum + itemCode` 独立落库和恢复，不被同 itemCode 覆盖。
4. `TASK-M460-ALARM-FUNC-004`：告警屏蔽/解除复用 `dev_alarm_log` 标准能力，不恢复旧 980 屏蔽 runtime。
5. `TASK-M460-ALARM-FUNC-005`：热失控、氢气、网络、传感器等无 energy 数据源的 M460 告警保持不生成，并在接口输出中可解释。

### P1：SOC/SOH/容量和后处理能力

1. `TASK-M460-CAPACITY-FUNC-001`：容量预测、SOC/SOH、核容/备电状态只在后处理和持久状态中维护，不进入采集轮询线程。
2. `TASK-M460-CAPACITY-FUNC-002`：`CapacityPredictionProcessor` 缺少状态、历史、有效电流或完整单体数据时跳过，不填 0、不猜测。
3. `TASK-M460-CAPACITY-FUNC-003`：操作日志 processor、容量预测 processor、内阻统计 processor 顺序清晰，单个 processor 异常不影响其他后处理。
4. `TASK-M460-CAPACITY-FUNC-004`：核容/备电 `_3/_5` 旧链路迁移前先定义状态流、日志字段、结果字段和外部读取口径。
5. `TASK-M460-CAPACITY-FUNC-005`：SOC/SOH 在页面、JSON/TCP、Modbus 中的未就绪输出统一，不把无来源字段伪装为 0。

### P2：采集主流程稳定性

1. `TASK-MAINFLOW-FUNC-001`：采集主流程继续以稳定功能为先，暂不大迁包；只在职责明显阻塞功能闭环时小步拆分。
2. `TASK-MAINFLOW-FUNC-002`：显式命令队列优先级必须保证控制命令不会被整轮 `1..245 + 246` 轮询长期阻塞。
3. `TASK-MAINFLOW-FUNC-003`：串口读写、pending、超时、主动帧分发顺序保持稳定，命令响应不能被普通轮询消费掉。
4. `TASK-MAINFLOW-FUNC-004`：6 通道并发、SQLite 写入压力、状态 upsert 频率进入现场观测，不用删除数据或关功能掩盖压力。
5. `TASK-MAINFLOW-FUNC-005`：运行指标覆盖队列长度、pending 超时、批次耗时、写库耗时、快照命中率和通道异常。

### P2：现场联调和资料回填

1. `TASK-FIELD-FUNC-001`：建立 600 控制命令样例记录，覆盖 `02/82`、`0F`、`11/91`、`03/83`、`08/88`、`18/A8`、`12/92`、`76/F6`。
2. `TASK-FIELD-FUNC-002`：每个命令记录成功样例和失败/超时样例，包括请求帧、响应帧、通道、packNum、模块地址、耗时和日志结果。
3. `TASK-FIELD-FUNC-003`：记录真实串口参数、超时/重试设置、6 通道并发压力和异常恢复表现。
4. `TASK-FIELD-FUNC-004`：待现场确认字段统一标为“待现场确认”，不把推测值写成已确认。
5. `TASK-FIELD-FUNC-005`：现场确认后同步更新 `当前计划任务.md`、`蓄电池采集整合计划.md` 和对应协议辅助文档。

## 交给其他 AI 的验证任务

1. `TASK-AI-VERIFY-STOP-001`：补停止路径单测或回归验证，包括同组同模式、无运行、类型不一致、非法参数、运行组不匹配。
2. `TASK-AI-VERIFY-SCHEDULE-001`：补计划任务调度测试，包括未到点、已有运行日志、600 同模式运行、成功推进、失败不推进。
3. `TASK-AI-VERIFY-EXEC-001`：补 `_2/_6` 执行入口测试，确认适配器成功/错误都截断旧链路。
4. `TASK-AI-VERIFY-LOG-001`：补 `dev_opt_log` 兼容查询测试，确认完成日志写旧 `result` 后不再阻塞运行中查询。
5. `TASK-AI-VERIFY-READ-001`：补读取口径回归测试，确认页面/JSON/TCP/Modbus 的标准实时快照优先和缺省回退。
6. `TASK-AI-VERIFY-ALARM-001`：补告警生成和恢复回归测试，尤其是单体维度不被 itemCode 覆盖。
7. 其他 AI 只补验证和小范围缺口，不自行调整功能路线、迁移大包、删除旧兼容链路或新增计划文档。

### P2：文档和归档维护

1. `TASK-DOC-001`：继续保持 `01document/README.md` 作为入口，新增计划先并入 `当前计划任务.md`。
2. `TASK-DOC-002`：完成的功能任务从“当前待办”移动到“近期已完成”，保留提交号和一句结果。
3. `TASK-DOC-003`：测试验证任务只记录在“交给其他 AI 的验证任务”，不与功能主线混排。
4. `TASK-DOC-004`：辅助文档只保留字段矩阵、告警规则、协议对照和寄存器草案；不再新增零散计划文档。
5. `TASK-DOC-005`：旧计划只归档不删除，恢复任务时先合并到当前任务入口。

## 执行约束

1. Java 源文件按 UTF-8 无 BOM + LF 写入，避免 PowerShell 默认编码。
2. 不修改或提交 `03code/energy/sql/rysqlite3.db`、`LoginInterceptor.java`、`.codegraph/`。
3. 代码任务先用 `.codegraph` 看调用面，再小步修改。
4. 功能开发优先；测试验证任务原则上交给其他 AI，当前主线只做必要的最小自检。
5. 提交前必须执行 `git diff --check`，审查 staged diff 后再 `git commit`。

## 主线必要自检

```powershell
mvn "-Dtest=BatteryOptCollectorCommandAdapterTest,ControlBatteryTest,BatteryOptScheduleJobTest" "-Dmaven.test.skip=false" test
git diff --check
```
