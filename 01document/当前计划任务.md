# 当前计划任务

更新时间：2026-07-08

本文是后续继续整理计划任务的唯一当前入口。已归档文档只作为历史依据和背景证据，不作为新会话自动执行任务来源；如需恢复归档任务，必须先把结论合并回本文的“当前待办”。


## 最高优先级限制：project.iot 旧代码边界

`03code/energy/src/main/java/com/shanhe/project/iot` 下所有代码均视为旧功能兼容入口，整体改造完成后要删除。后续任务不要再优化、重构或迁移该目录下功能；其他模块整改过程中如删除/收口旧兼容方法导致 `project.iot` 或 `iot.battery` 引用编译失败，只允许在旧入口内删除或注释该引用以保编译，不改接新实时链路、不补 fallback、不新增 adapter、不补测试。
## 会话决策记录

以下结论来自本轮连续审查，后续实现和审查时优先作为边界条件使用：

1. `0x30 mode=1/4` 是旧链路中 energy 与 M460 的交互协议，不是 energy 直接发给下层外部模块的协议帧。M460 收到后再由固件按下层 Modbus 语义控制外部备电/空开模块；当前整合目标是 energy 绕过 M460，直接复刻这段下层控制语义。
2. `_5` 只需要向外部模块下发备电运行/停止控制，即对应 M460 下层语义中的 `mode=1/4`。旧 `0x31..0x35` 已由当前 `DevBatteryOpt` 和平台计划承载配置，不作为 `_5` 执行时默认下发链路，避免平台计划和设备内置计划双触发。
3. `cmdBatteryControlService.genCmd30` 与 `BatteryCidEnum._E0` 属于旧 energy 与 M460 的上位交互协议，只能作为参考，不应作为当前 `_5` 直控外部模块的完成态依据。
4. `_3` 与 `_5` 的共同点只到 adapter 边界：`_3` 收进 `BatteryOptCapacityModuleCommandAdapter` 是为了禁止回退旧 M460 指令并保留注释指向 M460 对应底层功能；当前不做兼容下发、不创建 running log、不新增调度闭环。
5. `batteryPackStatus`、`resistanceTestStatus` 等状态字段要区分内部运行态和对外兼容输出。内部判断优先使用 running optLog、`BatteryModeStatusService`、pending/queued 命令、采集后处理状态；对外上报或 Modbus 兼容时再投影封装成旧字段。
6. `batteryPackStatus` 当前基于组充放电电流投影，现阶段可保留；`resistanceTestStatus` 后续应结合 `_1/_6` 内阻测试运行态投影，不应简单从实时采集字段或默认值硬填。
7. `_5` 结果计算不进入控制 adapter。备电时长、放电容量、SOH/SOC 等继续由采集后处理、`CapacityPredictionProcessor` 和 `BatteryPredictorServiceImpl` 闭合；控制层只负责外部模块启停、响应、超时和日志状态。
8. `_5` 停止触发不能只复用 `_1/_2/_6` 的“命令执行完成”语义。当前已有人手动 stop、状态离开 BACKUP 后处理关闭日志、容量预测触发三段闭环；M460 固件中的测试时长、截止电压、电流恢复等主动停止策略应作为后续独立评估任务。
9. 所有备注、错误信息、计划文档和后续任务说明默认使用中文；不再创建非必要测试用例，除非符合本文“执行入口规则”的测试新增条件。
10. 后续功能主路径优先读取标准实时模型和实时缓存；`dev_battery_report_log` / `BatteryReportLogService.lastCache` 只作为历史查询、旧兼容入口或过渡 fallback，不再作为新功能的默认当前态来源。
11. `BatteryModuleReportLogAdapterService.currentOrLastCache(...)`、`buildReportLog(...)` 仅是兼容旧调用面的过渡方法；当前态业务应直接使用 `BatteryModuleRealtimeSnapshotService.getCachedSnapshot(packNum)`、`BatteryModuleGroupRealtime`、`BatteryModuleCellRealtime` 或对应领域 DTO，不再反复适配成旧 `BatteryReportLog` 结构。
12. 每轮标准采集应按“组数据 + 单体数据完整”写入；采集失败不应落库。当前部分可用性判断只是兼容期保护，后续不应继续围绕“只有组或只有单体”的异常半成品形态扩展主流程。
13. JSON/TCP 向上上报链路继续保留，但不再通过 `jsonTcpRealtimeSourceEnabled` 判断是否读取实时数据；后续应默认直接取标准实时模型/实时缓存，逐步删除的是该切源开关和 `BatteryPackHandler` 旧 82 采集上报入口。
14. `BatteryPackHandler` 属于旧 82 实时上报兼容入口，后续应整体清理或降级为兼容路径；除非用户明确要求，不再围绕该入口做大规模行为修复或补测试。
15. 实时数据也需要新鲜度约束；后续应基于采集时间、通道最后推进时间或实时快照时间戳统一限制当前态有效期，而不是依赖历史表时间或旧 Redis 缓存时间。
16. 测试只保留能约束核心能力和迁移风险的必要用例；旧入口、过渡 adapter/service 或历史兼容分支的机械测试可随功能切换删除或改为最小覆盖。
## 执行入口规则

1. 当前阶段优先推进标准实时模型字段收口、当前态实时快照主线迁移和实时新鲜度规则；`ControlBattery` 全流程整合、采集模块命令闭环、`_5` 外部备电模块直控和必要的运行态补偿继续按本清单顺序推进。
2. 不从归档计划自动恢复任务；判断是否继续执行某项任务，以本文“当前待办”和用户最新指令为准。
3. 不再做重复无效防护：不围绕 `/collector/battery/execute` 调试入口、旧聚合枚举文档、通用 unsupported 文案做额外改造。
4. Modbus 读写与告警拆分已阶段完成，除非用户点名或现场确认回来，否则暂缓扩展。
5. 开始前必须查看 `git status --short`；不得提交 `.codegraph/`、运行态数据库、协议原件和非本轮目标文件。`project.iot` / `iot.battery` 只允许因其他模块收口导致编译失败时删除或注释旧引用，不做兼容改造。
6. 文本文件按 UTF-8 无 BOM + LF 写入；终端显示乱码只视为显示链路风险，不据此整文件转码。
7. 功能开发优先；默认不新增测试用例。只有改动高风险生产逻辑、修复已复现缺陷、补齐已有测试断言失效，或用户明确要求时，才新增或修改聚焦测试；不得为过渡 adapter/service、文档结论或低风险分流整理机械补测试。

## 当前待办

### P0-0：实时模型与当前态主线优先收口
`TASK-REALTIME-PRIORITY-001`：实时相关任务提升为当前待办最高优先级，先统一字段、读取入口和新鲜度规则，再继续扩展依赖实时当前态的业务能力。
执行顺序：
- `TASK-REALTIME-FIELD-AUDIT-001`：梳理实时表、实时实体和采集主流程字段矩阵，明确协议来源、单位、业务语义、唯一标准字段名、对外兼容投影字段名和允许消费方；内部处理只传递唯一标准属性，不在内部消费者中继续做兼容字段判断。
- `TASK-REALTIME-SNAPSHOT-CURRENT-001`：继续迁移当前态查询入口，除历史查询、历史导出和明确未迁移旧入口外，当前态业务直接读取 `BatteryModuleRealtimeSnapshotService`、`BatteryModuleGroupRealtime`、`BatteryModuleCellRealtime`，不再绕回 `BatteryReportLog`、`BatteryMonitor` 或字符串 Map。
- `TASK-REALTIME-SNAPSHOT-FRESHNESS-001`：补齐实时快照统一新鲜度判断，基于采集时间、组/单体更新时间、通道最后推进时间或快照时间戳收口，不依赖历史表时间或旧 Redis 缓存时间。
- `TASK-REALTIME-CURRENT-CONSUMER-001`：按业务入口清理当前态消费者，优先处理容量预测、内阻基准、运行态判断等内部计算链路，再处理页面/大屏 DTO，最后处理 JSON/TCP 向上上报 DTO；历史查询继续保留历史表路径。
- `TASK-REALTIME-OPLOG-POSTPROCESS-001`：分阶段精简操作日志实时后处理。旧 M460 分离链路只能通过上报状态判断测试计划，才需要 `OperationLogProcessor` / `OptLogService.insertBatteryRealtime(...)` 在采集后处理里补日志；当前测试计划执行入口已同步创建运行日志，后处理不应再创建或续写新日志。
  - 第一阶段（已完成 2026-07-13）：收缩 `OperationLogProcessor` 职责，只允许在采集状态确认已离开测试态时关闭已有 running log；禁止通过 `batteryPackStatus` / `resistanceTestStatus` 创建新日志、接管入口日志或重复续写。
  - 第二阶段（已完成 2026-07-13）：把“采集状态自然结束关闭日志”的能力并入统一运行态补偿/日志关闭服务，和手动 stop、命令完成、重启补偿共用同一关闭口径；已删除 `OperationLogProcessor` 和 `OptLogService.insertBatteryRealtime(...)`。
  - 审查边界：删除前必须确认 `_1/_2/_5/_6` 的测试计划创建、显式停止、命令失败回滚、状态自然结束、服务重启补偿均有唯一入口覆盖，避免重复创建、重复完成或遗漏自然结束关闭。
阶段完成记录（2026-07-14）：
- `TASK-REALTIME-FIELD-AUDIT-001`：已新增 `实时模型字段矩阵.md`，梳理实时表、实时实体、协议/计算来源、单位、业务语义、兼容投影字段和允许消费方；未确认单位或算法的字段保持 null/待确认，不写假默认值。
- `TASK-REALTIME-SNAPSHOT-CURRENT-001`：已复核当前态主入口，控制前置、容量预测、内阻基准、运行态判断、页面/大屏和 JSON/TCP 当前态主线均优先读取 `BatteryModuleRealtimeSnapshotService.getCachedSnapshot(...)`；历史查询、历史导出、旧 `project.iot` 和明确兼容同步路径继续保留历史/兼容结构。
- `TASK-REALTIME-SNAPSHOT-FRESHNESS-001`：已确认独立 Ehcache `battery-realtime-snapshot` TTL 为 180 秒且读取不回源实时表；新增 `BatteryModuleRealtimeSnapshot.isFresh(...)` 统一快照刷新时间判断，当前在线状态判断已纳入快照新鲜度约束。
- `TASK-REALTIME-CURRENT-CONSUMER-001`：已完成本轮消费者审查和低风险收口；遗留旧结构集中在历史查询/导出、统计兼容同步、旧入口和告警旧接口，告警按 `TASK-ALARM-REFORM-001` 独立重做实时上下文，不在本轮临时接旧状态查询。
- `TASK-REALTIME-OPLOG-POSTPROCESS-001`：已复核后处理职责，当前代码无 `OperationLogProcessor` / `OptLogService.insertBatteryRealtime(...)` 补日志入口；日志关闭继续由统一运行态补偿/显式停止/状态自然结束口径承载。
注意事项：
- `BatteryReportLogController` 和 `BatteryReportLogServiceImpl` 只查历史 `BatteryReportLog` / `dev_battery_report_log`，不得切到实时表。
- `project.iot` 仍按旧兼容边界处理；其他模块收口影响旧入口时，只在旧入口内删除或注释引用，不围绕旧入口补兼容、补测试、接新实时链路或继续迁移。
- `StatBatteryPackServiceImpl` 等统计写入要重点复核组压、组外压、充放电电流、浮充电流、SOC/SOH、放电时长等字段，避免用兼容投影值或错误字段给标准字段赋值。
### P0：运行态补偿继续收口

`TASK-M460-SCHEDULE-FUNC-008`：补齐运行态异常补偿机制。

已完成第一阶段：
- 新增 `BatteryOptRuntimeRecoveryService`。
- 系统重启后全量关闭 running `dev_opt_log`，并对 `_1/_2/_6` 对应模式执行 `markStopped(..., false)`。
- `BatteryOptScheduleJob` 每轮计划执行前按 packNum 调用保守补偿；仅在 running 日志超时且 `BatteryModeStatusService` 已非同组 running 时关闭残留日志，避免误停真实测试。

本轮完成（2026-07-15）：
- 新增 `BatteryTestLifecycleService`，以 `dev_opt_log` 为测试运行事实源，统一处理 `STARTING/RUNNING/SUCCEEDED/FAILED/CANCELLED/INTERRUPTED`，模式状态仅作为派生投影。
- `_1/_2/_6/_5` 启动统一先创建 `BusinessOptLogId`，命令层只透传该 ID；响应、超时、停止和启动恢复均通过生命周期服务收口。
- `BatteryModeStatusService` 已按 `packNum` 隔离缓存；停止流程调整为先校验运行类型，再取消队列并结束日志，避免失败请求提前关闭日志。
- 启动恢复对 `_5` 先幂等停止外设备电模块，再将遗留运行标记为 `INTERRUPTED`；已撤回 pending 快照字段方案。
- 模式投影已增加 `BusinessOptLogId` 所有权校验，迟到响应不能清理后续新任务；普通命令日志 ID 与业务所有者 ID 已分离。
- 停止和外设恢复已进入 `STOPPING` 两阶段流程；外设未确认恢复时保留活动运行，正常后处理、watchdog 和启动恢复统一重试。
- 新增活动运行部分唯一索引，同一设备和电池组只允许一个非 `_99` running 业务日志，多实例并发由数据库兜底。
- `OptLogService` 缓存已收敛为统计链路专用活动索引：running 插入定点写入、最终状态按 ID 删除；生命周期不再全量刷新，重建缓存也不再修改数据库。

后续任务：
- 为业务运行补充统一的 `lastProgressAt` 更新和 watchdog 超时策略；队列与 pending 只负责推进时间，不再向恢复层暴露内部命令字段。
- `_5` running log 补偿仅在实时 `batteryPackStatus` 已非 `BACKUP` 且超过现场确认窗口时关闭。
- 现场确认 `_1/_2/_6/_5` 典型耗时后，再调整不同测试类型的保守超时窗口。

### P0：`_5` 自动停止评估

`TASK-M460-BACKUP-STOP-001`：新增独立 `_5` 自动停止评估任务。

当前已闭合：
- `_5` start/stop 已进入 `BatteryOptCapacityModuleCommandAdapter`。
- adapter 默认调用 `BackupExternalModuleControlService`，由 energy 主站直接向外部备电模块发送 Modbus RTU 写单寄存器控制。
- `genCmd30/_E0` 只作为 M460 下层语义参考，不再是 `_5` 默认运行链路。
- 结果计算继续由实时采集、`CapacityPredictionProcessor` 和 `BatteryPredictorServiceImpl` 闭合，不在 adapter 中重复实现容量算法。

后续任务：
- 使用 `DevBatteryOpt` 计划参数和标准实时采集模型评估停止条件，例如测试时长、截止电压、组/单体电压、电流恢复等。
- 命中停止条件后调用现有 `BackupExternalModuleControlService.stopBackup(packNum)`。
- 必须先确认现场阈值单位、采样字段、自动断开/合上权限、异常恢复策略和人工停止优先级。
- 未确认前只保留文档任务，不在 adapter 内临时硬算。

### P0：`_5` 外部模块现场参数复核

`TASK-M460-BACKUP-FIELD-001`：复核外部备电模块直控参数和响应样例。

已完成第一阶段：
- 新增 `BackupExternalModuleProperties`，配置前缀为 `battery-opt.backup-external-module`，默认关闭。
- 默认参考 M460 下层配置：站号 `0x6E + packNum - 1`、备电运行寄存器 `0x1142`、停止/空闲寄存器 `0x1141`、写值 `0x00FF`。
- 未启用或未配置串口时返回中文错误，不会误发外部模块。

待确认：
- 外部模块串口/通道配置来源。
- 站号映射、寄存器地址、写入值是否与默认参考一致。
- 正常回显帧、异常码、CRC、超时和重试策略。
- 是否需要现场兼容 M460 代理链路；如需要，必须单独增加显式开关和中文日志，默认不得恢复。
- 是否需要 `0x35` 设备内置计划配置同步或 `0x36` 结果直读；未明确要求前不恢复旧配置下发。

### P0-1：当前态实时查询主线整改

`TASK-REALTIME-SNAPSHOT-CURRENT-001`：所有当前态查询改为直接读取标准实时快照。

已确认：
- 除历史查询外，所有“查询当前实时数据”的功能主路径应直接使用 `BatteryModuleRealtimeSnapshotService.getCachedSnapshot(packNum)` 获取实时缓存。
- 业务处理应基于 `BatteryModuleGroupRealtime`、`BatteryModuleCellRealtime` 和实时快照实体完成，不再先转换成旧 `BatteryReportLog`、`BatteryMonitor`、`packData/monitorData` 结构。
- 标准实时模型内部消费必须使用唯一标准字段；`packCurrent`、`batteryPackFloatCurrent`、`batteryPackOuterVoltage` 等旧兼容字段只允许在对外兼容投影或历史同步 adapter 中生成，后续需先梳理实时表字段属性矩阵后再决定是否从实时表结构中删除重复属性。
- `BatteryModuleReportLogAdapterService`、`currentOrLastCache(...)`、`buildReportLog(...)` 只作为旧接口和过渡兼容手段；后续遇到当前态业务入口，应优先把调用方改成实时实体，而不是继续扩展兼容适配器。
- `dev_battery_report_log` / `BatteryReportLogService` 保留给历史查询、历史导出、旧兼容 fallback 和明确仍未迁移的旧入口；不得作为新当前态功能的默认数据源。
- `BatteryPackHandler` 是旧 82 采集上报入口，后续逐步清理；JSON/TCP 向上上报链路保留，但上报数据应来自标准实时模型/实时缓存。

后续任务：
- 先冻结 `BatteryModuleReportLogAdapterService.currentOrLastCache(...)`、`buildReportLog(...)` 的扩展；除修复已知缺陷外，不再往该兼容 adapter 增加新鲜度、fallback、业务判断或新 DTO 逻辑。
- 逐项梳理当前态读取入口，按功能把 `BatteryReportLog` 兼容结构改为实时快照实体：控制前置判断、在线/告警上下文、容量预测、内阻基准、健康报告、页面当前态、大屏当前态、JSON/TCP 当前态上报。
- 每个功能整改时直接注入 `BatteryModuleRealtimeSnapshotService`，调用 `getCachedSnapshot(packNum)` 后用早返回处理 `snapshot/group/cells` 缺失；业务逻辑直接读取 `BatteryModuleGroupRealtime` 和 `BatteryModuleCellRealtime` 字段，不再经过 `BatteryReportLog`、`BatteryMonitor`、`packParam` 或字符串 Map。
- 新增实时表字段属性梳理任务：逐字段明确协议来源、单位、业务语义、唯一标准字段名、兼容投影字段名和允许消费方；重点核对组压/外组压、充放电电流/兼容电流、浮充电流、环境温度、容量/SOC/SOH、放电时长等字段，`StatBatteryPackServiceImpl` 等内部统计写入不得在口径未确认前把组压字段误用组外压或兼容投影值。
- 标准实时快照新鲜度改由独立 Ehcache 区域 `battery-realtime-snapshot` 的 TTL 控制；当前态读取入口只调用 `BatteryModuleRealtimeSnapshotService.getCachedSnapshot(...)` 查缓存，不回源实时表、不刷新缓存。缓存只在采集完成入库后的 `refreshAfterPoll(...)` 写入；启动不自动回源实时表，如后续确需启动预热或人工恢复，必须作为独立任务重新设计，不恢复普通读取回源链路。
- 后续继续把具体业务内部计算从旧 `BatteryReportLog` / `Map` 结构迁移到实时实体，并按功能删除不再需要的兼容适配。
- 代码精简要求：每迁移一个功能，同步删除该功能中不再需要的 `BatteryReportLogService`、`BatteryModuleReportLogAdapterService`、`BatteryReportLog`、`BatteryMonitor`、`Map<String,Object>` 解析和旧测试 mock；方法内部优先早返回，避免“查旧结构 -> 转 Map -> 再取字段”的多层嵌套。
- 迁移顺序建议：先改无页面协议兼容压力的内部计算链路（容量预测、内阻基准、运行态判断），再改页面/大屏当前态 DTO，最后改 JSON/TCP 向上上报 DTO；历史查询和历史导出最后只保留历史表路径。
- 每改一个功能只做该功能必要验证；旧上报入口、过渡 adapter/service 和历史兼容分支的机械测试可删除或降级。
- 不写假默认 `0`；缺失值保持 null/unsupported 或沿用当前兼容行为，迁移策略需单独评估。

### P1：告警能力后续整改

`TASK-ALARM-REFORM-001`：告警生成、告警上下文和告警值计算从旧 `project.iot` 兼容入口中剥离，作为标准实时模型后的独立整改任务，优先级晚于当前态实时快照主线。

已确认：
- `project.iot` 下告警解析入口属于旧功能，后续整体删除；不得围绕该目录继续优化或迁移。
- 新告警能力应基于标准实时快照、告警配置和专用上下文 DTO 重新整理，不再依赖旧 `BatteryReportLog` 作为主业务载体。
- 当前阶段若新接口签名影响旧入口，只删除或注释旧调用以保编译，不把旧入口改接新实时链路，不补 fallback、adapter 或旧逻辑测试。

后续任务：
- 设计新的告警上下文对象，覆盖组级实时值、单体实时值、离线/通讯状态和缺失值表达。
- 将 `IAlarmLogService.alarmBattery` 中对 `BatteryReportLog` 的依赖逐步替换为专用上下文；保留历史查询和兼容入口的边界适配。
- 重新评估离线告警、通讯告警、模拟量阈值和非模拟量告警的值来源，避免用空历史日志或假默认值驱动告警。
### P1：结果字段和状态投影清理

`TASK-M460-STATE-FUNC-001`：统一状态字段来源和对外投影。

已确认：
- `batteryPackStatus` 当前由 `BatteryModuleGroupCompatibilityFillService` 基于组充放电电流投影，当前阶段可保留，用于 `_5` 门控和对外上报适配。
- `resistanceTestStatus` 长期应由 `_1/_6` 内部运行态投影后，再通过实时实体或对外 adapter 投影暴露。
- M460 `Battery_State_Register` 应由投影后的 `batteryPackStatus` 和 `resistanceTestStatus` 组合，不基于未确认协议假设硬拼。

后续任务：
- 审查 `batteryPackStatus`、`resistanceTestStatus`、`deviceWorkStatus`、`deviceWorkIOStatus`、running optLog 和 `BatteryModeStatusService` 的所有消费方。
- 明确 `disChargeDuration`、`residualDischargeDuration`、`batteryPackSoc` 的来源、单位和缺失值表达。
- 不写假默认 `0`；缺失值保持 null/unsupported 或沿用当前兼容行为，迁移策略需单独评估。

### P1：业务日志与模块明细日志收敛

`TASK-M460-LOG-FUNC-001`：区分业务测试日志和模块命令明细日志。

已完成第一阶段：
- 新增 `moduleCommandSuccessLogEnabled` 配置，默认不从源头创建 `_99` 模块命令成功明细日志。
- `_1` 业务日志、`_2/_6` 业务类型日志不受该开关影响。
- 计划调度和异常补偿继续以业务日志和 `BatteryModeStatusService` 为准。

后续任务：
- 继续观察 SQLite 写入压力和现场排障需求。
- 如后续关闭更多明细日志，必须保证计划调度、页面状态、停止命令、异常补偿不依赖 `_99` 成功明细存在。

### P2：暂缓项

1. `_3` 核容测试：已收进 `BatteryOptCapacityModuleCommandAdapter` 作为占位 null 处理，只备注指向 M460 底层 `0x30 mode=2/4` 对应功能；不保留 `genCmd30/_E0` 兼容下发代码，不创建 running log，不新增执行、停止、状态、结果接入或调度工作。
2. `_4/0x31` 浮充配置：已明确为配置/核容模块转发边界，不是 600 采集命令；未确认前不恢复旧配置下发。
3. `_7` 充电测试：当前只可视为充电状态派生日志/状态类型，不凭枚举直接新增命令。
4. 通用空开：作为独立设备能力暂缓，不合并进 `_5`；`_5` 只封装备电运行所需的最小外部开关/备电模块控制。
5. Modbus 读写扩展、告警过滤/屏蔽/解除、板级外设、旧 980 聚合协议兼容层：阶段完成或排除，除现场反馈外不主动展开。

## 已整体完成并归档

| 范围 | 当前结论 |
|---|---|
| `ControlBattery` 统一入口 | 页面立即执行、同步立即执行、计划调度已统一进入 `ControlBattery.executeBatteryOpt`；manual/scheduled/sync 在入口内统一运行中拦截 |
| 执行分流 | `_1/_2/_6` 走采集模块 adapter；`_3/_5/stop` 走核容/备电模块 adapter；已迁移类型不再隐式回退旧 M460 |
| 旧直发 fallback | `ControlBattery.generateCommand/executeCommandAndLog` 已清理；`_1/_2/_6` 不再触发 `genCmd05/genCmd0F/getCmd36`；`_5` 不再调用 `genCmd30/_E0` |
| `_1` 整组内阻 | 已按 02/82 响应/超时逐节推进；单节失败继续下一节；最终结果仍依赖 01/81 状态回传 |
| `_1/_2/_6` 停止 | 已统一通过采集模块 adapter；手动停止不再为 `_1` 单独做实时状态二次校验；停止入口先关闭对应业务 running log，再处理队列取消和模式停止 |
| `_5` 手动启停 | 已固定到核容/备电模块 adapter，并切换为 energy 主站 Modbus RTU 直控服务；默认配置关闭，未启用或缺少串口时返回中文错误 |
| 计划保存入口 | `/batteryOpt/edit` 和同步计划保存只保存平台参数，不自动下发旧 `0x31..0x35`；页面立即执行不覆盖平台计划参数 |
| 计划调度入口 | `BatteryOptScheduleJob` 已复用 `ControlBattery.executeBatteryOpt(...SCHEDULED)`；`_4` 到点记录 SKIPPED 并推进下一次 |
| 执行来源日志 | 旧 M460/980 fallback 写入 `dev_opt_log` 时按 `BatteryOptExecuteType` 落库 source：`MANUAL -> web`、`SCHEDULED -> auto`、`SYNC -> json_tcp` |
| Modbus 读写 | 读保持寄存器和写白名单阶段完成，除现场反馈外暂缓扩展 |
| 告警新链路 | 阶段完成，未确认来源的告警位图暂缓扩展 |
| 重构计划 | `energy_refactor_current_plan_20260622.md` 中明确完成的重构和 AI 任务已归档；后续不再作为当前任务池 |
| `_5` 专项计划 | `TASK-M460-BACKUP-INTEGRATION.md` 的已完成结论和剩余待办已合并进本文；原文归档为专项历史依据 |

## 后续候选池

以下只作整理素材，不作为默认执行任务：

1. 6 路串口并发、SQLite 锁等待、状态 upsert 频率：进入现场观察，不通过删除数据或关闭功能掩盖。
2. 600 控制命令真实串口样例：等待现场样例后回填请求帧、响应帧、耗时和日志结果。
3. `/batteryOpt/edit` 是否恢复旧配置下发：需要业务确认，未确认前不改行为。
4. 现场是否要求外部备电模块结果直读：若要求，单独评估兼容开关；默认仍以 energy 实时采集和后处理为准。

## 现场确认清单

1. 6 路串口参数、轮询间隔、超时、重试、通道并发和 SQLite 锁等待。
2. `03/83`、`08/88`、`0F`、`11/91`、`18/A8`、`12/92`、`76/F6` 的真实请求、响应、耗时、失败恢复、页面返回和日志结果。
3. 外部备电/空开模块真实 Modbus RTU 站号、串口、寄存器地址、写值、响应帧、异常码、超时和失败恢复。
4. SOC/SOH、容量预测、备电时长算法是否由 `CapacityPredictionProcessor` / `BatteryPredictorServiceImpl` 完整闭合，以及旧 `dev_opt_log`、`pre_battery_group` 的保留边界。

## 执行约束

1. 默认只处理本文“当前待办”；其他分支只读，不主动执行。
2. 不再做“看起来更安全但不影响主流程”的重复拦截、重复文档或防御性改造。
3. 功能开发优先；默认不新增测试用例，除非符合本文入口规则第 7 条。
4. 提交前必须执行 `git diff --check`，审查 staged diff 后再 `git commit`。
5. 不提交 `.codegraph/`、运行态数据库、协议原件和非本轮目标文件。
