# 当前计划任务

更新时间：2026-07-01

本文是后续继续整理计划任务的唯一当前入口。上一轮 M460/600 采集主线已归档到 `archive/completed-plans/当前计划任务_20260630_M460主线收口.md`；归档文件只作为历史依据和背景证据，不作为新会话自动执行任务来源。后续如需恢复归档中的任务，必须先把结论合并回本文的“当前待办”。

## 执行入口规则

1. 默认执行范围只限“当前待办”里的 `TASK-*-FUNC-*` 功能任务；未明确点名时，不执行“交给其他 AI 的验证任务”和“后续候选池”。
2. 归档计划只作历史追溯，不作为后续任务判断依据；判断是否继续执行某项任务，以本文“当前待办”和用户最新指令为准。
3. 当前阶段重点仍是 `ControlBattery` 主线，继续收敛 M460 旧测试链路到统一入口；Modbus 读写与告警拆分已阶段性完成，除非用户点名或现场确认回来，否则暂缓继续展开。
4. 开始前必须查看 `git status --short`，不得整理、回滚或提交非本轮目标文件，尤其不要处理 `.codegraph/`、数据库文件和其他 AI 正在修改的测试文件。
5. 文本文件按 UTF-8 无 BOM + LF 写入；终端显示乱码只视为显示链路风险，不得据此整文件转码。疑似乱码先按 `文件编码与编辑约束.md` 的“乱码显示与转码禁令”确认，再小范围修复。
6. 代码任务先用 `.codegraph` 或 `rg` 确认调用面，再做小步修改；提交前必须执行 `git diff --check`，必要时补最小测试。

## 当前待办

### P0：ControlBattery 主线继续整合 M460 测试链路

1. `TASK-M460-CONTROLBATTERY-FUNC-004`：继续迁移 `_3/_5/stop` 核容/备电执行链路。
   - 已完成：`BatteryOptCapacityModuleCommandAdapter` 已作为 `_3/_5/stop` 固定插入点接入 `ControlBattery`；当前无等价新模块命令通道时返回 `null`，继续旧 `genCmd30` / `CommServer.returnCmd` fallback。本轮已把 `_3/_5/stop` 的旧 `0x30` 工作模式映射收口到 adapter 内部，后续接新模块通道时不再改 `ControlBattery` 主流程。
   - 已确认边界：旧 `_3/_5/stop` 下游是 `0x30/E_Sys_Set_Verify_Capacity_Operation_Mode` -> `Verify_Capacity_Package_Set_Command` -> 核容/空开/容量模块 Modbus，不属于 600 单体采集命令队列。
   - 后续目标：实现等价 `0x30` 的核容/空开/容量模块新命令通道与回执闭环；未实现前，不把 `_3/_5` 并入 `BatteryOptCollectorCommandAdapter` 的 600 队列。
   - Stop 边界：迁移前不得改变 `_3/_5` stop 下发后立即关闭 running log 的兼容行为；新通道具备回执闭环后再调整。
   - 验收建议：补 `ControlBatteryTest` 覆盖 `_3/_5/stop` 优先尝试 `BatteryOptCapacityModuleCommandAdapter`、返回空时继续旧链路。

2. `TASK-M460-CONTROLBATTERY-FUNC-002`：继续迁移 `_1` 整组内阻测试。
   - 已完成：`BatteryOptCollectorCommandAdapter` 已识别 `_1` 并调用 `BatteryCollectorCommandService.groupInternalResistanceTest` 稳定插入点；当前能力未就绪时回退旧 M460 链路。本轮已把回退判断改为基于 `BatteryCollectorCommandResult.mappedToModuleCommand` 的结构条件，不再依赖提示文案匹配。
   - 已完成：旧 `_1` stop 在本地判定无需继续下发停止命令时，会同步关闭 `dev_opt_log` 运行记录并调用 `BatteryModeStatusService.markStopped`，避免计划调度继续误判整组内阻运行中。
   - 已确认边界：旧 `_1` 下游是 `Protocol_Package_Set_Command` -> 600/submodule 整组内阻状态机，不等同 `_6` 单节内阻。
   - 后续目标：实现等价旧 M460 整组状态机的 600 显式控制流程，包括逐节地址推进、浮充继电器状态、约 7 分钟间隔、运行态、结果记录和 `dev_opt_log/battery_device_state` 闭环。
   - 禁止：不得把 `_1` 简化为循环调用 `_6`；不得绕过 `ControlBattery` 新增执行入口。
   - 验收建议：补执行入口、能力未就绪 fallback、非法状态拒绝、stop 不误关 `_6` 的最小测试。

3. `TASK-M460-CONTROLBATTERY-FUNC-006`：梳理 `/batteryOpt/edit` 旧计划配置下发边界。
   - 已确认：`0x32..0x35` 是 M460 内部 operational tasks 参数保存，后续由 M460 约 120s 周期调度；配置入口本身不立即执行测试。
   - 已确认：`0x31/_4` 是浮充参数配置，保存到 `Lcd_Floating_Charge_Management`，并可能按条件转发到核容模块；它不是后续 120s 定时测试项。
   - 当前原则：平台已有 `BatteryOptScheduleJob` 时，优先保留“平台计划调度 -> ControlBattery.executeBatteryOpt(...SCHEDULED)”作为统一执行主线；旧 `0x31..0x35` 配置下发是否保留，需要业务确认兼容要求。
   - 已完成：`/batteryOpt/edit` 和同步计划保存入口已通过 `savePlatformScheduleOpt` 明确为平台侧计划参数保存，不自动下发旧 M460 `0x31..0x35` 配置命令；立即执行仍保存同一份平台参数后进入 `ControlBattery`。
   - 后续目标：如业务确认仍需兼容设备内置计划配置下发，再通过已废弃旧兼容入口显式调用并加开关/说明；补测试覆盖保存计划参数、是否下发旧配置、是否由平台调度执行的区别。
   - 禁止：未确认前不得删除旧配置下发；不得让平台计划和 M460 内置计划无说明地同时触发同一测试。

4. `TASK-M460-CONTROLBATTERY-FUNC-007`：统一执行来源与日志语义。
   - 目标：manual、scheduled、sync 三类执行来源继续只进入 `ControlBattery.executeBatteryOpt`，日志中保留 executeType/source 语义，便于区分页面立即执行、计划触发和同步触发。
   - 已完成：旧 M460/980 fallback 写入 `dev_opt_log` 时已按 `BatteryOptExecuteType` 落库 `source`：`MANUAL -> web`、`SCHEDULED -> auto`、`SYNC -> json_tcp`；新链路仍由 600 命令日志自身维护来源。
   - 后续目标：检查 `_1/_3/_5/_4` fallback 路径的日志结果、错误原因、running 判断是否与 `_2/_6` 新链路保持一致。
   - 验收建议：补 `ControlBatteryTest` 或现有相关测试，覆盖 fallback 返回、业务错误、未支持类型提示和执行来源透传。

5. `TASK-M460-CONTROLBATTERY-FUNC-008`：收敛 980 聚合命令兼容层与 600 模块协议边界。
   - 当前结论：`BatteryCollectorCommandService` 现阶段是“旧 980/M460 业务语义入口 + 600 模块命令落地”的过渡兼容层；`BatteryAggregateCommandDefinition` 只表示旧聚合命令语义，不是底层最终协议码。
   - 整改方向：后续新增或已明确的 600 能力，优先直接用 `BatteryDeviceProtocolCode` / `BatteryModuleControlCommandService` 表达；`BatteryAggregateCommandDefinition` 只保留在旧 980 聚合命令兼容映射里，不再扩展新能力。
   - 当前边界：能转换的旧语义生成 `BatteryModuleControlCommand` 并进入 `BatteryCollectorService.submitModuleCommand`；不能转换的语义返回 `mappedToModuleCommand=false` 或 unsupported，由 `ControlBattery` 决定旧 M460 fallback。
   - 禁止：不得把未实现等价 600 状态机的 `_1/_3/_5` 直接标记为已映射；不得为新功能继续增加 `BatteryAggregateCommandDefinition` 枚举项。

### P1：计划调度与 ControlBattery 的一致性

1. `TASK-M460-SCHEDULE-FUNC-006`：计划任务继续复用 `ControlBattery.executeBatteryOpt(...SCHEDULED)`。
   - 已完成：`BatteryOptScheduleJob` 不再自行拼测试命令。
   - 后续目标：复核 `_1/_3/_5/_4` 在计划到点时的处理边界：已支持的进入统一入口，明确不支持或仅配置型任务应记录 SKIPPED/失败原因，并按既有规则推进或保留 `testTime`。
   - 特别边界：`_4/0x31` 是浮充配置，不作为平台定时执行命令；到点时不应每分钟重复失败。

2. `TASK-M460-SCHEDULE-FUNC-007`：避免平台计划与 M460 内置计划双触发。
   - 背景：旧 `0x32..0x35` 保存后由 M460 自身定时调度；平台 `BatteryOptScheduleJob` 也会按 `DevBatteryOpt.testTime` 调度。
   - 后续目标：在代码注释、计划文档和测试中明确当前选择：平台调度为主，旧配置下发作为兼容边界；如业务要求两者并存，必须有显式开关或说明。

### P2：Modbus 与告警阶段性暂缓

1. `TASK-M460-MODBUS-FUNC-001`：读保持寄存器阶段完成，暂缓继续扩展。
   - 当前状态：已支持单体电压/内阻/温度/纹波电压、设备状态、组测量和容量状态等读范围；状态寄存器已收敛为请求级设备状态快照，避免连续读高频 SQL。
   - 暂缓项：未冻结的告警/故障位图、参数区、新增可写寄存器暂不继续开发。
   - 恢复条件：用户明确点名 Modbus、现场确认寄存器表、或上位机联调反馈缺口。

2. `TASK-M460-MODBUS-FUNC-002`：写寄存器阶段完成，暂缓继续扩展。
   - 当前状态：仅开放 `404915` 单体均衡控制；非白名单地址拒绝。
   - 禁止：未确认权限、安全互斥、失败返回和审计日志前，不新增连接条/内阻/核容/备电触发类写寄存器。

3. `TASK-M460-ALARM-FUNC-001`：告警方案阶段完成，暂缓继续扩展。
   - 当前状态：新链路以标准实时模型和 `battery_device_state` 为来源；旧 JSON/TCP `87/8D` 位图只保留兼容解析。
   - 暂缓项：未确认来源的旧位图告警、屏蔽/解除写寄存器、未冻结 itemCode 不继续新增。

### P9：计划维护

1. `TASK-PLAN-FUNC-004`：保持当前入口文档可执行。
   - 本文只保留当前阶段要执行的功能任务；归档文件只作历史证据。
   - 完成任务后只更新本文对应任务状态和必要的一句结果，不从归档中自动恢复任务。

## 已阶段完成

| 范围 | 当前结论 |
|---|---|
| `_2/_6` 立即执行 | 已优先走 600 模块命令队列，旧链路仅作兼容 fallback |
| `_2/_6` 停止 | 已通过新适配器和运行态判断收口 |
| 计划调度入口 | 已统一到 `ControlBattery.executeBatteryOpt(...SCHEDULED)` |
| `_4/0x31` 边界 | 已明确为浮充配置/核容模块转发边界，不是 600 采集命令 |
| `0x32..0x35` 旧计划配置 | 已明确为 M460 内置 operational tasks 参数保存，配置入口不立即执行测试 |
| Modbus 读写 | 阶段完成，除现场反馈外暂缓扩展 |
| 告警新链路 | 阶段完成，未确认旧位图来源暂缓扩展 |
| 编码约束 | 已补充“终端显示乱码不等于文件内容乱码，禁止整文件猜测转码” |

## 后续候选池

以下只作整理素材，不作为默认执行任务：

1. `_2/_6` 旧 980 fallback 最终删除：待默认切到 600 后再评估。
2. `/batteryOpt/edit` 改为“只保存计划、不立即下发旧配置命令”：需要业务确认，未确认前不改行为。
3. `_7` 充电测试：当前只可视作充电状态派生日志/状态类型，不凭枚举直接新增命令。
4. 6 通道并发、SQLite 写入压力、状态 upsert 频率：进入现场观察，不通过删除数据或关闭功能掩盖。
5. 600 控制命令真实串口样例：等待现场样例后回填请求帧、响应帧、耗时和日志结果。

## 现场确认清单

以下依赖真实设备、现场平台或人工业务确认，不作为默认开发任务：

1. 6 路串口参数、轮询间隔、超时、重试、通道并发和 SQLite 锁等待。
2. `03/83`、`08/88`、`0F`、`11/91`、`18/A8`、`12/92`、`76/F6` 的真实请求、响应、耗时、失败恢复、页面返回和日志结果。
3. Modbus RTU 站号、端口、异常码、写权限、上位系统对寄存器地址的最终确认。
4. 告警确认、屏蔽、恢复、解除的页面验收路径，以及是否需要暴露到 Modbus 写寄存器。
5. SOC/SOH、容量预测、核容、备电时长算法是否完整迁移，以及旧 `dev_opt_log`、`pre_battery_group` 的保留边界。

## 执行约束

1. 默认只处理“当前待办”功能主线；其他 AI 分派池只读，不主动执行、不改完成状态。
2. 功能开发优先；测试验证任务原则上交给其他 AI，当前主线只做必要的最小自检。
3. 提交前必须执行 `git diff --check`，审查 staged diff 后再 `git commit`。
4. 不提交 `.codegraph/`、运行态数据库、协议原件和非本轮目标文件。