# 当前计划任务

更新时间：2026-06-29

本文是继续开发时的任务入口。已完成或过期计划不再散落在根目录，历史内容见 `archive/completed-plans/`。

## 当前工作重点

1. 蓄电池采集主流程继续收敛到 600 模块端采集链路。
2. M460 能力整合优先闭合测试计划、停止、执行和结果可观测路径。
3. `_2` 连接条电阻测试、`_6` 单节内阻测试优先走 600 命令队列。
4. `_1`、`_3`、`_5` 旧兼容链路暂保留，后续按风险逐步迁移。
5. 继续减少旧 `cmdBatteryControlService` 和 `CommServer.returnCmd` 依赖，但不做大范围重写。

## 近期已完成

| 提交 | 结果 |
|---|---|
| `b40540c` | 600 队列测试停止时取消未下发命令并标记测试停止 |
| `fd29106` | `_2/_6` 采集命令执行失败时不再回退旧链路 |
| `1f9982c` | 增加仓库编码/换行约束，统一 UTF-8/LF 规则 |
| `0299163` | 计划任务跳过 600 模块正在运行的同类测试 |
| `861a613` | 600 命令日志完成时同步写旧 `result`，避免运行日志阻塞 |
| `3c56272` | `_2/_6` 找不到采集通道时阻止旧链路兜底 |
| `3ab67c3` | 测试执行前置校验复用同一份上报数据，避免 `_2` 无数据空指针 |
| `8d4d611` | 补 `_2/_6` 停止适配和 `stopRunningTest` 同模式取消队列/拒绝分支最小覆盖 |

## 当前待办

### P0：测试计划调度闭环

1. `TASK-M460-OPT-SCHEDULE-001`：复查 `BatteryOptScheduleJob.executeDueBatteryOpt` 的筛选条件，确认只扫描启用计划、到点计划和有效 `packNum/testType/testTime`。
2. `TASK-M460-OPT-SCHEDULE-002`：补齐计划成功后推进规则：有 `intervalDays` 时推进下次 `testTime`，无间隔时禁用一次性计划。
3. `TASK-M460-OPT-SCHEDULE-003`：补齐计划失败后处理规则：失败或异常不推进下次计划，日志记录必须包含 `packNum/testType/result`。
4. `TASK-M460-OPT-SCHEDULE-004`：复查 `runningKeys` 并发保护，确认同一组同一测试类型不会被同一轮重复触发，finally 必须释放。
5. `TASK-M460-OPT-SCHEDULE-005`：复查 `hasRunningOptLog`：旧 `dev_opt_log` 运行中日志和 600 `BatteryModeStatusService` 同类型运行均应阻止重复计划。
6. `TASK-M460-OPT-SCHEDULE-006`：补最小测试：未到点跳过、已有运行日志跳过、600 同模式运行跳过、成功后禁用一次性计划、失败不更新计划。
7. `TASK-M460-OPT-SCHEDULE-007`：确认计划调度只调用 `ControlBattery.executeBatteryOpt(opt, SCHEDULED)`，不直接调用旧 `toSendCmdToOat`。

### P0：停止路径闭环

1. `TASK-M460-STOP-001`：复查 `ControlBattery.toSendStopBatteryCmdToOat`，确认 `_2/_6` 只走 `BatteryOptCollectorCommandAdapter.tryStop`，不进入旧 `genCmd30`。
2. `TASK-M460-STOP-002`：已完成。`BatteryOptCollectorCommandAdapter.tryStop` 已覆盖 `_2` 映射连接条模式、`_6` 映射内阻模式、模块命令开关关闭返回 `null`。
3. `TASK-M460-STOP-003`：已完成。`BatteryCollectorCommandService.stopRunningTest` 已覆盖同组同模式运行时停止成功，并取消队列中同组同模式未下发命令。
4. `TASK-M460-STOP-004`：部分完成。已覆盖无运行状态、运行类型不匹配；非法 `packNum`、非法 `mode` 和运行组不匹配仍待补。
5. `TASK-M460-STOP-005`：已完成最小验证。测试确认只取消同组同模式队列命令，不取消其他组内阻命令。
6. `TASK-M460-STOP-006`：复查停止后状态落点：`BatteryModeStatusService.markStopped`、`battery_device_state`、`dev_opt_log.result/status` 不应互相矛盾。
7. `TASK-M460-STOP-007`：确认停止返回文案能区分“已停止并取消队列”和“当前无运行测试/类型不一致”。
8. `TASK-M460-STOP-008`：确认 `_1/_3/_5` 停止链路暂不迁移，但不得影响 `_2/_6` 的 600 停止路径。

### P0：执行路径收敛

1. `TASK-M460-EXEC-001`：确认页面立即执行 `/batteryOpt/doCmdOptBatteryTest` 最终走 `ControlBattery.executeBatteryOpt` 或 `toSendBatteryCmdToOat`。
2. `TASK-M460-EXEC-002`：确认同步入口 `BatterySyncHandler` 执行测试时复用 `executeBatteryOpt(..., SYNC)`，不绕过采集适配器。
3. `TASK-M460-EXEC-003`：确认计划入口 `BatteryOptScheduleJob` 复用 `executeBatteryOpt(..., SCHEDULED)`，不重复写一套执行逻辑。
4. `TASK-M460-EXEC-004`：复查 `_2` 连接条电阻测试：前置电流条件通过后必须优先进入 `BatteryOptCollectorCommandAdapter.tryExecute`。
5. `TASK-M460-EXEC-005`：复查 `_6` 单节内阻测试：`modelNum` 必须在 `1..batteryCount`，非法时返回错误且不入队。
6. `TASK-M460-EXEC-006`：复查模块命令开关关闭时，非 `_2/_6` 和旧兼容类型仍可按原链路运行。
7. `TASK-M460-EXEC-007`：复查模块命令开关开启时，`_2/_6` 队列失败、异常、无通道都不得回退旧 `CommServer.returnCmd`。
8. `TASK-M460-EXEC-008`：扫描 `generateCommand`、`cmdBatteryControlService.genCmd0F/getCmd36` 调用面，确认没有新的 `_2/_6` 入口绕过适配器。
9. `TASK-M460-EXEC-009`：补 `ControlBatteryTest` 覆盖：`_2` 适配器返回成功时不调用旧命令生成；适配器返回错误时直接返回错误。
10. `TASK-M460-EXEC-010`：补 `_6` 执行路径测试：适配器返回成功/错误都截断旧链路。

### P0：日志和运行态一致性

1. `TASK-M460-LOG-001`：复查 `BatteryCollectorCommandLogService` 创建 600 命令日志时的 `type/source/status/result`，避免污染旧 `1..7` 测试类型。
2. `TASK-M460-LOG-002`：复查 `OptLogMapper.getRunningOptLog` 仍以 `result is null` 判断运行中，600 完成日志必须同步写旧 `result`。
3. `TASK-M460-LOG-003`：停止测试时应关闭或更新同类型运行日志，避免停止后计划任务仍认为有旧运行中测试。
4. `TASK-M460-LOG-004`：队列中未下发命令被取消时，应有可追踪状态或错误原因，不应表现为成功完成。
5. `TASK-M460-LOG-005`：失败、超时、响应码不匹配、payload 失败标志要映射到一致的 `status/result/error_message`。
6. `TASK-M460-LOG-006`：明确 `dev_opt_log` 中 `_2` 连接条测试、`_6` 单节内阻测试和 `type=99` 显式维护命令的边界。

### P1：连接条测试结果闭环

1. `TASK-M460-CONNRES-001`：复查 `0F` 启动命令成功后如何进入连接条工作模式，避免只入队但无可观测运行态。
2. `TASK-M460-CONNRES-002`：复查 `11/91` 地址推进：从 1 到 `batteryCount`，失败/超时是否停止推进并关闭运行态。
3. `TASK-M460-CONNRES-003`：确认 `batteryCount` 来源：优先 `BatteryPack` 单体数，空值或异常时上限 245，不能发送 0 或超过 245。
4. `TASK-M460-CONNRES-004`：确认 91 测试电压到连接条电阻的公式、单位、电流来源和无电流时处理。
5. `TASK-M460-CONNRES-005`：确认连接条结果写入标准实时表、兼容缓存和页面/统计读取字段，不再把电压伪装成电阻。
6. `TASK-M460-CONNRES-006`：补中断恢复策略：服务停止、串口重连、队列取消、模块无响应时如何结束 `dev_opt_log` 和模式状态。
7. `TASK-M460-CONNRES-007`：补最小测试覆盖：完整推进、单体失败、无电流跳过或失败、重复启动拒绝。

### P1：单节内阻测试闭环

1. `TASK-M460-SINGLEIR-001`：确认 `_6` 单节内阻测试只针对一个 `modelNum` 下发 600 `02/82`，不触发整组旧内阻流程。
2. `TASK-M460-SINGLEIR-002`：确认响应解析后结果写入哪个标准实时字段和统计入口。
3. `TASK-M460-SINGLEIR-003`：确认单节内阻测试期间 `BatteryModeStatusService` 使用的模式码与 `_1` 旧内阻测试不会互相误判。
4. `TASK-M460-SINGLEIR-004`：确认 `_6` 停止只停止 `_6` 当前运行或队列中的同类命令，不关闭 `_1` 旧内阻运行日志。
5. `TASK-M460-SINGLEIR-005`：补最小测试覆盖：合法单体入队、非法单体拒绝、无通道拒绝、停止类型不一致拒绝。

### P1：外部读取和接口契约

1. `TASK-M460-READ-001`：盘点页面测试记录、当前态、连接条结果、单节内阻结果读取是否还依赖旧 `dev_battery_report_log`。
2. `TASK-M460-READ-002`：JSON/TCP 输出字段继续切到标准实时快照，未确认字段保留兼容回退，不在输出层临时计算。
3. `TASK-M460-READ-003`：Modbus 高频读必须优先使用 `BatteryModuleRealtimeSnapshotService`，避免每次读 SQLite。
4. `TASK-M460-READ-004`：Modbus 告警和状态寄存器来源统一到 `battery_device_state`、`dev_alarm_log` 和标准实时模型。
5. `TASK-M460-READ-005`：确认 `/collector/battery/status`、`/metrics`、`/currentState` 能支撑现场排查 `_2/_6` 运行态。
6. `TASK-M460-READ-006`：为页面/第三方接口整理字段契约：空值、未就绪、stale、错误状态的返回口径。

### P1：告警和设备状态

1. `TASK-M460-ALARM-001`：继续补标准告警规则引擎，避免阈值、通信、设备状态告警散在多个 Map 拼装中。
2. `TASK-M460-ALARM-002`：复查 `CHANNEL_ERROR`、`MODULE_TIMEOUT`、`MODULE_ACTIVE=inactive`、`GROUP_246_FRESHNESS=stale` 的恢复口径。
3. `TASK-M460-ALARM-003`：补单体阈值告警按 `packNum + batNum + itemCode` 独立落库和恢复的回归覆盖。
4. `TASK-M460-ALARM-004`：告警屏蔽/解除继续复用 `dev_alarm_log` 标准能力，不恢复旧 980 屏蔽 runtime。
5. `TASK-M460-ALARM-005`：无数据源的热失控、氢气、网络、传感器类告警保持不生成，并在文档中标注原因。

### P1：SOC/SOH/容量和后处理

1. `TASK-M460-CAPACITY-001`：容量预测只在后处理流水线中运行，不进入采集轮询线程。
2. `TASK-M460-CAPACITY-002`：复查 `CapacityPredictionProcessor` 触发条件，确认缺少状态或历史数据时跳过而不是填 0。
3. `TASK-M460-CAPACITY-003`：复查操作日志 processor 与容量预测 processor 的执行顺序和异常隔离。
4. `TASK-M460-CAPACITY-004`：核容/备电 `_3/_5` 旧链路暂保留，后续迁移前先定义状态、日志和结果落点。
5. `TASK-M460-CAPACITY-005`：明确 SOC/SOH 字段在 JSON/TCP、页面、Modbus 中的未就绪输出口径。

### P2：采集主流程和运行稳定性

1. `TASK-MAINFLOW-001`：继续观察 `BatteryCollectorService` 主流程职责，暂不大迁包，优先稳定功能闭环。
2. `TASK-MAINFLOW-002`：复查显式命令队列优先级，确认不会被整轮 `1..245 + 246` 轮询长期阻塞。
3. `TASK-MAINFLOW-003`：复查串口读写、pending、超时、主动帧分发顺序，避免命令响应被当作普通轮询帧。
4. `TASK-MAINFLOW-004`：复查 6 通道并发和 SQLite 写入压力，现场前只做观测，不用删除数据掩盖锁等待。
5. `TASK-MAINFLOW-005`：补现场运行指标记录模板：队列长度、pending 超时、批次耗时、写库耗时、快照命中率。

### P2：现场联调和资料回填

1. `TASK-FIELD-001`：建立 600 控制命令样例记录，至少覆盖 `02/82`、`0F`、`11/91`、`03/83`、`08/88`、`18/A8`、`12/92`、`76/F6`。
2. `TASK-FIELD-002`：每个命令记录成功样例和失败/超时样例，包括请求帧、响应帧、通道、packNum、模块地址、耗时和日志结果。
3. `TASK-FIELD-003`：记录真实串口参数、超时/重试设置、6 通道并发压力和异常恢复表现。
4. `TASK-FIELD-004`：把待现场确认字段统一标为“待现场确认”，不把推测值写成已确认。
5. `TASK-FIELD-005`：现场确认后同步更新 `当前计划任务.md`、`蓄电池采集整合计划.md` 和对应协议辅助文档。

### P2：文档和归档维护

1. `TASK-DOC-001`：继续保持 `01document/README.md` 作为入口，新增计划先并入 `当前计划任务.md`。
2. `TASK-DOC-002`：完成的计划从“当前待办”移动到“近期已完成”，保留提交号和一句结果。
3. `TASK-DOC-003`：辅助文档只保留字段矩阵、告警规则、协议对照和寄存器草案；不再新增零散计划文档。
4. `TASK-DOC-004`：旧计划只归档不删除，恢复任务时先合并到当前任务入口。
5. `TASK-DOC-005`：文档编辑同样使用 UTF-8 无 BOM + LF，提交前跑 `git diff --check`。

## 执行约束

1. Java 源文件按 UTF-8 无 BOM + LF 写入，避免 PowerShell 默认编码。
2. 不修改或提交 `03code/energy/sql/rysqlite3.db`、`LoginInterceptor.java`、`.codegraph/`。
3. 代码任务先用 `.codegraph` 看调用面，再小步修改。
4. 功能优先，测试只覆盖核心共享逻辑和高风险回归。
5. 提交前必须执行 `git diff --check`，审查 staged diff 后再 `git commit`。

## 常用验证

```powershell
mvn "-Dtest=BatteryOptCollectorCommandAdapterTest,ControlBatteryTest,BatteryOptScheduleJobTest" "-Dmaven.test.skip=false" test
git diff --check
```
