# 当前计划任务

更新时间：2026-07-01

本文是后续继续整理计划任务的唯一当前入口。上一轮 M460/600 采集主线已归档到 `archive/completed-plans/当前计划任务_20260630_M460主线收口.md`；归档文件只作为历史依据和背景证据，不作为新会话自动执行任务来源。后续如需恢复归档中的任务，必须先把结论合并回本文的“当前待办”。

## 执行入口规则

1. 当前阶段只围绕 `ControlBattery` 全局整流程整合：页面立即执行、平台计划、同步立即执行、停止命令、日志状态闭环。
2. 不做重复无效防护：不再围绕 `/collector/battery/execute` 调试入口、旧聚合枚举文案、通用 unsupported 文案做额外改造；这些只作为背景边界。
3. 不从归档计划自动恢复任务；判断是否继续执行某项任务，以本文“当前待办”和用户最新指令为准。
4. Modbus 读写与告警拆分已阶段性完成，除非用户点名或现场确认回来，否则暂缓继续展开。
5. 开始前必须查看 `git status --short`；不得提交 `.codegraph/`、运行态数据库、协议原件和非本轮目标文件。
6. 文本文件按 UTF-8 无 BOM + LF 写入；终端显示乱码只视为显示链路风险，不得据此整文件转码。
7. 功能开发优先；测试验证任务原则上后置，当前主线只做必要的最小自检。

## 当前待办

### P0：ControlBattery 全流程整合主线

1. `TASK-M460-CONTROLBATTERY-FUNC-009`：梳理并固化统一执行流程。
   - 目标：把页面立即执行、同步立即执行、计划调度统一串为 `保存平台参数 -> ControlBattery.executeBatteryOpt -> 前置校验 -> adapter 分流 -> 旧 M460 fallback -> 日志/状态闭环`。
   - 已有基础：`/batteryOpt/doCmdOptBatteryTest`、`BatterySyncHandler` 非 `isNow=YES`、`BatteryOptScheduleJob` 均进入 `ControlBattery.executeBatteryOpt`；`/batteryOpt/edit` 和同步计划保存入口仅保存平台参数，不自动下发旧 `0x31..0x35`。
   - 已完成：页面立即执行的重复 running log 校验已下沉到 `ControlBattery.executeBatteryOpt`；manual/scheduled/sync 统一在入口内拦截同组运行中测试，入口不再各自维护分支。

2. `TASK-M460-CONTROLBATTERY-FUNC-010`：收敛执行分流表。
   - 目标：在 `ControlBattery.tryExecuteAdaptedCommand` 形成清晰分流表：`_1/_2/_6` 归采集模块 adapter，`_3/_5/stop` 归核容/备电模块 adapter，`_4` 明确为浮充配置边界。
   - 已有基础：`BatteryOptCollectorCommandAdapter` 负责 `_1/_2/_6`；`BatteryOptCapacityModuleCommandAdapter` 固定 `_3/_5/stop` 插入点；`_4` 执行入口返回边界提示。
   - 下一步：只在 `ControlBattery` 主流程和两个 adapter 内补齐真实缺口；不得再通过 `BatteryCollectorCommandService.execute(BatteryAggregateCommandDefinition...)` 间接处理测试计划。

3. `TASK-M460-CONTROLBATTERY-FUNC-011`：完善日志与状态闭环。
   - 目标：统一 manual/scheduled/sync 的 `dev_opt_log.source`、running log、`BatteryModeStatusService` 工作模式状态，避免计划误判运行中或重复触发。
   - 已完成：旧 M460/980 fallback 写入 `dev_opt_log` 时按 `BatteryOptExecuteType` 落库 `source`：`MANUAL -> web`、`SCHEDULED -> auto`、`SYNC -> json_tcp`；旧 `_1` stop 在本地判定无需继续下发停止命令时，会同步关闭 `_1` 运行日志并 `markStopped`。
   - 已完成：`_3/_5` 旧 fallback 仅在命令等待成功后插入 running log，失败不残留运行态；`_3/_5` stop 下发后继续按类型关闭 running log；计划失败仍不推进 `testTime`。

4. `TASK-M460-CONTROLBATTERY-FUNC-012`：补齐 `_1` 整组内阻真实新链路前置设计。
   - 当前边界：旧 `_1` 下游是 `Protocol_Package_Set_Command` -> 600/submodule 整组内阻状态机，不等同 `_6` 单节内阻；当前只能稳定插入并回退旧 M460。
   - 后续实现条件：需要等价旧 M460 整组状态机，包括逐节地址推进、浮充继电器状态、间隔控制、运行态、结果记录和 `dev_opt_log/battery_device_state` 闭环。
   - 禁止：不得把 `_1` 简化为循环调用 `_6`；不得绕过 `ControlBattery` 新增执行入口。

5. `TASK-M460-CONTROLBATTERY-FUNC-013`：补齐 `_3/_5/stop` 核容/备电真实新链路前置设计。
   - 当前边界：旧 `_3/_5/stop` 是 `0x30/E_Sys_Set_Verify_Capacity_Operation_Mode` -> 核容/空开/容量模块 Modbus 链路，不属于 600 单体采集队列。
   - 已有基础：`BatteryOptCapacityModuleCommandAdapter` 已固定插入点并记录旧 `0x30` 工作模式映射；无等价新通道时继续旧 `genCmd30` fallback。
   - 后续实现条件：需要明确核容/空开/容量模块的新命令通道、请求参数、回执、失败处理、停止闭环和容量预测依赖。
   - 禁止：未确认新模块通道前，不把 `_3/_5` 并入 `BatteryOptCollectorCommandAdapter` 或 600 单体采集队列。

### P1：计划调度一致性

1. `TASK-M460-SCHEDULE-FUNC-006`：计划任务继续复用 `ControlBattery.executeBatteryOpt(...SCHEDULED)`。
   - 已完成：`BatteryOptScheduleJob` 不再自行拼测试命令；`_4/0x31` 到点记录 SKIPPED 并推进下一次，避免每分钟重复失败。
   - 已完成：计划到点前会同时检查同组 running log 和采集侧任意运行中模式；可执行类型进入统一入口，`_4` 作为配置型任务跳过并推进，失败仍保留 `testTime` 等待下一轮。

2. `TASK-M460-SCHEDULE-FUNC-007`：避免平台计划与 M460 内置计划双触发。
   - 当前选择：平台调度为主，旧 `0x31..0x35` 配置下发只作为兼容边界。
   - 后续条件：如业务确认仍需设备内置计划配置下发，必须增加显式开关或说明，不能默认让平台计划和 M460 内置计划同时触发同一测试。

### P2：暂缓项

1. `TASK-M460-MODBUS-FUNC-001`：Modbus 读保持寄存器阶段完成，暂缓扩展。
2. `TASK-M460-MODBUS-FUNC-002`：Modbus 写寄存器阶段完成，暂缓扩展。
3. `TASK-M460-ALARM-FUNC-001`：告警方案阶段完成，暂缓扩展。
4. `TASK-M460-AGGREGATE-FUNC-001`：旧 980 聚合命令兼容层只作为背景边界；除非影响 `ControlBattery` 主流程，不主动扩展或新增防护。

## 已阶段完成

| 范围 | 当前结论 |
|---|---|
| `_2/_6` 立即执行 | 已优先走 600 模块命令队列，旧链路仅作兼容 fallback |
| `_2/_6` 停止 | 已通过新适配器和运行态判断收口 |
| `_1` 插入点 | 已接入稳定 adapter 并在能力未就绪时回退旧 M460；stop 本地结束会同步清理运行态；adapter 测试已按当前语义覆盖 execute fallback 与 stop 路由 |
| `_3/_5/stop` 插入点 | 已固定到核容/备电模块 adapter；未接入真实新模块通道前继续旧 `0x30` fallback |
| `_4/0x31` 边界 | 已明确为浮充配置/核容模块转发边界，不是 600 采集命令 |
| 计划保存入口 | `/batteryOpt/edit` 和同步计划保存只保存平台参数，不自动下发旧 `0x31..0x35`；页面立即执行不覆盖平台计划参数；无调用的旧配置下发入口 `toSendCmdToOat` 已删除 |
| 计划调度入口 | 已统一到 `ControlBattery.executeBatteryOpt(...SCHEDULED)`，并在同组任意运行中状态下跳过 |
| 统一运行中拦截 | manual/scheduled/sync 已统一在 `ControlBattery.executeBatteryOpt` 内检查同组 running log |
| 执行来源日志 | 旧 fallback 已按 manual/scheduled/sync 写入 `dev_opt_log.source` |
| Modbus 读写 | 阶段完成，除现场反馈外暂缓扩展 |
| 告警新链路 | 阶段完成，未确认旧位图来源暂缓扩展 |

## 后续候选池

以下只作整理素材，不作为默认执行任务：

1. `_2/_6` 旧 980 fallback 最终删除：待默认切到 600 后再评估。
2. `/batteryOpt/edit` 是否恢复旧配置下发：需要业务确认，未确认前不改行为。
3. `_7` 充电测试：当前只可视作充电状态派生日志/状态类型，不凭枚举直接新增命令。
4. 6 通道并发、SQLite 写入压力、状态 upsert 频率：进入现场观察，不通过删除数据或关闭功能掩盖。
5. 600 控制命令真实串口样例：等待现场样例后回填请求帧、响应帧、耗时和日志结果。

## 现场确认清单

以下依赖真实设备、现场平台或人工业务确认，不作为默认开发任务：

1. 6 路串口参数、轮询间隔、超时、重试、通道并发和 SQLite 锁等待。
2. `03/83`、`08/88`、`0F`、`11/91`、`18/A8`、`12/92`、`76/F6` 的真实请求、响应、耗时、失败恢复、页面返回和日志结果。
3. 核容/备电/空开/容量模块的新命令通道、回执语义和停止闭环。
4. Modbus RTU 站号、端口、异常码、写权限、上位系统对寄存器地址的最终确认。
5. SOC/SOH、容量预测、核容、备电时长算法是否完整迁移，以及旧 `dev_opt_log`、`pre_battery_group` 的保留边界。

## 执行约束

1. 默认只处理 `ControlBattery` 全流程整合主线；其他分支只读，不主动执行。
2. 不再做“看起来更安全但不影响主流程”的重复拦截、重复文案、防御性改造。
3. 功能开发优先；测试验证后置，当前主线只做必要的最小自检。
4. 提交前必须执行 `git diff --check`，审查 staged diff 后再 `git commit`。
5. 不提交 `.codegraph/`、运行态数据库、协议原件和非本轮目标文件。