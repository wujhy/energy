# 实时模型字段矩阵

更新时间：2026-08-04

本文承接 `TASK-REALTIME-FIELD-AUDIT-001`，用于约束标准实时模型、兼容投影和当前态消费边界。当前结论只覆盖代码中已经落地的字段；现场协议、单位或算法未确认的字段保持空值或兼容投影，不允许在内部计算链路写假默认值。

## 总体规则

1. 当前态业务优先读取 `BatteryModuleRealtimeSnapshotService.getCachedSnapshot(packNum)`，只使用 `BatteryModuleRealtimeSnapshot`、`BatteryModuleGroupRealtime`、`BatteryModuleCellRealtime`。
2. `getCachedSnapshot(...)` 只读 Ehcache `battery-realtime-snapshot`，不回源实时表；缓存由采集完成后的 `refreshAfterPoll(...)` 写入。
3. 快照新鲜度由 `battery-realtime-snapshot` TTL 和 `BatteryModuleRealtimeSnapshot.refreshedAt` 共同约束，消费者不得改用历史表时间或旧 Redis 缓存时间判断当前态有效性。
4. `BatteryReportLog`、`BatteryMonitor`、`packParam`、`monitorData` 只保留给历史查询、历史导出、旧兼容入口和明确的兼容同步处理。
5. `project.iot` / `iot.battery` 下旧入口不作为新实时链路消费方，后续只做删除或编译兜底。

## 组级实时字段

| 标准字段 | 数据库字段 | 协议/计算来源 | 单位 | 业务语义 | 兼容投影字段 | 允许消费方 |
|---|---|---|---|---|---|---|
| `packNum` | `pack_num` | 通道配置 | 编号 | 电池组编号 | - | 全部实时消费者 |
| packVoltage | pack_voltage | BatteryModuleGroupCalculationService 汇总有效单体电压 | V | 组总电压 | packVoltage | 当前态 DTO、历史兼容适配、统计入库等分别透传 packVoltage；不因 externalVoltage 缺失或存在而改写该字段 |
| `chargeDischargeCurrent` | `charge_discharge_current` | 246 组模块 | A | 组充放电电流，内部标准电流字段 | `packCurrent` | 控制门控、容量预测、状态投影、统计入库、页面/上报 DTO；`pack_current` 旧列不作为新链路来源 |
| `floatCurrent` | `float_current` | 246 组模块 | A | 浮充电流，标准 600 链路按 0.001A 解析 | `batteryPackFloatCurrent` | 页面/上报 DTO、统计展示；旧兼容名只在 DTO/packParam 投影使用 |
| externalVoltage | external_voltage | 246 组模块 | V | 外组压，标准 600 链路按 0.01V 解析 | batteryPackOuterVoltage | 与 packVoltage 分别透传；组压告警按 externalVoltage 优先、缺失时才取 packVoltage |
| `environmentTemperature1` | `environment_temperature1` | 246 组模块 | 摄氏度，按 0.1℃ 解析 | 环境温度 1 | - | 页面/上报 DTO、统计展示、告警上下文 |
| `environmentTemperature2` | `environment_temperature2` | 246 组模块 | 摄氏度，按 0.1℃ 解析 | 环境温度 2 | - | 页面/上报 DTO、统计展示、告警上下文 |
| `cellCount` | `cell_count` | 采集后计算 | 个 | 配置/快照内单体数量 | - | 运行态视图、诊断 |
| `onlineCellCount` | `online_cell_count` | 采集后计算 | 个 | 本轮有效单体数量 | - | 运行态视图、诊断 |
| `staleCellCount` | `stale_cell_count` | 采集后计算 | 个 | 连续缺采单体数量 | - | 运行态视图、诊断 |
| `dataFresh` | `data_fresh` | 采集后计算 | 布尔 | 本轮组+单体完整性结果 | - | 运行态视图、诊断；不替代快照 TTL |
| `maxCellVoltage` / `minCellVoltage` / `avgCellVoltage` / `voltageRange` | 对应统计列 | 单体实时计算 | V | 单体电压统计 | `batteryVoltageDeviation`、`batteryVoltageRange` | 页面/统计/告警上下文 |
| `maxCellTemperature` / `minCellTemperature` / `avgCellTemperature` / `temperatureRange` | 对应统计列 | 单体实时计算 | 摄氏度 | 单体温度统计 | `batteryAvgTemperature` | 页面/统计/告警上下文 |
| `maxInternalResistance` / `minInternalResistance` / `avgInternalResistance` / `resistanceRange` | 对应统计列 | 单体实时计算 | 微欧或协议原单位，待现场确认 | 单体内阻统计 | - | 内阻基准、页面/统计 |
| `batteryPackSoc` | `battery_pack_soc` | 旧 82 入口解析；标准 600 链路暂无生产方 | % | SOC | 同名旧字段 | 页面/上报兼容展示；内部计算不得从 SOH、容量或备电时长推导，缺失保持 null |
| `batteryPackSoh` | `battery_pack_soh` | `CapacityPredictionProcessor` / `BatteryPredictorServiceImpl` 预测结果；旧 82 入口仅保留历史兼容 | % | SOH | 同名旧字段 | 健康报告、页面/上报兼容展示；由 `PreBatteryGroup.soh` 回填，缺失保持 null |
| `batteryPackStatus` | `battery_pack_status` | 当前由兼容填充服务按电流投影 | 枚举 | 对外电池组状态投影 | 同名旧字段 | `_5` 门控、Modbus/页面兼容输出 |
| `resistanceTestStatus` | `resistance_test_status` | 由 `_1/_6` 运行态通过 `BatteryModeStatusService` 投影；无运行态时兼容 NOT_TESTING | 枚举 | 内阻测试状态投影 | 同名旧字段 | 仅对外兼容；内部不得依赖该字段判断运行或结束 |
| `deviceWorkStatus` / `deviceWorkIOStatus` | `device_work_status` / `device_work_io_status` | 标准 600 链路暂无生产方，旧兼容字段 null-safe 透传 | 枚举 | 设备工作状态兼容投影 | 同名旧字段 | 当前态 DTO、页面/大屏、JSON/TCP 和历史同步兼容输出；内部不得依赖该字段判断运行态 |
| `bcapacity` | `bcapacity` | `BatteryPredictorServiceImpl` / `PreBatteryGroup` 预测结果 | Ah 或待现场确认 | 预估可用容量 | 同名旧字段 | 容量预测结果展示、统计入库、Modbus 读；本轮未算出的单体可沿用上次预测值，组容量只从非空单体预测容量中取最小值，缺失保持 null |
| `capacity` | `capacity` | 组级标准 600 链路暂无生产方；单体 `capacity` 由 `PreBatteryVo.bcapacity` 缓存回填 | Ah 或待现场确认 | 额定或展示容量兼容字段 | 同名旧字段 | 页面兼容展示；内部额定容量计算使用 `BatteryPack.batCapacity`，单体容量仅表示预测可用容量缓存 |
| `disChargeCapacity` | `dis_charge_capacity` | `_5` 放电结束后 `BatteryPredictorServiceImpl` 预测结果 | Ah 或待现场确认 | 本次放电容量 | 同名旧字段 | 容量预测结果展示、操作日志容量结果、Modbus 读；缺失保持 null |
| `backupDuration` | `backup_duration` | `BatteryPredictorServiceImpl` / `PreBatteryGroup` 预测结果；旧 82 入口仅保留历史兼容 | 分钟，待现场确认 | 预估备电时长 | 同名旧字段 | 健康报告、Modbus 读、页面/上报兼容展示；缺失保持 null，不从 `residualDischargeDuration` 回填 |
| `disChargeDuration` | `dis_charge_duration` | 旧 82 入口解析；标准 600 链路暂无生产方，`_5` 实际耗时以业务日志开始/结束时间计算 | 分钟，待现场确认 | 已放电时长兼容字段 | 同名旧字段 | 页面/上报兼容展示和 Modbus 兼容读；缺失保持 null，不作为 `_5` 自动停止依据 |
| `residualDischargeDuration` | `residual_discharge_duration` | 旧 82 入口解析；标准 600 链路暂无生产方 | 分钟，待现场确认 | 剩余放电时长兼容字段 | 同名旧字段 | 页面/上报兼容展示；缺失保持 null，不与 `backupDuration` 合流或互相回填 |
| `latestCellUpdateTime` | `latest_cell_update_time` | 采集后计算 | 时间 | 最近单体更新时间 | - | 诊断和后续新鲜度评估 |
| `latestGroupUpdateTime` | `latest_group_update_time` | 246 组模块新鲜采集 | 时间 | 最近组模块更新时间 | - | 诊断和后续新鲜度评估 |
| `groupModuleFresh` | `group_module_fresh` | 246 组模块采集结果 | 布尔 | 组模块是否为本轮新数据 | - | 诊断、告警状态来源和兼容投影保护 |
| `pollBatchNo` / `pollStartedAt` | `poll_batch_no` / `poll_started_at` | 采集上下文 | 批次/时间 | 本轮采集批次 | - | 快照合并、后处理、诊断 |

## 单体实时字段

| 标准字段 | 数据库字段 | 协议/计算来源 | 单位 | 业务语义 | 兼容投影字段 | 允许消费方 |
|---|---|---|---|---|---|---|
| `packNum` | `pack_num` | 通道配置 | 编号 | 电池组编号 | - | 全部实时消费者 |
| `batNum` | `bat_num` | 600 节模块端单体地址 | 编号 | 单体编号 | `BatteryMonitor.batNum` | 全部单体实时消费者 |
| `voltage` | `voltage` | 600 节模块端 | V | 单体电压 | `BatteryMonitor.voltage` | 告警、统计、页面、Modbus 读 |
| `resistance` | `resistance` | 600 节模块端/内阻采集 | 协议原单位，待现场确认 | 单体内阻 | `BatteryMonitor.resistance` | 内阻基准、统计、页面；基准平均值只统计有效内阻，缺失不按 0 参与 |
| `temperature` | `temperature` | 600 节模块端 | 摄氏度 | 单体温度 | `BatteryMonitor.temperature` | 告警、统计、页面 |
| `capacity` | `capacity` | 协议暂不直接提供 | 待确认 | 单体容量 | `BatteryMonitor.capacity` | 缺失保持 null |
| `resistanceRageSlip` | `resistance_rage_slip` | 连接条电阻命令/兼容填充 | 待确认 | 连接条电阻 | 同名旧字段 | 连接条电阻结果展示 |
| `resistanceRateChange` | `resistance_rate_change` | 实时单体内阻与内阻基准对比兼容计算 | % 或待确认 | 内阻变化率 | 同名旧字段 | 统计/页面；缺少实时单体、内阻基准或有效可比单体时保持 null，有有效可比单体且无升高时才为 0 |
| `swollenVoltage` | `swollen_voltage` | 鼓包检测 | V | 鼓包电压 | - | 告警/页面 |
| `leakageStatus` | `leakage_status` | 漏液检测 | 枚举 | 漏液状态 | - | 告警/页面 |
| `createTime` / `pollBatchNo` / `pollStartedAt` | `create_time` / `poll_batch_no` / `poll_started_at` | 采集上下文 | 时间/批次 | 单体数据入库时间与本轮采集批次 | - | 当前态 DTO、快照合并、后处理、诊断 |

## 当前态消费者边界

| 消费方 | 当前状态 | 后续边界 |
|---|---|---|
| 控制前置判断 `ControlBattery` / `ControlBatterySet` | 已直接读取实时快照 | 缺快照时早返回，不回源历史日志；内阻基准保存缺少有效内阻时保持 null |
| 容量预测 `CapacityPredictionProcessor` / `BatteryPredictorServiceImpl` | 已使用组/单体实时实体 | 算法字段缺失保持 null；本轮单体预测与上一轮缓存按单体号合并，不在控制 adapter 内计算结果 |
| 内阻基准与统计 `ConfigurationBatteryServiceImpl`、`StatBattery*` | 已有实时实体入口，仍保留历史/兼容接口 | 内阻统计入库需覆盖配置单体编号且内阻非空；组压统计按 `packVoltage` 缺失才退化 `externalVoltage`；继续减少 `BatteryMonitor` 适配，只保留历史导出 |
| 当前态 DTO `BatteryCurrentStateService` | 已读取实时快照并透出快照诊断字段 | `snapshotRefreshedAt`、`snapshotDataReady`、`snapshotFresh`、`currentBatchCellNums`、`staleCellNums`、`missingCellNums`、`cellMissCounts` 仅用于页面/诊断判断新鲜度和缺采状态，不替代业务内部实时实体计算 |
| 页面/大屏 `ScreenServiceImpl` | 已读取实时快照，DTO 保留 `BatteryReportLogIndex.packParam` 兼容输出 | 大屏相关已完成且基本稳定，后续不再默认扩展；对外结构继续兼容，内部不得再查历史当前态 |
| JSON/TCP 上报 `DataReportJob` | 已读取实时快照 | 继续保留协议 DTO，不恢复 `jsonTcpRealtimeSourceEnabled` 切源判断 |
| 告警 `AlarmContextProcessor` / `BatteryAlarmEvaluationService` | 已基于标准实时模型和状态/阈值来源上下文 | 陈旧快照只提交状态告警；旧位图专属告警保持 unsupported，等待现场来源确认 |
| 历史查询 `BatteryReportLogController` / `BatteryReportLogServiceImpl` | 继续查 `dev_battery_report_log` | 不迁移到实时表 |
| 旧入口 `project.iot` / `iot.battery` | 旧兼容路径 | 不接新实时链路 |

## 未确认项

1. 内阻、连接条电阻、容量、SOC/SOH、放电时长等字段单位和算法口径仍需现场确认；其中 `batteryPackSoc`、`disChargeDuration`、`residualDischargeDuration` 在标准 600 链路暂无生产方，`batteryPackSoh`、`bcapacity`、`backupDuration`、`disChargeCapacity` 只承接容量预测结果。
2. `batteryPackStatus` 当前可作为对外兼容投影和 `_5` 门控，但不代表内部运行态唯一来源。
3. `resistanceTestStatus` 已结合 `_1/_6` 运行态和 `BatteryModeStatusService` 做对外投影；内部运行/结束判断仍以生命周期、running optLog 和模式状态为准。
4. `deviceWorkStatus` / `deviceWorkIOStatus` 标准 600 链路暂无生产方，当前仅按 null-safe 对外透传；未确认来源前不参与内部运行态判断。
5. 告警新链路已不依赖旧状态查询补上下文；未确认来源的旧位图告警继续保持 unsupported。
