# 蓄电池采集整合计划

更新时间：2026-06-02

## 1. 总体方向

本次整合不是恢复 `energy -> M460` 的 980 节中间协议链路，而是把 M460 已承担的底层采集、控制、计算和上行能力逐步收敛到 `energy` 内部。整合完成后，`energy` 应直接面对下行 600 节采集模块、内部蓄电池标准数据模型、JSON/TCP 兼容上报和后续上行 Modbus。

核心原则：

1. 下行采集以 `3_CM-02N 600节协议A0版_采集模块端_20231025.xlsx` 为运行协议来源。
2. 980 节 PC 端、网络端协议只作为旧 M460 行为参考和兼容语义参考，不再作为新增运行链路。
3. 页面、JSON/TCP、后续 Modbus 写控制必须调用内部服务，再由 600 模块端显式命令队列下发。
4. JSON/TCP 和 Modbus 最终读取同一套标准实时模型和缓存，避免多套数据口径。
5. `configId/devId/config_id` 可继续作为外部兼容字段和数据库字段保留，但内部业务不再把调用方传入的 `configId` 当作可变维度。

协议定位：

| 协议/链路 | 当前定位 | 处理原则 |
|---|---|---|
| 600 节采集模块端协议 | 下行采集主协议 | 直接实现采集、解析、入库、组计算和显式控制 |
| 980 节 PC 端协议 | 旧 `energy <-> M460` 中间协议 | 不恢复运行链路，只参考命令语义和状态机 |
| 980 节网络端协议 | 旧 JSON/TCP 聚合控制参考 | 不恢复任意透传，只按内部蓄电池服务逐项迁移 |
| SH 980 节 Modbus | 后续上行控制/查询协议 | 标准模型稳定后接入，写寄存器映射到内部控制服务 |
| 原 energy JSON/TCP | 外部兼容上报协议 | 保留，逐步从旧兼容历史切到标准实时模型 |

## 2. 目标架构

```text
下行采集模块/蓄电池
        |
        | 6 路 RS485，600 节采集模块端协议
        v
energy battery collector
        |
        | 解析、批次落库、轻量组计算、异步后处理
        v
energy 蓄电池标准实时模型
        |
        +--> 页面、告警、统计
        +--> JSON/TCP 兼容上报
        +--> 后续 Modbus RTU 查询与控制；Modbus TCP 是否纳入范围待确认
```

功能分层：

1. 通道配置：`BatteryPack.packNum` 对应电池组号，`BatteryPack.batSinSize` 对应该组期望单体数，6 路下行 RS485 固定映射 6 组蓄电池。
2. 自动采集：默认轮询 `01/81`。单体地址为 `1..245`，`246` 固定为组电流/温度模块地址。
3. 批次落库：每轮生成 `pollBatchNo`，批量写入单体实时表、组实时表，并执行轻量组计算。
4. 异步后处理：SOC/SOH、容量预测、告警上下文、兼容历史写入和缓存刷新不阻塞采集线程。
5. 控制命令：页面、JSON/TCP、后续 Modbus 写控制统一走内部控制服务和 600 模块端命令队列。
6. 对外读取：实时页面、告警、JSON/TCP、Modbus 逐步统一读取标准实时表和缓存；`dev_battery_report_log` 降级为兼容历史过渡表。

## 3. 当前已有基础

已具备并应保留的基础：

- `com.shanhe.project.collector.battery` 独立采集模块。
- 6 路下行采集配置模型、通道运行态、轮询、超时、重试和接收缓冲处理。
- RS485 帧基础：`START + address + command + length + payload + check + 0D`。
- `BatteryDeviceProtocolCode`、`BatteryModuleFrameSummaryService` 等 600 模块端识别和摘要能力。
- `BatteryModuleFrameDispatcher`、`BatteryModuleFrameConsumer` 作为 600 原始帧扩展点。
- `BatteryModuleRealtimeConsumer` 已支持批次缓冲、批量 upsert、单体实时表、组实时表和兼容历史过渡。
- `BatteryModuleGroupCalculationService` 已具备电压、温度、内阻、在线数、新鲜度等轻量组计算入口。
- 600 控制命令已通过 `BatteryModuleControlCommandService` 构造，并由采集通道显式命令队列发送。

已确认的采集边界：

- 自动轮询只允许 600 模块端 `01/81` 作为默认主命令。
- `02/82`、`06/86`、`07/87`、`0B/8B`、`0D/8D`、`0E/8E` 属于旧 980 聚合语义，不能作为 600 默认轮询命令来源。
- `246` 是组模块地址，不是组计算结果本身；它提供组电流、外组电压、环境温度等原始组数据。
- 每轮必须保留 `246` 采集；响应缓存可减少 `1..245` 的无效全量扫描，但不能跳过 `246`。

## 4. 标准数据模型方向

当前模型方向：

| 对象 | 定位 | 口径 |
|---|---|---|
| `battery_module_cell_realtime` | 单体最新有效值 | 以 `pack_num + bat_num` 唯一；字段向旧 `monitor_data` / `BatteryMonitor` 兼容 |
| `battery_module_group_realtime` | 组最新状态 | 以 `pack_num` 唯一；同时承载 `246` 原始组数据和轮询结束后的轻量组计算结果 |
| `dev_battery_report_log` | 兼容历史过渡 | 继续服务历史查询、导出和未改造旧功能；不作为长期实时主数据源 |
| `dev_alarm_log` | 标准告警落地 | 优先复用/扩展，不新增独立模块告警表 |
| `dev_config_attribute` | 蓄电池组属性/告警参数 | 继续配合 `dev_battery_pack` 使用，暂不改名 |
| `dev_template_attribute` | 电池组默认属性种子 | 保留，由迁移脚本维护 |

暂不做：

- 暂不新增独立 `battery_module_cell_history`、`battery_module_group_history`。
- 暂不删除数据库 `config_id` 字段。
- 暂不一次性迁移旧缓存 key。
- 暂不把 SOC/SOH/容量预测放入采集轮询线程。

## 5. 已完成状态

### 5.1 下行采集和 600 控制

- 已建立 600 模块端采集帧解析、分发、批次上下文和实时表写入路径。
- 已将 600 原始帧默认桥接旧 `BatteryHandler` 的路径移除。
- 已阻断 980 聚合命令直接透传到 600 采集通道。
- 已实现或收口以下 600 显式控制命令构造：
  - `02/82` 内阻测试
  - `03/83` 均衡
  - `08/88` 手动编号
  - `18/A8` 自动编号
  - `0F` + `11/91` 连接条测试基础链路
  - `12/92` 内阻系数
  - `0A` 清调试数据
  - `76/F6` 校准维护
- 已补齐显式命令完成状态观测：成功、失败、超时均更新通道状态。
- 手动编号、自动编号成功后会清理对应通道地址缓存，并强制下一轮全量发现。

### 5.2 旧 energy 精简

- 已删除或阻断旧动态协议维护、模板维护、通用设备控制、原始命令透传、空调/红外/通用历史等大量旧平台能力。
- 已删除 `BatteryPackAsync`、`DeviceCmdService`、`CmdBatteryService`，电池组维护后不再向 M460 下发串口参数、组配置、告警同步或旧存储指令。
- 已删除旧 `dev_battery_monitor` 持久化链路，`BatteryMonitor` Java 对象仅作为兼容 DTO 保留。
- 已删除数据库字典管理功能；`DictService` 当前作为 Java 枚举静态字典保留。
- `resources/dist` 不作为维护对象，不能因旧前端产物恢复已删除后端功能。

### 5.3 `/battery/set` 兼容入口

旧 URL 和返回结构继续保留，但实现逐步迁入 `ControlBatterySet`、本地能力或 600 控制队列：

| 能力 | 当前处理 |
|---|---|
| 手动编号 | 使用 600 `08/88` |
| 自动编号 | 使用 600 `18/A8`，按本地电池组配置驱动 |
| 内阻系数 | 使用 600 `12/92` |
| 校准维护 | 使用 600 `76/F6` |
| 清组调试数据 | 本地清理后下发 600 `0A` |
| 电池版本 | 读取本地主机配置版本/软件版本兼容返回 |
| 均衡/蜂鸣状态 | 先本地保存状态，实际单体均衡动作后续接入 600 `03/83` |
| 鼓包基准 `delGb` | 已确认不是清调试数据，暂不迁移，需先设计鼓包基准落点 |

### 5.4 默认设备和配置边界

最近已完成：

- `Constants.DEFAULT_CONFIG_ID = 1` 作为唯一默认蓄电池设备 ID。
- `IConfigService` 已弱化为静态默认配置提供者。
- `IConfigService.getCache/updateCache` 已删除，`CacheKeyEnum.CONFIG` 已删除，不再维护 `device-config/config` 缓存。
- `IBatteryPackService.selectBatteryPackListCache` 新增缓存列表读取方法，返回 `BatteryPack` 副本并按 `packNum` 排序。
- `Config.packList` 只作为接口/上报兼容结构临时封装，不作为内部业务状态来源。
- `DataReportJob`、`DeviceOnlineJob`、`ControlBattery`、`ConfigHandler` 已改为直接读取 `BatteryPack` 缓存列表或默认设备。
- 扩展状态从 `Config` 移到 `Host`：`dev_host.extend3` 维护平衡、蜂鸣器等本机扩展 JSON，`HostMapper` 已映射 `extend1..extend4`。
- `Host` / `Config` 不再承载上下线状态；`DeviceOnlineJob` 读取 `BATTERY_ONLINE` 和 `BatteryPack` 缓存列表判断组 `TXZT`。
- `PreBatteryGroupMapper` 的最新预测查询和删除链路已收口为按 `packNum` 调用，XML 内部固定默认设备过滤，`config_id` 仅作为数据库兼容字段保留。
- `StatBatteryBatMapper`、`StatBatteryPackMapper`、`StatBatteryResMapper` 的统计删除链路已收口为按 `packNum` 调用，XML 内部固定默认设备过滤，`config_id` 仅作为数据库兼容字段保留。
- `BatteryReportLogMapper` 的兼容历史删除链路已收口为按 `packNum` 调用，XML 内部固定默认设备过滤，`config_id` 仅作为数据库兼容字段保留。
- `DevBatteryOptMapper` 的测试操作参数删除链路已收口为按 `packNum` 调用，XML 内部固定默认设备过滤，`config_id` 仅作为数据库兼容字段保留。
- `OptLogMapper` 的默认设备清理和按组删除链路已收口为无 `configId` 入参调用，XML 内部固定默认设备过滤，运行中日志查询和缓存逻辑保持不变。
- `OptLogMapper` 的运行中日志、数量统计和最近日志查询已收口为无 `configId` 入参调用，XML 内部固定默认设备过滤，service 接口和缓存逻辑保持不变。
- `BatteryPackMapper` 的默认设备列表、按组查询、默认设备清理和最大单体数查询已收口为无 `configId` 入参调用，XML 内部固定默认设备过滤，外部 service 接口和缓存 key 保持兼容。
- `StatBatteryResMapper.selectList` 和 `StatBatteryBatMapper.selectDataPointList` 已收口为无 `configId` 入参调用，XML 内部固定默认设备过滤，统计/容量 service 对外接口保持不变。
- `ConfigAttributeMapper` 的默认设备属性列表、默认设备清理、按组删除和按编码查询已收口为无 `configId` 入参调用，XML 内部固定默认设备过滤，模板属性插入和缓存 key 保持兼容。
- `ConfigAttributeMapper.insertByTemplateAttribute` 已收口为无 `configId` 入参调用，XML 内部固定默认设备写入；属性缓存 key 仍保持兼容。
- `BATTERY_PACK_INFO`、`ATTRIBUTE`、`ALARM`、`STORAGE_TIME` 缓存 key 已移除默认设备 ID 维度，按 `packNum`、`packNum/code`、`packNum/modelNum/itemCode` 或 `grouping` 命名；落库字段仍保留 `config_id` 兼容。
- `AlarmLogMapper` 的默认设备清理和按组删除链路已收口为无 `configId` 入参调用，XML 内部固定默认设备过滤，告警缓存 key 和落库字段保持兼容。
- M460 系统状态整合方案已收敛：`BatteryModeStatusService` 仅承接当前测试/维护模式缓存（0 空闲、1 自动编号、6 内阻测试、10 连接条电阻测试），用于页面刷新和控制互斥；操作/测试过程继续复用并扩展 `dev_opt_log`，设备/通道/组/模块当前态单独落到 `battery_device_state`。
- 已新增迁移脚本 `V20260529.01__battery_state_task.sql`，只扩展既有 `dev_opt_log` 并新增 `battery_device_state` 当前态表；代码接入后续再处理。

提交说明：

- 后续接手以 `git log --oneline -5` 和 `git status --short` 的实时结果为准，不再在本文档维护固定最近提交列表。
- 已提交内容与未提交工作区可能并存，执行任务前必须先确认本地 diff，避免把其他任务的代码改动混入本任务。

## 6. 阶段路线和命令边界

### 6.1 阶段路线

| 阶段 | 目标 | 当前状态 |
|---|---|---|
| 1. 下行采集协议闭环 | 600 模块端 `01/81` 轮询、地址缓存、`246` 固定采集、原始帧调试和状态观测 | 已具备基础能力，真实串口参数仍需现场校正 |
| 2. 标准数据模型与入库 | 单体实时表、组实时表、批次 upsert、兼容历史过渡 | 已具备实时表和兼容历史路径，切源仍需逐项推进 |
| 3. 电池组计算 | 每轮结束后同步轻量计算，长周期算法进入异步后处理 | 轻量组计算已具备，SOC/SOH/容量预测仍待迁移 |
| 4. 原业务衔接 | 页面、告警、统计、JSON/TCP 逐步改读标准模型 | 正在推进，`dev_battery_report_log` 仍作为过渡 |
| 5. 上行 Modbus | 只读映射、异常/空值口径、写控制映射到内部控制服务 | 后置，RTU 运行模型和 TCP 范围待确认 |
| 6. 原 energy 精简 | 移除多设备平台残留，收口默认设备边界 | 持续推进，数据库字段和旧缓存 key 暂保留 |

### 6.2 命令边界

| 命令来源 | 处理边界 |
|---|---|
| 600 模块端 `01/81` | 默认自动轮询主命令，负责单体和 `246` 组模块数据采集 |
| 600 显式控制命令 | 只能通过内部服务构造并进入采集通道命令队列，不允许页面或外部接口直接透传原始命令 |
| 980 PC/网络聚合命令 | 仅作为旧 M460 行为参考；不得重新成为 `energy -> M460 -> 600 模块` 的运行链路 |
| `/battery/set` 旧 URL | URL 和返回结构保持兼容，具体能力迁到 `ControlBatterySet`、本地状态或 600 控制队列 |
| JSON/TCP `controlDev` | 保留废弃响应，阻断通用透传；后续只允许明确映射到内部蓄电池控制服务的能力 |
| JSON/TCP 蓄电池测试 `_43` | `BatterySyncHandler` 仍保留迁移开关；开启后只对已明确映射的测试控制优先走采集模块命令队列，未映射或无法定位通道时回退旧兼容路径 |
| Modbus 写寄存器 | 先做映射草案，最终必须调用内部控制服务；不提前固化 RTU/TCP 运行链路 |
| `configId` 精简 | `BatteryCollectorCommandService.resolveChannelName(Integer)` 已作为内部主入口，`ControlBatterySet` 与 `BatterySyncHandler` 通道定位只按 `packNum`；`OptLogServiceImpl` 内部测试日志私有链路不再传递 `configId`，`OPT_LOG` 缓存 key 收敛为 `log:packNum:type`；`STAT_BATTERY_GROUP` 缓存 key 收敛为 `stat:battery:group:packNum`；`StatBatteryBat/Pack/ResService`、`PreBatteryGroupService`、`BatteryReportLogService`、`DevBatteryOptService` 对外删除方法改名为 `deleteByPackNum`，`IBatteryPackService.selectBatteryPackListConfigId` 改名为 `selectBatteryPackList`，`IConfigAttributeService.selectByConfigId` 改名为 `selectDefaultDeviceAttributes`，Mapper/数据库字段名暂保留；`StatBatteryController`、`ConfigurationBatteryController`、`OptBatteryController`、`ScreenController`、`BatteryPackController`、`BatteryReportLogController`、`AlarmLogController` 保留外部 `configId` 请求参数但在 Java 侧显式标记为 `ignoredConfigId`；`OptBatteryController.applyDefaultConfigId` 已删除，确需默认 ID 时直接使用 `Constants.DEFAULT_CONFIG_ID`；旧 `resolveChannelName(Long, Integer)` 暂保留为兼容代理 |

### 6.3 专项文档索引

| 专项文档 | 用途 | 注意 |
|---|---|---|
| [文件编码与编辑约束.md](文件编码与编辑约束.md) | 统一约束项目内所有文件的可行编码与编辑方式，防止 PowerShell 重写导致的 BOM/UTF-16 问题重复出现 | 优先 `apply_patch`，禁止用 `Set-Content` / `Out-File` / `>` / `>>` 重写文本文件 |
| `01document/protocol/600节采集模块端命令核对.md` | 600 模块端 `01/81`、`02/82`、`03/83`、`08/88`、`18/A8`、`0A`、`0F`、`11/91`、`12/92`、`76/F6` 等请求/响应和 payload 核对 | 作为下行采集和 600 控制命令细节来源 |
| `01document/protocol/M460协议功能对照表.md` | 从 M460 源码能力反向校验 `energy` 是否遗漏采集、控制、告警、计算和上行能力 | 不复制源码过程，只记录能力覆盖状态和取舍 |
| `01document/protocol/SH_Modbus寄存器映射草案.md` | Modbus 读寄存器到标准实时模型、写寄存器到内部控制服务的映射草案 | 真实 RTU 链路和写控制仍后置 |

### 6.4 数据和脚本边界

- 后续迁移脚本统一放在 `03code/energy/src/main/resources/db/migration`，数据库按 SQLite 语法处理。
- `dev_template_attribute`、`dev_config_attribute`、`dev_battery_report_log` 继续保留。
- 可删除旧表需要先确认运行期升级策略、现场历史数据保留策略和 Flyway 记录处理方式。
- 开发期可以谨慎精简建表脚本；不把删除 Flyway 记录当作运行期升级方案。
- `resources/dist` 不维护；不能因旧前端产物恢复已删除的后端旧功能。

### 6.5 关键开关和联调入口

| 配置/入口 | 当前口径 |
|---|---|
| `activeChannels` | 现场联调时先启用单通道，确认稳定后再开 6 通道并发 |
| `debugEnabled` / `debugChannels` | 串口联调用于限定调试日志输出范围 |
| `rawFrameLogEnabled` | 原始帧日志开关，联调需要时开启，避免长期放大 SQLite 写入 |
| `realtimeDataEnabled` | 标准单体/组实时表写入开关，确认表结构和字段口径后再切流 |
| `groupCalculationEnabled` | 轻量组计算开关，需在实时表写入稳定后开启 |
| `compatReportLogEnabled` | 旧 `dev_battery_report_log` 兼容历史写入开关，只作为过渡能力 |
| `jsonTcpRealtimeSourceEnabled` | JSON/TCP 从标准实时模型取数的切源开关，默认关闭 |
| `jsonTcpModuleCommandEnabled` | JSON/TCP 蓄电池测试控制迁移开关，默认关闭；开启后仅对已明确映射的测试控制优先走采集模块命令队列 |
| `moduleAddressMissThreshold` | 响应缓存地址连续未响应移除阈值，当前默认 `3` |
| `GET /collector/battery/status` | 采集通道状态观测入口，用于查看通道状态、当前地址、批次、超时和活跃地址等联调信息 |

## 7. 当前待办

### 7.1 P0 状态持久化接入收尾

1. 固化状态码、来源、任务状态枚举，至少覆盖 `ONLINE`、通道串口异常、模块超时/无响应、`246` 新鲜度、工作模式、显式命令状态；落点为 `battery_device_state.state_code/state_value/state_level/source` 与 `dev_opt_log.status`。
2. `BatteryModeStatusService` 继续只做快速模式缓存，模式写入、清理、测试开始和结束时同步 upsert 到 `battery_device_state`；自动编号、内阻测试、连接条测试必须能从表查询到当前态。
3. `BatteryCollectorService` 将串口打开/关闭、通道异常、请求超时、地址缓存变化写入 `battery_device_state`；避免每个单体响应高频写状态表，只在状态变化或关键事件写入。
4. 明确 `dev_opt_log.result` 与新增 `status` 的关系：旧测试继续兼容 `result`，600 显式命令主要使用 `status`，必要时提供映射，避免影响运行中测试、最近测试和容量预测。
5. 显式命令补全 `request_payload/response_payload/error_message`，一条 600 控制命令应能追踪来源、通道、地址、协议码、开始/结束时间、结果和错误原因。
6. 自动编号、手动编号、清调试数据、校准等非传统测试默认使用 `BatteryTestEnum._99`，通过 `command_name/protocol_code` 区分，不污染 `type=1..7` 的既有测试口径。

### 7.2 P0/P1 采集主链路现场联调

1. 现场确认 6 路串口参数：端口、波特率、数据位、停止位、校验位、超时、重试、轮询间隔；先单通道，再逐步打开 6 通道。
2. 确认 `01/81` 每轮固定采集普通单体 `1..245` 和 `246`，`246` 无响应时不清空上一轮组原始值，只更新状态和失败计数。
3. 优化全量发现与地址缓存：启动全量发现、手动重置全量发现、周期全量发现、达到 `BatteryPack.batSinSize` 后可跳过剩余普通地址，模块更换后能重新收敛。
4. 压测 `battery_module_cell_realtime`、`battery_module_group_realtime`、`dev_battery_report_log` 兼容同步和原始帧日志开关，重点观察 SQLite 锁等待、批量 upsert 耗时和 CPU。
5. `BatteryCollectorService` 保持显式命令优先：常态轮询中有其他方式加入的命令时，应在当前 pending 完成或超时后尽快插队执行，不等待整轮自动轮询结束。

### 7.3 P1 控制和测试能力补齐

1. 连接条测试补齐 `0F + 11/91` 状态机：地址推进、`11/91` 读取调度、测试电压读取、连接条电阻计算、超时恢复、模式停止、结果落库和统计链路。
2. 手动编号 `08/88` 按 `packNum + modelNum + newModelNum` 构造命令，成功后重置该组地址缓存并触发全量发现。
3. 自动编号 `18/A8` 继续现场联调：重复启动探测、超时恢复、蜂鸣提示、结果落库；前端不应额外提供 600 现场参数，由内部按 `packNum` 读取组配置。
4. 均衡控制 `03/83` 先补策略和互斥：测试中禁止、权限控制、批量/单体范围限制、操作日志和下发结果口径。
5. 内阻系数 `12/92`、校准/数据校正 `76/F6` 继续确认字节序、负数补码和设备响应码，成功/失败/超时均写入 `dev_opt_log`。

### 7.4 P2 标准模型、告警和长周期算法

1. 页面实时态、JSON/TCP、告警、统计逐步改读标准实时表、缓存和 `battery_device_state`；`dev_battery_report_log` 只保留历史查询、导出和未改造旧功能过渡。
2. 告警输入从三类数据生成：`01/81` 直接状态字段、轮询链路产生的通信/超时状态、组计算和业务参数计算出的阈值结果，最终落到 `dev_alarm_log`。
3. 建立设备故障标准口径，区分串口通道异常、模块无响应、`246` 组模块缺失、energy 本机通信异常，供页面、JSON/TCP 和后续 Modbus 读取。
4. SOC/SOH、核容值、备电时长、放电容量迁移到轮询外后处理服务，优先复用旧 `BatteryPredictorService`、`pre_battery_group`、实时表和历史数据点，禁止在 600 帧解析或轮询线程里写简化算法。
5. 核容/备电/放电过程继续复用 `dev_opt_log`，必要中间状态放标准状态表或现有预测缓存，不新增独立任务表。

### 7.5 P3 JSON/TCP 与 Modbus 后置接入

1. JSON/TCP 蓄电池测试 `_43` 通过 `jsonTcpModuleCommandEnabled` 切源；开启后仅对明确映射的能力走内部控制服务，无法定位通道或未映射时保留兼容回退。
2. 当前 Modbus 只完善只读寄存器映射和空值/无效值口径；已存在 `BatteryModuleModbusReadMappingService` 和 `BatteryModuleModbusReadMappingServiceTest`，不启动 RS485 监听。
3. Modbus 读取来源统一为实时表、`battery_device_state`、标准告警/故障模型；寄存器倍率、偏置、首次未就绪异常和空值口径必须有单测覆盖。
4. RTU 从站运行模型、站号、端口、并发访问、异常码、写寄存器权限需现场确认；Modbus TCP 是否纳入范围仍待确认。
5. 写寄存器必须后置到下行采集、标准模型、JSON/TCP 切源和内部控制服务稳定之后，只能调用内部控制服务，不能拼旧 980 命令。

### 7.6 默认设备边界收尾审计

当前 `configId` 精简主链路已基本完成：通道定位、测试日志缓存、统计缓存、默认设备属性、组配置、告警、历史日志和删除链路已收口到默认设备。后续不再做”大范围继续精简”，只做残留审计和低风险清理。

1. 保留范围：外部 Controller/JSON/TCP 的 `configId/devId` 入参、domain/DTO 的 `configId` 字段、Mapper XML 的 `config_id` 字段、插入默认设备 ID、旧表兼容查询条件。
2. 可清理范围：未被调用的 mapper/service 方法、过期注释、仍把 `configId` 描述为内部设备维度的日志文案、仍按 `configId + packNum` 定位通道的代码。
3. 当前已扫描到的候选残留：`BatteryPackMapper.selectBatteryPackByConfigIds` 及 XML 同名 SQL 如确认无调用可删除；`AlarmLogMapper.clear(Long configId)` 注释和签名需确认是否已收口；`CacheKeyEnum.RESULT` 注释仍写 `device:result:configId:packNum:C3`，应按实际 key 语义修正。
4. `BatteryCollectorChannelConfig.configId`、`BatteryModuleControlCommand.configId`、`BatteryPendingRequest.configId` 继续作为日志/兼容写表字段保留；`BatteryCollectorService.applyCommandChannelContext` 注释应改为”填充兼容写表字段和电池组号”，避免误导。
5. 旧 `BatteryPackHandler` 中 `BatteryMonitor.configId` 只作为旧 JSON/TCP 兼容解析字段，若后续切源到标准实时模型，应整体降级而不是零散删除字段。
6. 每次清理必须先用 `rg` 证明无调用，再删除方法/XML；不能因为字段名出现就删除数据库兼容字段或外部入参。

**审计结果（2026-06-02）：**

| 候选残留 | 处理 | 说明 |
|----------|------|------|
| `BatteryPackMapper.selectBatteryPackByConfigIds` + XML | 已删除 | Java/XML 均无调用 |
| `AlarmLogMapper.clear(Long configId)` + XML | 已删除 | service/controller 均无调用 |
| `CacheKeyEnum.RESULT` 注释 | 已修正 | 按当前缓存 key 的真实业务语义改为 `packNum:modelNum:C3` |
| `BatteryCollectorService.applyCommandChannelContext` 注释 | 已修正 | 改为”填充兼容写表字段和电池组号” |
| `BatteryCollectorChannelConfig.configId` 等字段 | 保留 | 日志/兼容写表字段，不删除 |

### 7.7 AI 执行任务卡

本节用于把计划拆成其他 AI 工具可直接执行的任务。每张任务卡应独立实施、独立验证、独立更新本文档；除非任务明确要求，不要跨卡做大范围重构。

**通用执行规则：**

1. 每个任务开始前先运行 `git status --short`，确认工作区是否存在其他工具或人工改动；不得回滚无关改动。
2. 修改前必须用 `rg` 证明调用点和落点，禁止凭字段名猜测删除或迁移。
3. 代码实现优先复用现有 service、mapper、缓存和测试结构，不新增独立任务表、事件表或临时协议模型。
4. 涉及采集线程的写库、日志和告警必须捕获异常，不能影响串口轮询、显式命令队列和 `waitForPendingComplete`。
5. 每个任务完成后至少执行对应任务卡的验收命令；如命令不可执行，需在任务结果中写明原因。
6. 完成后更新本节任务状态、补充实际修改文件和遗留风险；不要只改代码不更新计划。

**任务交付模板：**

每个后续 AI 工具完成任务时，在本节对应任务卡下追加“执行结果”，格式固定为：

- 执行日期：
- 修改文件：
- 已执行命令：
- 行为变化：
- 未完成/待现场确认：
- 是否建议提交：

**当前可继续派发的剩余任务优先级：**

| 优先级 | 任务 | 状态 | 适合执行方 | 备注 |
|--------|------|------|------------|------|
| P0 | `TASK-BAT-CONNRES-002` | 提交阻断，待执行 | 熟悉 M460 连接条计算的代码工具 | 当前仍将 91 电压写入电阻缓存，需先修正 |
| P0 | `TASK-BAT-CONNRES-001` | 已部分执行，需修正 | 熟悉 600 协议和实时模型的代码工具 | 影响连接条测试闭环，需小步实现和单测 |
| P0 | `TASK-BAT-AUTOADDR-002` | 待审查/补齐 | 熟悉自动编号状态机的代码工具 | 补成功条件、停止帧和失败恢复单测 |
| P0 | `TASK-BAT-AUTOADDR-001` | 待现场联调/补代码 | 熟悉控制命令和模式互斥的代码工具 | 不做阻塞 Web，同步状态和日志即可 |
| P1 | `TASK-BAT-ALARM-002` | 待执行 | 熟悉告警和状态表的代码工具 | 补齐 `CHANNEL_ERROR/MODULE_TIMEOUT/MODULE_ACTIVE` 通信故障 |
| P1 | `TASK-BAT-ALARM-001` | 已部分执行，需补齐 | 熟悉告警和阈值服务的代码工具 | 先做通信状态和阈值输入，不碰未确认鼓包规则 |
| P1 | `TASK-BAT-MODBUS-READ-002` | 待审查/补齐 | 熟悉寄存器映射的代码工具 | `001` 已完成基础映射，继续做状态/告警来源补齐 |
| P1 | `TASK-BAT-OPTLOG-002` | 待执行 | 熟悉 `dev_opt_log` 和旧页面查询的代码工具 | 统一过程日志最终态和旧 `result` 兼容 |
| P1 | `TASK-BAT-STATE-004` | 待执行 | 熟悉状态表和查询接口的代码工具 | 提供页面/第三方可用的当前态查询服务 |
| P2 | `TASK-BAT-EQUILIBRIUM-001` | 待拆解 | 熟悉页面控制和 600 `03/83` 的代码工具 | 先做互斥、日志和单组单体动作，不做策略算法 |
| P2 | `TASK-BAT-JSONTCP-001` | 待拆解 | 熟悉 JSON/TCP 输出链路的代码工具 | 默认不开关切源，先补读取适配和回退 |

**7.7 执行审查记录（2026-06-02）：**

已执行验证：

- `mvn -DskipTests compile`
- `mvn -q clean "-Dtest=BatteryCollectorCommandServiceTest,BatteryModuleModbusReadMappingServiceTest,BatterySyncHandlerTest" test`
- `git diff --check`

发现的问题和待修正项：

1. `TASK-BAT-CONNRES-001` 当前实现将 `91` 响应中的测试电压直接写入 `BatteryModuleCellCompatibilityFillService.putConnectResistance`，最终落到 `resistanceRageSlip` 连接条电阻字段；这只是电压值，不是连接条电阻。后续必须补齐 M460 连接条电阻计算公式、单位和所需电流/参考值来源，不能把电压伪装成电阻。
2. `TASK-BAT-CONNRES-001` 当前只在命令上携带 `connectResistanceNextAddress/connectResistanceMaxAddress` 推进流程，缺少独立流程状态和中断恢复记录。后续需明确重复启动、队列失败、服务停止、串口重连时如何结束 `dev_opt_log` 和工作模式。
3. `TASK-BAT-CONNRES-001` `connectResistanceTest` 在 service 层未强校验 `batteryCount`，仅 controller/sync 做默认值；其他调用方传 `<=0` 时可能只发送 `0F` 而不读取任何单体。后续需在 service 层统一规范化或拒绝。
4. `TASK-BAT-ALARM-001` 当前通信告警只读取 `CHANNEL_OPEN` 的 `stateLevel=error` 和 `GROUP_246_FRESHNESS=stale`，尚未覆盖 `CHANNEL_ERROR`、`MODULE_TIMEOUT`、`MODULE_ACTIVE=inactive`。后续告警适配必须补齐这些状态来源。
5. `TASK-BAT-MODBUS-READ-001` 当前新增“首次数据未就绪抛 `IllegalStateException`”和边界单测，基础方向可接受；状态/告警寄存器仍未接入 `battery_device_state` 或 `dev_alarm_log`，继续放入 `TASK-BAT-MODBUS-READ-002`。

**复审记录（2026-06-03）：**

- 编译和格式检查通过，但 `BatteryCollectorService.storeConnectResistanceResult` 仍调用 `compatibilityFillService.putConnectResistance(batteryGroup, address, voltage)`，只是增加了 TODO 和日志说明；这仍会把 91 测试电压暴露为 `resistanceRageSlip` 连接条电阻，属于提交阻断项。
- `OptLogMapper.findRunningList` 已排除 `type=99`，连接条 91 中间响应也已避免提前写最终成功状态，方向正确；仍需在 `TASK-BAT-OPTLOG-002` 中补齐过程日志摘要和失败分支。
- `BatteryModuleAlarmAdaptService` 已补 `CHANNEL_ERROR/MODULE_TIMEOUT/MODULE_ACTIVE` 读取，方向正确；仍需确认 `MODULE_TIMEOUT` 历史状态是否有恢复/过期口径，避免一次超时长期触发通信异常。
- `BatteryModuleModbusReadMappingService` 已增加状态寄存器读取，方向正确；仍需确认地址 `411483..411488` 是否与 `SH_Modbus寄存器映射草案.md` 完全一致，并补状态寄存器单测。

#### TASK-BAT-STATE-001 统一状态枚举和写入常量（已完成 2026-06-02）

目标：消除 `battery_device_state`、`dev_opt_log.status/source` 的裸字符串扩散，为后续状态入库、页面和 Modbus 读取提供稳定语义。

建议修改范围：

- `03code/energy/src/main/java/com/shanhe/project/collector/battery/model` 或 `protocol` 下新增状态常量/枚举类。
- `BatteryDeviceStateService` / `BatteryDeviceStateServiceImpl`
- `BatteryModeStatusService`
- `BatteryCollectorService`
- `DeviceOnlineJob`

执行步骤：

1. 新增状态常量，至少包含 `scope_type`：`SYSTEM`、`CHANNEL`、`PACK`、`MODULE`；`state_code`：`ONLINE`、`CHANNEL_OPEN`、`CHANNEL_ERROR`、`MODULE_TIMEOUT`、`MODULE_ACTIVE`、`GROUP_246_FRESHNESS`、`WORK_MODE`、`COMMAND_STATUS`；`source`：`COLLECTOR`、`DEVICE_ONLINE_JOB`、`MODE_STATUS`、`WEB`、`JSON_TCP`、`MODBUS`、`AUTO`。
2. 新增命令状态常量，至少包含 `pending`、`success`、`failed`、`timeout`、`rejected`、`cancelled`。
3. 替换当前新增链路中的裸字符串，保留数据库字段值不变，避免破坏既有数据。
4. 在 `BatteryDeviceStateServiceImpl.upsert` 入口做最小校验：`scopeType/scopeKey/stateCode` 不能为空；非法状态只记录日志并跳过写库，不抛出影响采集线程的异常。
5. 更新本文档第 9 章枚举说明。

验收：

- `mvn -q -DskipTests compile`
- `git diff --check`
- 搜索 `rg "\"pending\"|\"timeout\"|\"CHANNEL\"|\"WORK_MODE\"" 03code/energy/src/main/java/com/shanhe/project/collector/battery 03code/energy/src/main/java/com/shanhe/project/scheduled`，新增链路不应继续散落可替换裸字符串。

禁止事项：

- 不修改数据库字段名。
- 不新增独立任务表或事件表。
- 不把状态枚举绑定到旧 980 聚合命令。

#### TASK-BAT-STATE-004 当前态查询服务和适配出口

目标：在 `battery_device_state` 已有写入的基础上，提供稳定的查询服务，供页面、JSON/TCP、Modbus、告警适配读取当前态，避免各处直接拼 `scope_type/scope_key/state_code`。

前置输入：

- `BatteryDeviceStateService` / `BatteryDeviceStateMapper` 当前查询能力。
- `BatteryCollectorCommandController` 当前设备状态接口。
- `BatteryDeviceStateConstants` 状态码和来源枚举。

建议修改范围：

- `BatteryDeviceStateService`
- `BatteryDeviceStateServiceImpl`
- `BatteryDeviceStateMapper` / XML
- `BatteryCollectorCommandController`
- 必要 DTO/VO 和单测。

执行步骤：

1. 用 `rg "selectByScope|battery_device_state|BatteryDeviceStateService|DeviceState"` 扫描当前状态查询入口。
2. 增加面向业务的只读方法：按 pack 查询通道态、模块态、工作模式、246 新鲜度、模块超时列表。
3. 查询方法返回明确 DTO，不让调用方散落比较 `stateCode/stateValue/stateLevel` 字符串。
4. 状态过期策略先只读取 `expire_time` 和 `last_update_time`，不在查询层硬编码过期时间；过期判断如需配置，写入任务说明。
5. Controller 可提供只读查询接口，但不得改变采集线程状态；接口返回结构必须可用于页面和第三方诊断。
6. 告警、Modbus、JSON/TCP 后续任务优先依赖这些 service 方法，不直接访问 mapper。
7. 补单测覆盖：按 pack 聚合、无状态、过期状态、模块列表排序。

验收：

- `mvn -q -DskipTests compile`
- 状态查询 service 单测通过。
- `rg "selectByScope\\(" 03code/energy/src/main/java` 中直接调用点应集中在状态 service 或少量适配器内。

禁止事项：

- 不在查询接口里写库或改变采集状态。
- 不把 `battery_device_state` 当历史明细分页表。
- 不让页面/Modbus 直接依赖 mapper XML。

#### TASK-BAT-STATE-002 完善工作模式当前态入库（已完成 2026-06-02）

目标：`BatteryModeStatusService` 仍作为快速缓存，但所有模式变化同步写入 `battery_device_state`。

建议修改范围：

- `BatteryModeStatusService`
- `BatteryDeviceStateService`
- 相关模式调用点：`BatteryCollectorService`、`ControlBatterySet`、`BatteryCollectorCommandService`

执行步骤：

1. 梳理 `BatteryModeStatusService` 的模式写入、清理、M460 同步入口和 600 命令完成入口。
2. 每次模式变化 upsert 一条 `scope_type=PACK`、`scope_key=packNum`、`state_code=WORK_MODE` 当前态。
3. `state_value` 存模式码字符串，`mode` 存整数模式，`state_level` 空闲为 `normal`，测试/维护中为 `running`，异常恢复失败时为 `warn`。
4. `opt_log_id` 只在由显式命令或测试任务触发时写入；自动缓存同步无日志 ID 时保持空。
5. 模式从测试态变为空闲时必须更新 `last_change_time`，不能只更新 `last_update_time`。
6. 捕获写库异常，只记录 warn，不影响采集和页面互斥判断。

验收：

- `mvn -q -DskipTests compile`
- 增加或更新针对 `BatteryModeStatusService` 的单元测试，覆盖模式进入、重复写入、退出空闲。
- 手动检查 XML upsert 逻辑：状态值变化时 `last_change_time` 更新。

禁止事项：

- 不把所有 M460 系统状态都塞进 `BatteryModeStatusService`。
- 不在模式服务里计算 SOC/SOH/容量。

#### TASK-BAT-STATE-003 采集通道状态入库（已完成 2026-06-02）

目标：串口通道、超时、地址缓存和 `246` 新鲜度形成可查询当前态，供页面、JSON/TCP 和后续 Modbus 使用。

建议修改范围：

- `BatteryCollectorService`
- `BatteryCollectorChannelState`
- `BatteryDeviceStateService`
- 必要时新增小型状态写入 helper，避免轮询主逻辑膨胀。

执行步骤：

1. 在串口打开成功时写入 `scope_type=CHANNEL`、`state_code=CHANNEL_OPEN`、`state_value=open`。
2. 在串口打开失败、读写异常、关闭时写入 `state_value=error/closed` 和错误详情。
3. 在请求超时达到放弃条件时写入 `scope_type=MODULE`、`scope_key=channelName:address`、`state_code=MODULE_TIMEOUT`，并带 `packNum/channelName/modelNum/detail`。
4. 地址从活跃缓存移除时写入 `MODULE_ACTIVE=inactive`；重新响应时写入 `MODULE_ACTIVE=active`。
5. `246` 响应成功/失败单独写 `scope_type=PACK`、`state_code=GROUP_246_FRESHNESS`，成功为 `fresh`，超时或缺失为 `stale`。
6. 加入简单节流：同一 `scope_key + state_code` 的相同 `state_value` 不要在短时间内重复写库；可先用内存 last-state map 做轻量去重。
7. 保证状态写入失败不会阻塞 `waitForPendingComplete` 或自动轮询。

验收：

- `mvn -q -DskipTests compile`
- 新增或更新 `BatteryCollectorService` 相关测试，至少覆盖超时状态和地址缓存状态。
- 本地模拟无串口或写失败时，服务日志可读，线程不退出。

禁止事项：

- 不对每个正常 `01/81` 单体响应逐条写 `battery_device_state`。
- 不把 `battery_device_state` 当历史表使用。

#### TASK-BAT-OPTLOG-001 完善 600 显式命令日志（已完成 2026-06-02）

目标：`dev_opt_log` 可完整追踪 600 控制命令的请求、响应、结果和错误，且不污染旧测试类型。

建议修改范围：

- `BatteryCollectorService`
- `BatteryModuleControlCommand`
- `BatteryPendingRequest`
- `OptLogMapper` / `OptLogMapper.xml`
- `OptLog`

执行步骤：

1. 保持 600 控制命令 `type=BatteryTestEnum._99`，不要使用 `1..7`。
2. 创建命令日志时写入 `request_payload`，内容可为请求帧 hex 或 payload hex；建议同时在 `detail`/`content` 保留人类可读描述。
3. 收到响应时写入 `response_payload`、`response_code`、`status=success/failed`、`ended_at`。
4. 超时时写入 `status=timeout`、`error_message=命令响应超时`、`ended_at`。
5. 串口未打开、写入失败、互斥拒绝时写入 `status=rejected/failed` 和明确原因。
6. 如旧页面依赖 `result is null` 查询运行中测试，新增命令日志不得影响旧 `getRunningOptLog` 语义；必要时查询条件排除 `type=99` 或按 `status` 区分。
7. 更新本文档第 9 章 `dev_opt_log.status/result` 映射说明。

验收：

- `mvn -q -DskipTests compile`
- `mvn -q clean "-Dtest=ControlBatterySetTest,RestoreServiceImplTest" test`
- 检查 `OptLogMapper.xml`：旧测试查询仍能查 `result is null`，600 命令能按 `status` 追踪。

禁止事项：

- 不新增 `battery_operation_task`。
- 不改变旧 `dev_opt_log.result` 对页面测试记录的兼容含义。

#### TASK-BAT-OPTLOG-002 过程日志最终态和旧查询兼容

目标：统一 600 显式命令、连接条、自动编号等过程型任务的 `dev_opt_log` 写入口径，避免中间响应提前把整条日志标记为最终成功，同时不破坏旧页面基于 `result is null` 的运行中测试查询。

前置输入：

- `dev_opt_log` 当前扩展字段：`status/request_payload/response_payload/error_message/started_at/ended_at`。
- `OptLogMapper.xml` 中 `getRunningOptLog/findRunningList/select/count` 查询。
- `BatteryCollectorService.updateCommandOptLog` 当前调用点。

建议修改范围：

- `BatteryCollectorService`
- `OptLogMapper` / `OptLogMapper.xml`
- `OptLogServiceImpl`
- 旧页面运行中测试相关测试。

执行步骤：

1. 用 `rg "updateCommandOptLog|status =|result is null|getRunningOptLog|findRunningList|BatteryTestEnum._99"` 扫描所有过程日志写入和读取点。
2. 明确状态语义：`pending` 表示已入队或流程进行中；`success/failed/timeout/rejected/cancelled` 只作为最终态。
3. 对连接条、自动编号这类多帧流程，新增或复用进度更新方法；中间帧不得调用最终态 `success` 更新。
4. `response_payload` 可以保存最后一次响应或追加摘要，但必须明确是“最后响应”还是“完整过程摘要”；不要静默覆盖导致排障困难。
5. 旧测试 `type=1..7` 继续依赖 `result`；600 显式命令 `type=99` 主要依赖 `status`，旧运行中查询应排除或正确处理 `type=99`。
6. 超时、拒绝、串口未打开、队列失败必须写 `error_message`，不能只写状态。
7. 补单测覆盖：旧运行中测试查询不受 `type=99 pending` 影响；600 命令最终态更新不清空原始 `response_code`。

验收：

- `mvn -q -DskipTests compile`
- `mvn -q clean "-Dtest=RestoreServiceImplTest,BatteryCollectorCommandServiceTest" test`
- `rg "response_code = #\\{responseCode\\}" 03code/energy/src/main/resources/mybatis/device/OptLogMapper.xml` 不应出现无条件覆盖。

禁止事项：

- 不迁移或删除旧 `result` 字段。
- 不把 `type=99` 混入旧内阻/连接条/核容页面测试类型。
- 不新增独立过程日志表。

#### TASK-BAT-COLLECT-001 显式命令优先调度验收（已完成 2026-06-02）

目标：采集是常态化持续轮询，页面/JSON/TCP/未来 Modbus 加入的显式命令必须尽快执行，不能被整轮 `1..245 + 246` 轮询长期阻塞。

建议修改范围：

- `BatteryCollectorService`
- `BatteryCollectorChannelState`
- 相关命令入队服务：`BatteryCollectorCommandService`

执行步骤：

1. 审查当前 `processQueuedModuleCommandsImmediately` 是否在每个地址轮询后执行，并确认 pending 完成或超时后能立即处理队列。
2. 如果显式命令队列仍可能等到整轮结束，调整轮询循环：每个地址完成后检查队列；队列非空时连续处理显式命令，直到队列为空或通道停止。
3. 对无响应命令也要调用完成/失败标记，避免队列卡死。
4. 通道快照中保留 `queuedModuleCommandCount`、当前 pending 请求、最后完成命令和超时状态。
5. 增加测试：构造自动轮询进行中，插入显式命令，断言命令在下一次 pending 释放后优先发送。

验收：

- `mvn -q -DskipTests compile`
- 相关单测通过。
- `GET /collector/battery/status` 能看到队列长度和 pending 状态。

禁止事项：

- 不强行中断已经发出的 pending 请求。
- 不允许显式命令和自动轮询同时占用同一串口等待响应。

#### TASK-BAT-CONNRES-001 连接条测试完整状态机（已完成 2026-06-02）

目标：补齐连接条测试从启动、读取、计算、落库到恢复的完整流程。

前置输入：

- M460 `0F` 启动连接条测试、`11/91` 读取测试电压的协议解析规则。
- 旧页面连接条测试入口、返回结构和 `BatteryTestEnum` 类型。
- 标准实时模型或兼容缓存中可承载连接条电阻结果的字段。

建议修改范围：

- `BatteryCollectorCommandService`
- `BatteryModuleControlCommandService`
- `BatteryCollectorService`
- `BatteryModuleFrameDataParserService`
- 连接条电阻缓存/填充服务
- `dev_opt_log` 写入路径

交付物：

- 一个可由旧入口触发的连接条测试流程。
- 至少 4 个单测：启动成功、读取成功、读取超时、异常恢复。
- 文档记录 `0F`、`11/91` 是否仍需现场确认的字段。

执行步骤：

1. 用 `rg "MODE_CONNECT_RESISTANCE|CONNECT|0F|11|91|BatteryTestEnum"` 扫描现有入口、模式码和解析代码，记录实际调用点。
2. 保持 `0F` 作为连接条测试启动命令，创建 `dev_opt_log`，进入 `MODE_CONNECT_RESISTANCE`，并写 `battery_device_state` 工作模式当前态。
3. 启动成功后调度 `11/91` 读取测试电压；该命令只在测试流程中发送，不加入默认自动轮询。
4. 明确测试地址范围：普通单体 `1..245`，`246` 仅作为组模块，不参与连接条单体结果。
5. 按 M460 行为推进地址：成功则记录该地址结果并推进，超时按任务策略重试或失败结束，停止条件必须可从测试中断言。
6. 解析 `91` 响应后计算连接条电阻，写入标准实时模型或兼容缓存；若字段落点未确认，先封装为独立适配方法并在文档标注待确认。
7. 成功、失败、超时、拒绝均更新同一条 `dev_opt_log` 的 `status/ended_at/error_message/response_payload`，不要产生多个互不关联的日志。
8. 测试结束、异常恢复、重复启动拒绝时均清理 `BatteryModeStatusService` 和 `battery_device_state` 工作模式。
9. 页面旧入口保持返回结构兼容；长流程不得阻塞 Web 请求等待整轮测试完成。

验收：

- `mvn -q -DskipTests compile`
- 增加连接条流程单测：启动成功、读取成功、读取超时、异常恢复。
- 文档更新 `M460协议功能对照表.md` 对应状态。
- `rg "11/91|CONNECT_RESISTANCE|MODE_CONNECT_RESISTANCE" 01document 03code/energy/src/main/java` 能看到协议、入口、状态和文档一致。

禁止事项：

- 不把 `11/91` 加入默认自动轮询。
- 不把连接条结果只留在临时缓存而无可追踪日志。
- 不新增连接条专用历史表。

#### TASK-BAT-CONNRES-002 连接条测试审查修正

目标：修正 `TASK-BAT-CONNRES-001` 部分执行后的语义问题，确保 `91` 测试电压不会被当作连接条电阻写入，并补齐流程异常恢复。

前置输入：

- M460 连接条电阻计算公式、单位、测试电压与电流/参考量的关系。
- 当前 `BatteryModuleCellCompatibilityFillService.putConnectResistance` 和 `BatteryModuleCellRealtime.resistanceRageSlip` 语义。
- 当前 `BatteryCollectorService` 中 `queueNextConnectResistanceVoltageRead`、`storeConnectResistanceResult`、`handleTimedOutPendingRequest` 实现。

建议修改范围：

- `BatteryCollectorService`
- `BatteryModuleCellCompatibilityFillService`
- 连接条测试相关单测
- 必要时新增轻量流程状态对象，但不新增数据库表。

执行步骤：

1. 用 `rg "storeConnectResistanceResult|putConnectResistance|resistanceRageSlip|GET_CONNECT_STRIP_RESISTANCE_VOLTAGE"` 定位所有连接条结果写入和读取点。
2. 若 M460 公式尚未确认，立即停止把 `91` 电压写入 `putConnectResistance`；可暂存为测试电压字段/局部过程值，或仅记录到日志和 `dev_opt_log.response_payload`。
3. 公式确认后，按公式计算真正连接条电阻，再写入 `resistanceRageSlip`；文档中注明单位和倍率。
4. 在 `BatteryCollectorCommandService.connectResistanceTest` service 层统一校验或规范化 `batteryCount`：有效范围 `1..245`，非法值拒绝或默认策略必须明确且有测试。
5. 补齐流程异常恢复：队列 offer 失败、串口关闭、服务停止、单体中途超时、重复启动时，必须结束 `dev_opt_log` 并清理工作模式。
6. 避免每个 `91` 成功都把整条测试日志标记为最终完成；最终地址完成后再写最终 `status=success/ended_at`，中间过程可仅更新 payload/detail 或保留内存进度。
7. 增加单测覆盖：电压不误写电阻、非法 `batteryCount`、中途超时、最后一个地址成功后才结束日志。

验收：

- `mvn -q -DskipTests compile`
- `mvn -q clean "-Dtest=BatteryCollectorCommandServiceTest,BatteryCollectorServiceTest" test`
- `rg "putConnectResistance" 03code/energy/src/main/java/com/shanhe/project/collector/battery/service/BatteryCollectorService.java` 不应出现未经计算的测试电压直写。

禁止事项：

- 不把 91 响应电压直接当连接条电阻。
- 不新增连接条专用历史表。
- 不把连接条流程做成阻塞 Web 长请求。

#### TASK-BAT-MANUALADDR-001 手动编号闭环（已完成 2026-06-02）

目标：旧 `manualModelNum` 入口按 600 `08/88` 完成手动编号，并在成功后重建地址发现。

建议修改范围：

- `ControlBatterySet`
- `BatteryCollectorCommandService`
- `BatteryModuleControlCommandService`
- `BatteryCollectorService`

执行步骤：

1. 保持入参语义：`packNum + modelNum + newModelNum`，外部 `configId` 只作为兼容入参。
2. 校验 `modelNum/newModelNum` 范围为普通单体地址，不允许使用 `246`。
3. 构造 600 `08/88` 命令并入对应通道队列。
4. 成功响应后清理该组地址缓存，触发全量发现。
5. 写入 `dev_opt_log`：`type=99`、`protocol_code=SET_SUBMODULE_ID`、目标地址为旧地址，payload 中保留新地址。
6. 失败和超时返回旧接口可识别的失败响应。

验收：

- `mvn -q clean "-Dtest=ControlBatterySetTest" test`
- 手动编号成功后通道状态显示已请求全量发现。

禁止事项：

- 不要求前端传 600 协议细节。
- 不允许编号到 `246`。

#### TASK-BAT-AUTOADDR-001 自动编号现场联调收尾（已完成 2026-06-02）

目标：自动编号 `18/A8` 使用本地组配置驱动，补齐重复启动、超时恢复、结果落库和现场提示。

前置输入：

- M460 自动编号流程确认记录：246 启动帧、单体推进规则、停止帧和失败响应。
- `BatteryPack.batSinSize`、规格字段与现场模块数量的关系。
- 当前手动编号和地址缓存重置实现。

建议修改范围：

- `ControlBatterySet`
- `BatteryCollectorCommandService`
- `BatteryModuleControlCommandService`
- `BatteryCollectorService`
- `BatteryModeStatusService`

交付物：

- 自动编号启动、推进、停止、失败恢复的状态机代码或明确的状态机 helper。
- 自动编号过程日志写入 `dev_opt_log`，当前模式写入 `battery_device_state`。
- 现场联调清单和失败恢复说明。

执行步骤：

1. 用 `rg "AUTO_MODEL|AUTO_SET|18|A8|resetModuleAddressCache|manualModelNum"` 扫描现有自动/手动编号入口，确认不要重复造入口。
2. 按 `packNum` 读取 `BatteryPack.batSinSize` 和规格，不要求前端传 600 现场参数；缺配置时拒绝并写失败原因。
3. 启动阶段发送 246 启动帧，进入 `MODE_AUTO_MODEL_NUM`，写 `dev_opt_log.status=pending` 和工作模式当前态。
4. 246 返回启动标志后，从 1 号普通单体开始推进；每个单体响应按 M460 已确认字段推进。
5. 达到期望单体数量、收到停止条件或异常终止时发送停止帧；停止失败也必须结束日志和模式。
6. 处理重复启动、启动无响应、单体中途超时、停止失败；所有失败分支都要有可读 `error_message`。
7. 成功或失败均清理模式状态，必要时触发 `resetModuleAddressCache`，下轮全量发现。
8. 蜂鸣提示如无硬件落点，只记录状态和文档，不新增虚假硬件控制。
9. 自动编号不得在 Web 请求线程里同步等待全流程；入口只负责发起或查询状态。

验收：

- `mvn -q clean "-Dtest=ControlBatterySetTest" test`
- 现场联调记录串口请求/响应和失败恢复结果。
- `GET /collector/battery/status` 或等价接口能看到自动编号期间 pending、队列和模式状态。

禁止事项：

- 不恢复旧 980 `_18/A8` 中间下发链路。
- 不把自动编号做成阻塞 Web 请求的长时间同步等待。
- 不允许普通单体编号到 `246`。

#### TASK-BAT-AUTOADDR-002 自动编号状态机审查修正

目标：在自动编号已部分接入 600 `18/A8` 的基础上，审查并补齐成功条件、停止帧、失败恢复、日志和地址缓存重建，形成可现场联调的状态机。

前置输入：

- 当前 `BatteryCollectorService.queueNextAutoSetAddressStep`、`autoSetAddressCommand`、`firstAutoSetAddressCellPayload`、`stopAutoSetAddressPayload`。
- M460 自动编号现场协议记录：246 启动响应、单体响应 payload、停止 payload。
- `BatteryModeStatusService.MODE_AUTO_MODEL_NUM` 和 `dev_opt_log` 当前接入。

建议修改范围：

- `BatteryCollectorService`
- `BatteryCollectorCommandService`
- `BatteryModuleControlCommandService`
- `BatteryModeStatusService`
- 自动编号相关单测。

执行步骤：

1. 用 `rg "AUTO_SET_MODULE_ADDRESS|queueNextAutoSetAddressStep|firstAutoSetAddressCellPayload|stopAutoSetAddressPayload|MODE_AUTO_MODEL_NUM"` 定位自动编号全链路。
2. 明确状态机阶段：启动 246、单体 1..N 推进、停止当前单体、停止 246、完成/失败恢复。
3. 校验成功条件：246 启动响应 payload、单体响应 payload、停止帧是否需要响应；不能只凭响应码或任意 payload 视为成功。
4. 自动编号期望单体数来自 `BatteryPack.batSinSize` 或已确认配置，service 层统一规范化 `1..245`。
5. 所有失败分支必须结束 `dev_opt_log`、清理 `BatteryModeStatusService`、写 `battery_device_state` 工作模式为空闲或失败状态。
6. 成功完成后重置模块地址缓存并请求全量发现；失败时也应评估是否需要全量发现，文档写明策略。
7. 不允许同一 pack 重复启动自动编号；重复启动返回拒绝并保留当前任务状态。
8. 补单测覆盖：启动成功、246 启动失败、单体中途失败、停止失败、完整成功、重复启动拒绝。

验收：

- `mvn -q -DskipTests compile`
- `mvn -q clean "-Dtest=BatteryCollectorServiceTest,BatteryCollectorCommandServiceTest" test`
- 现场联调前输出一份请求/响应样例清单，包含 246 启动、首个单体、最后单体、停止帧。

禁止事项：

- 不恢复旧 980 自动编号链路。
- 不让 Web 请求同步等待完整编号流程。
- 不把停止帧失败静默当成功。

#### TASK-BAT-ALARM-001 标准告警输入和设备故障口径（已完成 2026-06-02）

目标：独立模块告警从标准实时模型和采集状态生成，落到 `dev_alarm_log`，不依赖旧 980 `07/87`、`0D/8D`。

前置输入：

- 当前 `ConfigAttribute`、`AlarmItemLevelVo`、`AlarmLogServiceImpl` 的阈值和恢复规则。
- 标准实时模型中单体电压、内阻、温度、电流、组信息的字段。
- `battery_device_state` 中通道、模块、`246` 当前态定义。

建议修改范围：

- `BatteryModuleAlarmAdaptService`
- `BatteryModulePollContext`
- `AlarmLogServiceImpl`
- `ConfigAttributeServiceImpl`
- `battery_device_state` 读取服务

交付物：

- 一个标准告警输入适配器：输入标准实时数据和当前态，输出既有 `dev_alarm_log` 所需参数。
- 通信故障和阈值告警的最小测试集。
- 未确认告警项清单，尤其是鼓包基准。

执行步骤：

1. 用 `rg "alarmBattery|alarmValid|AlarmItemLevel|TXZT|ConfigAttribute|dev_alarm_log"` 扫描现有告警入口和 itemCode，整理可复用方法。
2. 将告警输入分三类：实时数值阈值、采集通信状态、设备/通道运行状态；每类明确来源字段。
3. 通信状态优先使用 `battery_device_state`：`CHANNEL_OPEN/CHANNEL_ERROR`、`MODULE_TIMEOUT/MODULE_ACTIVE`、`GROUP_246_FRESHNESS`。
4. 阈值判断继续使用 `ConfigAttribute` / `AlarmItemLevelVo`，输出仍走 `AlarmLogServiceImpl`，不新增独立模块告警表。
5. 设备故障细分为：串口通道异常、模块无响应、`246` 缺失、energy 本机通信异常；每类映射到现有或待新增配置项。
6. 告警恢复、确认、屏蔽沿用 `dev_alarm_log` 现有口径；不得绕过已有恢复逻辑直接插入重复告警。
7. 鼓包阈值来源未确认前，只记录输入字段和待确认项，不生成强业务告警。
8. 告警适配器必须可单测，不直接依赖真实串口或长轮询线程。

验收：

- `mvn -q -DskipTests compile`
- 增加告警适配测试：正常、通信异常、阈值触发、恢复。
- `rg "battery_module_alarm|MODULE_ALARM" 03code/energy/src/main/java 03code/energy/src/main/resources` 不应出现新增独立表或独立历史模型。

禁止事项：

- 不把旧 980 `07/87` 或 `0D/8D` 作为独立模块自动轮询命令。
- 不新增独立 `battery_module_alarm` 表。
- 不在告警适配层计算 SOC/SOH 或容量预测。

#### TASK-BAT-ALARM-002 通信故障状态补齐

目标：在已开始接入 `battery_device_state` 的基础上，补齐独立模块通信故障告警输入，避免只覆盖通道关闭和 246 stale。

前置输入：

- `BatteryDeviceStateConstants.StateCode` 当前状态码。
- `BatteryModuleAlarmAdaptService.buildCommunicationAlarmContext` 当前实现。
- `AlarmLogServiceImpl` 对 `TXZT/DTTXZT` 等通信状态 itemCode 的处理。

建议修改范围：

- `BatteryModuleAlarmAdaptService`
- `BatteryDeviceStateService` 查询方法，必要时新增按 pack/stateCode 查询。
- 告警适配单测。

执行步骤：

1. 用 `rg "CHANNEL_ERROR|MODULE_TIMEOUT|MODULE_ACTIVE|GROUP_246_FRESHNESS|DTTXZT|TXZT"` 扫描状态写入和告警消费点。
2. 通道故障同时读取 `CHANNEL_OPEN` 和 `CHANNEL_ERROR`：关闭、打开失败、读写异常都应产生通信异常候选。
3. 模块故障读取 `MODULE_TIMEOUT` 和 `MODULE_ACTIVE=inactive`，至少能按 `packNum` 聚合出该组存在模块无响应。
4. `246` stale 继续映射为组通信异常，但需区分普通单体模块异常和组模块缺失，避免全部挤到同一文案。
5. 告警恢复时要能在状态恢复为 open/active/fresh 后输出正常值，沿用现有 `dev_alarm_log` 恢复逻辑。
6. 补单测：通道关闭、通道异常、普通模块超时、普通模块 inactive、246 stale、全部恢复。

验收：

- `mvn -q -DskipTests compile`
- 告警适配相关单测通过。
- 不新增 `battery_module_alarm` 表。

禁止事项：

- 不直接插入绕过恢复逻辑的告警日志。
- 不把通信故障和阈值告警混成同一判断函数。
- 不使用旧 980 告警命令作为独立输入。

#### TASK-BAT-MODBUS-READ-001 Modbus 只读映射完善（已完成 2026-06-02）

目标：只完善寄存器到标准模型的读取和单测，不启动 RTU/TCP 服务。

建议修改范围：

- `BatteryModuleModbusReadMappingService`
- `BatteryModuleModbusReadMappingServiceTest`
- `SH_Modbus寄存器映射草案.md`

执行步骤：

1. 按草案确认单体区、组区、状态区的来源：实时表、`battery_device_state`、标准告警/故障模型。
2. 补齐倍率、偏置、无效值和首次未就绪异常口径。
3. `246` 不作为普通单体输出到 `1..245` 区间。
4. SOC/SOH 算法未迁移前允许为空或默认未就绪，不在映射服务中临时计算。
5. 增加单测覆盖边界地址、未就绪、空值、无效地址。

验收：

- `mvn -q clean "-Dtest=BatteryModuleModbusReadMappingServiceTest" test`
- 文档草案同步更新。

禁止事项：

- 不启动 RS485 监听。
- 不实现写寄存器控制。
- 不新增 Modbus TCP 服务入口。

#### TASK-BAT-MODBUS-READ-002 状态/告警寄存器来源补齐

目标：在 `TASK-BAT-MODBUS-READ-001` 已完成基础只读映射后，补齐状态区、告警区和未就绪口径，使第三方读取不依赖临时缓存。

前置输入：

- `SH_Modbus寄存器映射草案.md` 中状态区、告警区、保留区定义。
- `battery_device_state` 当前状态码。
- 标准实时表和 `dev_alarm_log` 的可读服务。

建议修改范围：

- `BatteryModuleModbusReadMappingService`
- `BatteryModuleModbusReadMappingServiceTest`
- 可能新增只读查询 adapter，但不要启动 Modbus 服务。
- `SH_Modbus寄存器映射草案.md`

执行步骤：

1. 用 `rg "BatteryModuleModbusReadMappingService|readRegister|battery_device_state|dev_alarm_log"` 确认当前读取来源和测试覆盖。
2. 状态寄存器优先从 `battery_device_state` 读取：通道在线、模块活跃、模块超时、`246` 新鲜度、工作模式。
3. 告警寄存器优先从标准告警/故障模型读取；若告警口径未完成，返回未就绪或保留值，不临时发明告警算法。
4. 明确空值策略：未配置地址、无实时数据、状态过期、告警未初始化分别返回不同的约定值或异常。
5. `246` 仍不得作为普通单体映射到 `1..245` 区间。
6. 增加单测覆盖状态正常、模块超时、告警存在、告警未就绪、非法地址。
7. 更新寄存器草案，标注每个状态寄存器的数据来源和未就绪值。
8. 若实现 `411483..411488` 状态寄存器，必须逐项对照 `SH_Modbus寄存器映射草案.md`；草案未定义的地址不得只在代码中单方面固定。
9. `MODULE_TIMEOUT`、`MODULE_ACTIVE=inactive` 读取需考虑恢复/过期口径，避免一次历史超时导致 Modbus 长期显示异常。

验收：

- `mvn -q clean "-Dtest=BatteryModuleModbusReadMappingServiceTest" test`
- `mvn -q -DskipTests compile`
- 文档草案同步更新。
- 单测必须覆盖 `411483..411488`：通道 open/closed、channel error、active/inactive、timeout/no-timeout、246 fresh/stale、work mode。

禁止事项：

- 不启动 RS485、RTU 或 TCP 监听服务。
- 不实现写寄存器控制。
- 不把 Modbus 读取服务反向写入状态表或告警表。

#### TASK-BAT-EQUILIBRIUM-001 单体均衡控制接入

目标：将页面均衡动作接入 600 `03/83`，补齐互斥、日志、状态和结果查询；先做单组单体动作，不做自动均衡策略。

前置输入：

- M460 `03/83` 均衡协议字段和成功/失败响应。
- 当前页面均衡入口和旧本地状态逻辑。
- 工作模式互斥规则。

建议修改范围：

- `ControlBatterySet`
- `BatteryCollectorCommandService`
- `BatteryModuleControlCommandService`
- `BatteryCollectorService`
- `BatteryModeStatusService`
- `dev_opt_log` 写入路径

执行步骤：

1. 用 `rg "balance|BALANCE|均衡|03|83|BatteryTestEnum"` 扫描旧入口、页面状态和协议定义。
2. 保持页面入参兼容：`packNum + modelNum + 均衡开关/参数`，前端不传 600 协议码。
3. 校验 `modelNum` 只能是普通单体 `1..245`，不允许 `246`。
4. 进入均衡动作前检查工作模式互斥；若已有自动编号、内阻、连接条测试运行，直接拒绝并写明原因。
5. 构造 600 `03/83` 命令入显式队列，写 `dev_opt_log type=99`、`protocol_code`、目标单体、请求 payload。
6. 响应成功/失败/超时后更新 `dev_opt_log.status`，并同步必要的 `battery_device_state` 命令状态或工作模式。
7. 页面查询继续使用旧返回结构，但结果来源应能追踪到 `dev_opt_log`。

验收：

- `mvn -q -DskipTests compile`
- 增加均衡控制单测：参数校验、互斥拒绝、命令入队、响应成功、超时失败。
- `git diff --stat` 不应出现自动均衡策略、容量算法或大范围页面重构。

禁止事项：

- 不实现自动均衡策略。
- 不把均衡动作加入默认自动轮询。
- 不允许对 `246` 组模块下发单体均衡。

#### TASK-BAT-JSONTCP-001 JSON/TCP 实时数据切源准备

目标：为 JSON/TCP 输出从旧 `dev_battery_report_log` 过渡到标准实时模型做只读适配和回退，不直接打开默认切源开关。

前置输入：

- `DataService`、JSON/TCP handler 当前输出字段。
- 标准实时模型和兼容缓存字段。
- `jsonTcpRealtimeSourceEnabled` 当前配置。

建议修改范围：

- `DataService`
- JSON/TCP 相关 handler
- 标准实时模型读取 service
- 必要测试

执行步骤：

1. 用 `rg "jsonTcpRealtimeSourceEnabled|DataService|BatteryReportLog|device:result|BatteryOptResHandler"` 梳理当前输出来源。
2. 为 JSON/TCP 增加只读适配层：优先标准实时模型，缺失时回退旧 `dev_battery_report_log` 或缓存。
3. 保持 `jsonTcpRealtimeSourceEnabled` 默认关闭；实现只允许在配置打开时切源。
4. 明确空值口径：单体缺失、组信息缺失、状态未就绪时字段如何输出，不能随意填 0。
5. 告警和通信状态优先接入 `battery_device_state` 和标准告警模型；未完成时保留旧输出。
6. 增加测试覆盖开关关闭、开关打开且实时数据完整、实时数据缺失回退。

验收：

- `mvn -q -DskipTests compile`
- 相关 JSON/TCP 输出测试通过；没有现成测试时补最小 service 测试。
- 默认配置下外部输出结构和旧行为保持兼容。

禁止事项：

- 不默认打开切源开关。
- 不删除 `dev_battery_report_log` 历史查询和导出依赖。
- 不在 JSON/TCP 输出层计算 SOC/SOH/容量预测。

#### TASK-BAT-CONFIGID-001 默认设备边界残留审计（已完成 2026-06-02）

目标：在 `configId` 主链路已精简的基础上，只清理确认无调用或明显误导的残留，避免过度删除外部兼容字段和数据库字段。

建议修改范围：

- `BatteryPackMapper.java` / `BatteryPackMapper.xml`
- `AlarmLogMapper.java` / `AlarmLogMapper.xml`
- `CacheKeyEnum`
- `BatteryCollectorService`
- 相关注释和日志文案

执行步骤：

1. 执行扫描并保存结果：`rg "selectBatteryPackByConfigIds|void clear\\(Long configId\\)|device:result:configId|configId和电池组号|resolveChannelName\\(Long|selectByConfigId|selectBatteryPackListConfigId" 03code/energy/src/main/java 03code/energy/src/main/resources/mybatis`。
2. 对 `BatteryPackMapper.selectBatteryPackByConfigIds` 做调用确认；若 Java 和 XML 均无调用，删除 mapper 方法、XML SQL 和过期注释。
3. 审查 `AlarmLogMapper.clear(Long configId)`：若 service/controller 已不传可变 configId，则改为无参 `clearDefaultDeviceLogs` 或保留方法但注释写明参数忽略/仅兼容；XML 内部固定 `config_id = 1`。
4. 修正 `CacheKeyEnum.RESULT` 的过期注释，确认 key 是否仍实际包含 configId；如果 key 仍含三段格式但第一段不再是 configId，注释按真实含义更新。
5. 修正 `BatteryCollectorService.applyCommandChannelContext` 注释，明确 `configId` 是兼容写表字段，不参与通道定位。
6. 对 `BatteryPackHandler`、domain/DTO、Controller 入参只做标注，不删除；它们属于外部协议或旧兼容结构。
7. 更新本文档“已完成状态”或本任务卡，记录实际删除/保留项。

验收：

- `mvn -q -DskipTests compile`
- 如改动告警清理链路，执行相关 controller/service 测试；没有现成测试时至少补最小 mapper/service 单测。
- `rg "selectBatteryPackByConfigIds|selectByConfigId|selectBatteryPackListConfigId|resolveChannelName\\(Long" 03code/energy/src/main/java 03code/energy/src/main/resources/mybatis` 不应再出现未说明的旧内部入口。
- `git diff --stat` 不应出现无关大面积改动。

禁止事项：

- 不删除数据库 `config_id` 字段。
- 不改变外部接口兼容入参。
- 不删除 `BatteryMonitor`、`BatteryPack`、`OptLog`、`AlarmLog` 等 domain 上的 `configId` 字段。
- 不把固定 `config_id = 1` 的 XML 条件改成读取调用方传入值。

## 8. 中高风险项

| 事项 | 当前状态 | 风险/处理 |
|---|---|---|
| 连接条测试完整状态机 | `0F` 启动和 `11/91` 解析基础已具备 | 地址推进、结果计算、落库、页面展示、超时恢复仍需补齐 |
| 实际单体均衡 | 页面状态已本地化，600 `03/83` 动作未完整接入 | 需策略服务、权限、互斥和下发结果口径 |
| `delGb` 鼓包基准 | 已确认不是 600 `0A` 清调试数据 | energy 尚无鼓包基准落点，不可直接映射 |
| SOC/SOH/容量预测 | 只保留字段和兼容缓存填充 | 不能写简化算法进轮询线程，需按 M460/旧预测服务迁移 |
| JSON/TCP 实时切源 | `jsonTcpRealtimeSourceEnabled` 默认关闭 | 需确认切换窗口、回退策略、字段空值和外部平台验收 |
| 告警落地口径 | 已有候选上下文和 `TXZT` 基础适配 | 阈值、鼓包、通信异常、设备故障细分、恢复/确认/屏蔽口径需业务确认 |
| 6 通道并发和 SQLite 压力 | 未完成真实现场压力确认 | 批量 upsert、组计算、告警后处理、兼容历史同步需现场观测 |
| M460/energy 状态持久化 | 已改为扩展 `dev_opt_log` + 新增 `battery_device_state`，状态枚举、模式态、通道态和 600 命令日志已基础接入 | 后续重点转向连接条、告警、Modbus 状态读取和 JSON/TCP 切源 |
| 显式命令日志类型污染 | 已将 600 控制命令日志收敛到 `BatteryTestEnum._99` | 后续新增非传统测试时必须继续避免占用 `1..7` 旧测试类型 |
| 运行期数据库删表 | 可删旧表已初步识别 | 运行期升级、现场数据保留、Flyway 记录修复需人工确认 |

## 9. 状态与任务持久化方案

### 9.1 方案审查结论

M460 的“系统状态”不能全部放进 `BatteryModeStatusService`，也不应另起多套任务/事件表导致与 `dev_opt_log` 并存混乱。全局收敛原则是：

1. `BatteryModeStatusService` 只做当前模式快速缓存。
2. `dev_opt_log` 已经是电池测试业务事实表，继续承担操作/测试过程记录，并通过新增字段扩展到 600 显式命令、JSON/TCP、Modbus 等来源。
3. `battery_device_state` 只保存当前态快照，供页面、第三方和后续 Modbus 读取。
4. 不新增独立 `battery_operation_task` 和 `battery_device_state_event`，避免和 `dev_opt_log`、操作日志、告警日志形成多套历史口径。

以下状态需要落库：

1. 设备/通道当前态：采集通道在线、串口异常、模块无响应、`246` 新鲜度、第三方连接状态等，需要给页面、JSON/TCP 和后续 Modbus 读取。
2. 操作/测试过程：自动编号、手动编号、内阻测试、连接条电阻测试、均衡、核容/备电等，统一进入扩展后的 `dev_opt_log`。
3. 长周期算法状态：SOC/SOH、容量预测、核容/备电中间状态，优先复用 `dev_opt_log`、`pre_battery_group`、实时表和后续标准状态表，不进入轮询线程缓存。

### 9.2 表边界

| 表 | 用途 | 覆盖范围 |
|---|---|---|
| `dev_opt_log` | 电池操作/测试事实表 | 页面测试记录、运行中测试查询、容量预测、统计链路、600 显式命令结果、JSON/TCP/Modbus 来源 |
| `battery_device_state` | 当前状态快照 | SYSTEM、CHANNEL、PACK、MODULE 级当前状态，供页面/第三方/Modbus 读取 |

`dev_opt_log` 本次扩展字段：

| 字段 | 用途 |
|---|---|
| `source` | WEB、JSON_TCP、MODBUS、AUTO、COLLECTOR 等来源 |
| `channel_name` | 采集通道名称 |
| `target_type` / `target_address` | PACK、MODULE、CELL 等目标和地址 |
| `mode` / `status` | M460 模式码和统一任务状态 |
| `request_code` / `response_code` / `protocol_code` / `command_name` | 600 模块端命令定位 |
| `request_payload` / `response_payload` / `error_message` | 请求、响应和错误详情 |
| `poll_batch_no` / `started_at` / `ended_at` | 关联采集批次和任务时间 |

### 9.3 后续代码接入原则

1. 先定义稳定枚举：`status`、`scope_type`、`state_code`、`source`，并明确与 `BatteryTestEnum` 的映射。
2. `dev_opt_log` 不迁移、不删除；既有内阻、连接条、核容、浮充、备电、充电测试仍以它满足页面和容量预测依赖。
3. 自动编号、手动编号、清调试数据、校准等非传统测试也可以写入 `dev_opt_log`，使用 `type=99` 或后续扩展 `BatteryTestEnum`，并通过 `command_name/protocol_code` 区分。
4. `BatteryModeStatusService` 保留为快速缓存，写入/清理时同步更新 `battery_device_state`。
5. `BatteryCollectorService` 的串口通道异常、超时、地址缓存变化写入 `battery_device_state`；显式命令结果写入扩展后的 `dev_opt_log`。
6. Modbus 状态寄存器和第三方查询统一从 `battery_device_state` 或标准实时表读取，不直接读取临时缓存。

### 9.4 后续落地顺序

1. 先补枚举和状态映射，避免 `state_code/source/status` 裸字符串继续扩散。
2. 再补 `BatteryModeStatusService` 和 `DeviceOnlineJob` 当前态 upsert 口径，确认 `first_seen_time`、`last_change_time`、`last_update_time` 语义。
3. 再补 `BatteryCollectorService` 通道状态、超时、地址缓存和 `246` 新鲜度入库，控制写库频率。
4. 再补 `dev_opt_log` 显式命令请求/响应 payload、错误原因、旧 `result` 兼容映射。
5. 最后接页面、JSON/TCP、Modbus 读取；未完成前不让外部直接依赖临时缓存。

## 10. 人工确认清单

| 序号 | 事项 | 未确认前处理 |
|---|---|---|
| 1 | `DevBatteryOpt.testType` 其他测试类型和旧 `doCmdOptBatteryTest` 行为 | 不新增未知映射 |
| 2 | 连接条测试完整页面流程 | 只保留命令构造和运行态观测 |
| 3 | 声光告警输出是否继续使用旧 D8 开关量 | 保留最小链路，不扩展为通用开关设备 |
| 4 | JSON/TCP 外部系统是否仍发送旧空调/通用控制 | 保留废弃响应，不恢复任意透传 |
| 5 | JSON/TCP 标准实时模型切源验收方式 | 默认继续读旧兼容缓存/表 |
| 6 | Modbus RTU 端口、站号、异常码、写权限 | 只保留映射草案 |
| 7 | Modbus TCP 是否需要支持 | 暂不实现 |
| 8 | 告警确认、屏蔽、恢复的页面验收口径 | 复用/扩展 `dev_alarm_log` |
| 9 | SOC/SOH/容量预测算法是否完整迁移 | 不在轮询线程实现 |
| 10 | 真实串口参数和 6 通道现场性能 | 使用安全默认值和状态观测入口 |
| 11 | 状态码、来源枚举、`dev_opt_log.status` 与旧 `result` 的映射 | 先保留表结构，代码接入前统一定义 |

## 11. 后续执行要求

每次继续整合必须回填本文档：

1. 明确本次处理的是下行采集、入库计算、旧业务衔接、JSON/TCP、Modbus 还是原 energy 精简。
2. 明确引用的协议文件和命令码。
3. 明确新增或修改的数据对象、表、字段。
4. 明确是否新增旧 `BatteryHandler` 兼容映射；禁止把 600 模块端原始帧直接交给旧 `BatteryHandler`。
5. 明确编译、单测、打包、串口联调结果。
6. 更新已完成/待办/风险状态。
7. 不维护 `resources/dist`。
8. 不自动提交 git；只有用户明确要求提交时再提交。
9. 接手前必须先执行 `git status --short` 和 `git diff --stat`；当前本地 SQLite `03code/energy/sql/rysqlite3.db` 属运行态数据，默认不要提交。
10. 每个后续任务必须说明是否影响 `dev_opt_log`、`battery_device_state`、标准实时表、`dev_battery_report_log` 和 `dev_alarm_log` 的口径。

推荐验证命令：

```bash
mvn -q -DskipTests compile
mvn -q clean "-Dtest=DataReportJobTest,DeviceOnlineJobTest,ControlBatterySetTest,ConfigServiceImplTest,RestoreServiceImplTest" test
mvn -q -DskipTests package
git diff --check
```

最近一次已验证通过：

```bash
git diff --check
mvn -DskipTests compile
mvn clean "-DskipTests=false" "-Dmaven.test.skip=false" "-Dtest=RestoreServiceImplTest" test
```

上一轮全量建议验证通过：

```bash
git diff --check
mvn -q -DskipTests compile
mvn -q clean "-Dtest=DataReportJobTest,DeviceOnlineJobTest,ControlBatterySetTest,ConfigServiceImplTest,RestoreServiceImplTest" test
mvn -q -DskipTests package
```

## 12. 当前工作区注意事项

截至 2026-06-02：

- 当前继续推进蓄电池采集整合、状态持久化、控制命令和 `configId` 精简。
- `03code/energy/sql/rysqlite3.db` 若因运行或测试出现本地变更，默认不提交，必要时由人工单独提交。
- 根目录遗留图片 `扫描.png` 已确认不再保留，后续工作区出现该删除状态按遗留文件清理处理。
- 后续接手必须先执行 `git status --short` 和 `git diff --stat`，不要假设工作区干净。
