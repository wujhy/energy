<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shanhe.project.system.user.mapper.UserMapper">

	<resultMap type="com.shanhe.project.system.user.domain.User" id="UserResult">
		<id     property="userId"        column="user_id"         />
		<result property="loginName"     column="login_name"      />
		<result property="userName"      column="user_name"       />
		<result property="password"      column="password"        />
		<result property="salt"          column="salt"            />
	</resultMap>

	<select id="selectUserList" resultMap="UserResult">
		select user_id, login_name, user_name, password, salt from sys_user
	</select>

	<select id="selectUserById" parameterType="Long" resultMap="UserResult">
		select user_id, login_name, user_name, password, salt from sys_user where user_id = #{userId}
	</select>

	<update id="updateUser" parameterType="com.shanhe.project.system.user.domain.User">
		update sys_user
		<set>
			<if test="password != null and password != ''">password = #{password},</if>
			<if test="salt != null and salt != ''">salt = #{salt},</if>
		</set>
		where user_id = #{userId}
	</update>
</mapper> 